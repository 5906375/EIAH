{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://eiah.local/contracts/imob/market-scan-result.v1.schema.json",
  "title": "IMOB Market Scan Result v1",
  "type": "object",
  "required": ["version", "runId", "sourceStatus", "totalItems", "groups", "confidenceScore", "sourceCoverageScore"],
  "properties": {
    "version": { "const": "1.0" },
    "runId": { "type": "string", "minLength": 1 },
    "sourceStatus": { "type": "string", "enum": ["completed", "empty", "unavailable", "blocked"] },
    "totalItems": { "type": "integer", "minimum": 0 },
    "groups": { "type": "array", "items": { "type": "object", "additionalProperties": true } },
    "confidenceScore": { "type": "number", "minimum": 0, "maximum": 1 },
    "sourceCoverageScore": { "type": "number", "minimum": 0, "maximum": 1 },
    "sourceDataQuality": {
      "type": ["object", "null"],
      "required": ["status", "fillRate", "confidencePenalty", "reasonCodes", "message"],
      "properties": {
        "status": { "type": "string", "enum": ["pass", "degraded", "blocked"] },
        "fillRate": {
          "type": "object",
          "required": ["price", "areaM2", "priceAreaM2"],
          "properties": {
            "price": { "type": "number", "minimum": 0, "maximum": 1 },
            "areaM2": { "type": "number", "minimum": 0, "maximum": 1 },
            "priceAreaM2": { "type": "number", "minimum": 0, "maximum": 1 }
          },
          "additionalProperties": false
        },
        "confidencePenalty": { "type": "number", "minimum": 0, "maximum": 1 },
        "reasonCodes": { "type": "array", "items": { "type": "string" } },
        "message": { "type": "string" }
      },
      "additionalProperties": false
    },
    "priceRange": {
      "type": ["object", "null"],
      "properties": {
        "min": { "type": "number" },
        "max": { "type": "number" },
        "currency": { "const": "BRL" }
      },
      "required": ["min", "max", "currency"],
      "additionalProperties": false
    },
    "disclosure": { "type": ["object", "null"], "additionalProperties": true }
  },
  "additionalProperties": false
}
