# Evidência — correção origem/fingerprint (2026-09-08)

Branch `experiment/signalforward-origin-fingerprint-fix`, base `c590f6118c0a41a5423ae48cea15a79e056dd93c`,
commit desta unidade `768e1b2`.

## Versões (idênticas ao baseline, não atualizadas)
- PostgreSQL 16.11 (pgvector/pgvector:pg16), container descartável `eiah-originfix-poc-pg`, porta 5546
- `@prisma/client@7.2.0`, `@prisma/adapter-pg@7.2.0`
- Node.js v22.17.1, pnpm 10.12.4

## Comandos executados
```
docker run --rm -d --name eiah-originfix-poc-pg -e POSTGRES_PASSWORD=*** -e POSTGRES_DB=originfix_poc -p 5546:5432 pgvector/pgvector:pg16
prisma migrate deploy   # 26 migrations do baseline
prisma migrate diff --from-migrations ./prisma/migrations --to-schema ./prisma/schema.prisma --script > migration.sql
prisma migrate deploy   # aplica a nova migration 20260908072609_origin_scope_fk_and_snapshot
node ./node_modules/typescript/lib/tsc.js -p apps/api/tsconfig.build.json --noEmit
tsx --test apps/api/src/services/signalForward/__tests__/*.test.ts   # 3 execuções
tsx --test apps/api/src/tests/workspace-agent-assignments.fail-closed.test.ts
docker stop eiah-originfix-poc-pg
```

## Resultados (sanitizados, sem credenciais)

`tsc --noEmit`: 0 erros, todas as 3 execuções.

Suíte nova/adaptada (19 testes, A–I): **19/19 passando**, 3 execuções consecutivas
(24.2s / 26.1s / 24.2s). Achado corrigido durante a execução: `AgentMetadata.agent`
é único globalmente (não por tenant) — upsert não-atômico sob paralelismo real
de arquivos de teste causou 1 falha na primeira execução; corrigido trocando
`upsert` por `create` com tolerância a P2002 em `helpers.ts`.

Suíte pré-existente `workspace-agent-assignments.fail-closed.test.ts` (mock,
não integração): **9/10 passando**, 1 skip pré-existente (`PENDING_PR1B`,
campo de expiração ainda não implementado — não relacionado a esta unidade).

## Constraint composta real aplicada (verificação direta no banco descartável)
```
Foreign-key constraints:
    "signal_forward_requests_destination_run_id_tenant_id_works_fkey"
        FOREIGN KEY (destination_run_id, tenant_id, workspace_id)
        REFERENCES runs(id, tenant_id, workspace_id) ON DELETE RESTRICT
    "signal_forward_requests_source_run_id_tenant_id_workspace__fkey"
        FOREIGN KEY (source_run_id, tenant_id, workspace_id)
        REFERENCES runs(id, tenant_id, workspace_id) ON DELETE RESTRICT
```

## Limitações confirmadas nesta unidade
- Autorização individual (vs. tenant/workspace) não resolvida — ver aviso em `signalForwardingService.ts`.
- "Contrato Radar" real não existe no repositório — validação aplicada é mínimo estrutural provisório.
- Fila, worker, interface: fora do escopo, não implementados.
