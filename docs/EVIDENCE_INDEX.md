# EVIDENCE INDEX — EIAH

> Roadmap atual (fonte da verdade): `ROADMAP_UNIFICADO_v8_ATUALIZADO_2026-06-15.md`
> Roadmap anterior (historico): `ROADMAP_UNIFICADO_v8_ATUALIZADO_2026-05-23.md`
> ADR de stack oficial para domain/go-live: `docs/adr/ADR-001-domain-runtime-stack.md`

## Domain & DNS / Go-Live Controlado

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Decisão canônica de stack | `docs/adr/ADR-001-domain-runtime-stack.md` | Declara a stack oficial desta revisão como `Cloudflare + Vercel + Render`, elimina ambiguidade com alternativas e define gatilhos de revisão. |
| Plano de produção IMOB | `docs/Domínio e DNS/PLANO_PRODUCAO_IMOB_EIAH.md` | Define a topologia operacional inicial, papéis por camada, rollout controlado e exigências de fail-closed. |
| Guia prático de domínio e DNS | `docs/Domínio e DNS/Domínio e DNS.md` | Mostra a configuração prática de domínio, DNS, web e API, além das alternativas documentadas para a borda pública. |
| Snapshot DNS/Cloudflare | `ops/evidence/latest/domain-go-live/dns-cloudflare-snapshot.md` | Consolida a decisão de borda/DNS e o hardening já evidenciado para a camada pública. |
| Check TLS Full Strict | `ops/evidence/latest/domain-go-live/tls-full-strict-check.md` | Promove a evidência atual de TLS edge para o pacote canônico de domain/go-live. |
| Resposta pública de health | `ops/evidence/latest/domain-go-live/api-health-response.json` | Materializa o contrato público de `/health` já validado pela suíte automatizada. |
| Smoke DNS/TLS de staging | `ops/evidence/latest/domain-go-live/staging-dns-tls-smoke.md` | Registra o escopo faltante de staging e separa explicitamente o que ainda depende de captura live. |
| Smoke DNS/TLS de produção | `ops/evidence/latest/domain-go-live/production-dns-tls-smoke.md` | Consolida a prontidão de produção com base nas evidências existentes de TLS, origem protegida e WAF. |
| Fail-closed de policy ausente | `ops/evidence/latest/domain-go-live/tenant-policy-fail-closed-403.md` | Prova o bloqueio `403 POLICY_NOT_FOUND` por teste automatizado local. |
| Plano de rollback | `ops/evidence/latest/domain-go-live/rollback-plan.md` | Define os gatilhos e passos mínimos de reversão para borda pública, app e API. |

## Sprint 1 (F5.3) — Evidencias operacionais

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Guia de execucao S1-01 | `docs/ops/s1-f53-e2e-high-staging.md` | Procedimento oficial para validar cadeia HIGH em staging. |
| Relatorio S1-01 (template) | `docs/ops/s1-01-e2e-high-staging-report.md` | Estrutura de evidencias por acao HIGH (Run/SCL/PoU/txId/bundle). |
| Contrato publico txId | `docs/ops/ledger-txid-api-contract.md` | Invariante `txId -> runId -> bundleHash -> bundle` do endpoint publico. |
| Contrato de export bundle | `docs/ops/run-bundle-api-contract.md` | Contrato canônico do endpoint `/api/runs/:id/bundle` para evidência externa. |
| Exposicao de metricas S1-05 | `apps/api/src/routes/metrics-prom.ts` | Publica gauges de missing/mismatch SCL/PoU/txId/bundle e flags de alerta. |

## Sprint 2 (F5.1) — Receipt Canon v1 completo (evidência real)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Endpoint de origem | `apps/api/src/routes/governance.ts:222` | Endpoint público `/api/ledger/:txId` com `receiptCanon` aditivo. |
| Catálogo oficial de reason codes | `docs/ops/reason-codes-catalog.md` | Fonte única para reason codes de receipts/erros/eventos. |
| Política de versionamento de contrato | `docs/ops/receipt-canon-versioning-policy.md` | Regra explícita de major para breaking e minor/patch apenas aditivo. |
| Gate de CI de compatibilidade | `scripts/checkReceiptCanonVersioning.ts` | Falha CI para breaking changes sem bump major e drift de spec/changelog/example. |
| Baseline de compatibilidade | `contracts/receipt-canon.v1.baseline.json` | Snapshot base para avaliação de backward compatibility do schema ativo. |
| Guia externo de consumo + verifier | `docs/ops/receipt-canon-external-verifier.md` | Passos 200/erro/limites e fluxo de validação externa do receipt canon. |
| Verifier CLI de vínculo run/bundle/tx | `scripts/verifyReceiptCanon.ts` | Verificação automatizada de consistência canônica a partir da resposta `/api/ledger/:txId`. |
| Gate CI da cadeia crítica P1 | `scripts/checkP1CriticalChain.ts` | Bloqueia regressão de approval/schema/receipt em fluxos críticos e exige evidência de fail-closed. |
| Gate CI de recorrência da reconciliação P1 | `scripts/checkP1ReconciliationRecurring.ts` | Exige, por padrão, **3 ciclos semanais recentes** com `auditGap=0` e `duplicateSideEffects=0` de forma contínua. |

## Sprint 4 (F5.5) — Outcome/experimentos (S4-03 a S4-06)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Fluxo de experimento (start/decision/rollback/status) | `apps/api/src/routes/governance.ts` | Endpoints de experimento com transições auditáveis, reason codes e timeline por experimento. |
| Auto-rollback em falha de promoção | `apps/api/src/routes/governance.ts` | Bloqueio `PROMOTION_GATE_FAILED` e rollback automático auditável quando habilitado. |
| Endpoint de telemetria FP/FN | `apps/api/src/routes/governance.ts` | `GET /api/governance/telemetry/fpfn` com `windowDays`, `methodVersion` e cálculo versionado. |
| Runbook operacional de experimentos | `docs/ops/governance-experiments-runbook.md` | Procedimento de operação, incidentes e critérios de decisão para experimentos em shadow/promoção. |

## Sprint 5 (F5.6 + Track P) — DocOps depreciação/sunset (S5-05)

Sem novos artefatos adicionais nesta revisão; manter rastreabilidade pelos runbooks e checks ativos.

## Sprint 5 (F5.3/F5.4/F5.5/F5.6 + Track P) — Fechamentos v7 (2026-02-25)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Runbook operacional de Interop | `docs/ops/interop-runbook.md` | Procedimento operacional para gerar evidência recorrente e tratar incidentes de interop. |
| Policy canônica de risco por ação | `docs/ops/risk-tiering-by-action.md` | Fonte oficial `action -> tier -> txIdRequired` usada pelo gate HIGH no CI. |
| Runbook operacional de Economy/disputas | `docs/ops/economy-dispute-runbook.md` | Procedimento, incidentes e critérios operacionais para F5.6. |
| Runbook operacional de webhook billing | `docs/ops/billing-webhook-runbook.md` | Operação de assinatura/replay/idempotência e playbook de incidente. |
| Política operacional de billing da EIAH | `docs/operations/eiah-billing-operational-policy.md` | Política executiva e runbook de operação para custos, reconciliação, quotas, gates e resposta por perfil. |
| Matriz de acesso da plataforma EIAH | `docs/operations/eiah-access-matrix.md` | Separação formal entre Founder Global, Tenant Admin, Workspace Admin e Usuário final, com escopo e telas esperadas. |
| Runbook operacional de DocOps | `docs/ops/docops-runbook.md` | Rotina por ciclo, critérios de estabilidade e tratamento de drift documental. |

## Operação contínua (marco 2026-03-04)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Smoke de ruleset/branch protection em `main` | `ops/evidence/latest/branch-protection-smoke-2026-03-04.md` | Confirmação operacional de que o fluxo de PR em `main` exige checks obrigatórios antes de merge. |
| APE Weekly Cycle #1 (shadow/pilot) com GO hard metrics | `ops/evidence/latest/ape-weekly-cycle-run7-2026-03-04.md` | Run #7 em `main` com `decision=GO`, `hardMetricsGo=true`, `auditGap=0`, `duplicateSideEffects=0`, `breakGlass=0` e artefatos semanais completos. |
| APE Weekly Cycle #2 (shadow/pilot) com GO hard metrics | `ops/evidence/latest/ape-weekly-cycle-run8-2026-03-04.md` | Segundo ciclo consecutivo com `decision=GO`, `hardMetricsGo=true`, `hardReasons=[]`, `auditGap=0`, `duplicateSideEffects=0`, `breakGlass=0` (estabilidade consecutiva atingida). |
| APE Weekly Cycle #3 (governança recorrente) | `ops/evidence/latest/ape-weekly-cycle-run9-2026-03-09.md` | Terceiro ciclo dentro da janela de recorrência com `auditGap=0` e `duplicateSideEffects=0`, sustentando fechamento operacional de P1. |
| APE Weekly Cycle #10 (janela renovada) | `ops/evidence/latest/ape-weekly-cycle-run10-2026-03-18.md` | Renovação da janela recorrente com `decision=GO`, `hardMetricsGo=true`, `nonRegressionGo=true`, `auditGap=0`, `duplicateSideEffects=0` e `breakGlass=0`. |
| APE Weekly Cycle #11 (continuidade operacional) | `ops/evidence/latest/ape-weekly-cycle-run11-2026-03-18.md` | Confirma que o conjunto mais recente de ciclos APE permanece dentro da janela exigida pelos gates P1, P3 e P4. |
| APE Weekly Cycle #19 (janela recorrente reaberta) | `ops/evidence/latest/ape-weekly-cycle-run19-2026-04-10.md` | Reabre a janela de recorrência com `hardMetricsGo=true`, `auditGap=0`, `duplicateSideEffects=0` e `breakGlass=0`. |
| APE Weekly Cycle #20 (estabilidade recorrente) | `ops/evidence/latest/ape-weekly-cycle-run20-2026-04-10.md` | Mantém o segundo ciclo consecutivo recente em `GO`, sem gap de auditoria e sem side effects duplicados. |
| APE Weekly Cycle #21 (gates P1/P3/P4 renovados) | `ops/evidence/latest/ape-weekly-cycle-run21-2026-04-10.md` | Sustenta o terceiro ciclo recente exigido pelos checks recorrentes de reconciliação, estabilidade e rollout. |
| APE Weekly Cycle #22 (janela recorrente renovada) | `ops/evidence/latest/ape-weekly-cycle-run22-2026-04-27.md` | Reabre a janela válida dos gates recorrentes com `hardMetricsGo=true`, `auditGap=0`, `duplicateSideEffects=0` e `breakGlass=0`. |
| APE Weekly Cycle #23 (continuidade recorrente) | `ops/evidence/latest/ape-weekly-cycle-run23-2026-04-27.md` | Mantém o segundo ciclo recente em `GO`, sem gap de auditoria e sem side effects duplicados. |
| APE Weekly Cycle #24 (janela P1/P3/P4 regularizada) | `ops/evidence/latest/ape-weekly-cycle-run24-2026-04-27.md` | Completa o trio recente exigido pelos checks recorrentes de reconciliação, estabilidade e rollout. |
| APE Weekly Cycle #25 (janela recorrente automatizada) | `ops/evidence/latest/ape-weekly-cycle-run25-2026-05-11.md` | Ciclo semanal automatizado registrando o estado atual dos gates recorrentes para acompanhamento operacional. |
| APE Weekly Cycle #26 (janela recorrente automatizada) | `ops/evidence/latest/ape-weekly-cycle-run26-2026-05-11.md` | Ciclo semanal automatizado com hard metrics em GO, reconciliacao estavel e renovacao da janela recorrente. |
| APE Weekly Cycle #27 (janela recorrente automatizada) | `ops/evidence/latest/ape-weekly-cycle-run27-2026-05-11.md` | Ciclo semanal automatizado com hard metrics em GO, reconciliacao estavel e renovacao da janela recorrente. |
| APE Weekly Cycle #28 (janela recorrente automatizada) | `ops/evidence/latest/ape-weekly-cycle-run28-2026-05-11.md` | Ciclo semanal automatizado com hard metrics em GO, reconciliacao estavel e renovacao da janela recorrente. |
| APE Weekly Cycle #38 (janela recorrente automatizada) | `ops/evidence/latest/ape-weekly-cycle-run38-2026-06-24.md` | Ciclo semanal automatizado com hard metrics em GO, reconciliacao estavel e renovacao da janela recorrente. |
| APE Weekly Cycle #39 (janela recorrente automatizada) | `ops/evidence/latest/ape-weekly-cycle-run39-2026-06-24.md` | Ciclo semanal automatizado com hard metrics em GO, reconciliacao estavel e renovacao da janela recorrente. |
| APE Weekly Cycle #40 (janela recorrente automatizada) | `ops/evidence/latest/ape-weekly-cycle-run40-2026-06-24.md` | Ciclo semanal automatizado com hard metrics em GO, reconciliacao estavel e renovacao da janela recorrente. |
| APE Weekly Cycle #41 (janela recorrente automatizada) | `ops/evidence/latest/ape-weekly-cycle-run41-2026-07-08.md` | Ciclo semanal automatizado registrando o estado atual dos gates recorrentes para acompanhamento operacional. |
| APE Weekly Cycle #42 (janela recorrente automatizada) | `ops/evidence/latest/ape-weekly-cycle-run42-2026-07-08.md` | Ciclo semanal automatizado registrando o estado atual dos gates recorrentes para acompanhamento operacional. |
| APE Weekly Cycle #43 (janela recorrente automatizada) | `ops/evidence/latest/ape-weekly-cycle-run43-2026-07-08.md` | Ciclo semanal automatizado registrando o estado atual dos gates recorrentes para acompanhamento operacional. |
| APE Weekly Cycle #44 (janela recorrente automatizada) | `ops/evidence/latest/ape-weekly-cycle-run44-2026-07-09.md` | Ciclo semanal automatizado registrando o estado atual dos gates recorrentes para acompanhamento operacional. |
| APE Weekly Cycle #45 (janela recorrente automatizada) | `ops/evidence/latest/ape-weekly-cycle-run45-2026-07-13.md` | Ciclo semanal automatizado registrando o estado atual dos gates recorrentes para acompanhamento operacional. |

## Sprint P1 (Imobiliaria Digital) — Interop Protocol Layer (2026-03-09)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Contrato API do Agent Protocol (`discovery/negotiate/execute`) | `docs/ops/agent-protocol-api-contract.md` | Contrato canônico inicial da camada protocolar para interop entre agentes/sistemas. |
| Schema versionado Agent Protocol v1 | `contracts/agent-protocol.v1.schema.json` | Forma oficial do contrato (`action/version/tier/txIdRequired/inputSchema/receiptSchema/trustRequirements`). |
| Baseline de compatibilidade Agent Protocol | `contracts/agent-protocol.v1.baseline.json` | Snapshot mínimo para detectar breaking changes sem major bump. |
| Exemplo oficial do contrato v1 | `contracts/examples/agent-protocol.v1.example.json` | Exemplo validável para `realestate.apply_adjustment` v1.2.0. |
| Política de versionamento Agent Protocol | `ops/contracts/agent-protocol-versioning-policy.md` | Regras explícitas de versionamento/compatibilidade para o protocolo. |
| Gate de CI Agent Protocol compat | `scripts/checkAgentProtocolVersioning.ts` | Check automatizado de compatibilidade/baseline no pipeline. |
| Smoke de rotas interop | `ops/evidence/latest/interop-routes-smoke-2026-03-09.json` | Prova de implementação das rotas `POST /api/agents/discovery|negotiate|execute`. |
| Evidência e2e da cadeia interop | `ops/evidence/latest/interop-e2e-agent-call-2026-03-09.json` | Prova da trilha `discovery -> negotiate -> execute -> verify receipt`. |
| Gate complementar global de cobertura HIGH | `scripts/checkP2HighGlobalCoverage.ts` | Inventário completo das ações HIGH do core (`billing/finance/notifications`) e status explícito de cobertura E2E. |
| Evidência do inventário HIGH global | `ops/evidence/latest/p2-high-global-coverage.json` | Matriz inicial de cobertura HIGH global (base para fechar P2 além do recorte IMOB). |

### Trilha C (P2) — Foco A2A explícito

- **Contrato canônico público**: Agent Protocol (`discovery/negotiate/execute`) é a base oficial de interop entre agentes.
- **Operação padrão**: fluxo `discovery -> negotiate -> execute` com evidência de smoke e e2e.
- **Evolução contratual**: versionamento, baseline e gate de compatibilidade bloqueiam breaking sem major.

## Sprint P1 (Imobiliaria Digital) — Economy Base (2026-03-09)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Evidência de schema e rotas PaymentIntent | `ops/evidence/latest/payment-intent-schema-YYYY-MM-DD.json` | Prova da camada `PaymentIntent` com campos mínimos e índices operacionais; o gate usa sempre o artefato mais recente. |
| Evidência e2e de PoU-gated payment release | `ops/evidence/latest/pou-gated-payment-e2e-YYYY-MM-DD.json` | Prova da bifurcação `blocked` sem PoU/SCL e `released` com PoU/SCL válido; o gate usa sempre o artefato mais recente. |
| Evidência e2e de PoU-gated payment release (2026-07-13) | `ops/evidence/latest/pou-gated-payment-e2e-2026-07-13.json` | Artefato real referenciado pelo gate W4 de não-regressão para release de pagamento governado por PoU. |
| Evidência e2e de settlement providers | `ops/evidence/latest/settlement-provider-e2e-YYYY-MM-DD.json` | Prova de providers em modo suportado por ambiente (`stripe=full`, `crypto/bank=simulated`) + settlement com vínculo em ledger; o gate usa sempre o artefato mais recente. |
| Evidência de replay/idempotência webhook billing | `ops/evidence/latest/billing-webhook-replay-YYYY-MM-DD.json` | Prova de replay rejeitado com `duplicateSideEffects=0`; o gate usa sempre o artefato mais recente. |
| Contrato público de settlement provider | `ops/contracts/settlement-provider-contract.v1.json` | Contrato versionado de endpoints/providers/status e política de assinatura/idempotência. |
| Runbook operacional de settlement provider | `docs/ops/settlement-provider-runbook.md` | Procedimento operacional para PaymentIntent, release gate, settlement e incidente de webhook. |
| Gate CI de drift contrato/implementação | `scripts/checkSettlementContractDrift.ts` | Falha CI em drift entre contrato publicado e runtime (`providers/endpoints`). |
| Evidência de execução do gate de drift | `ops/evidence/latest/settlement-contract-check-YYYY-MM-DD.md` | Resultado do check `pnpm check:settlement-contract-drift` com `ok=true`. |
| Gate CI de hardening econômico P3 | `scripts/checkP3EconomyHardening.ts` | Bloqueia regressão de invoice/settlement/webhook/disputa/reputação e vínculo `receipt -> ledger -> provider`. |
| Gate CI de estabilidade recorrente P3 | `scripts/checkP3StabilityRecurring.ts` | Exige, por padrão, 3 ciclos APE recentes com `hardMetricsGo=true`, `auditGap=0`, `duplicateSideEffects=0` e `breakGlass=0`. |
| Gate CI de recência de evidência P3 | `scripts/checkP3EvidenceRecency.ts` | Exige evidência econômica recente por pattern (`YYYY-MM-DD`) e falha fechado quando a trilha fica stale. |
| Gate CI de suporte por provider/ambiente | `scripts/checkP3SettlementSupportByEnv.ts` | Exige que a evidência mais recente de settlement respeite a matriz de suporte por ambiente. |

## Sprint P1 (Imobiliaria Digital) — Reputação + Disputas (2026-03-09)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Smoke de reputação por agente | `ops/evidence/latest/agent-reputation-smoke-2026-03-09.json` | Snapshot de reputação por `tenant/workspace/agent` com campos operacionais esperados. |
| Fluxo de atualização por eventos | `ops/evidence/latest/reputation-update-flow-YYYY-MM-DD.json` | Atualização idempotente por `receipt.finalized` e `dispute.closed` com journal de eventos; o gate usa sempre o artefato mais recente. |
| Evidência e2e do lifecycle de disputa | `ops/evidence/latest/dispute-lifecycle-e2e-YYYY-MM-DD.json` | Fluxo `open -> under_review -> resolved` com bloqueio de transição inválida e impacto na reputação; o gate usa sempre o artefato mais recente. |
| API de reputação/disputas | `apps/api/src/routes/billing.ts` | Endpoints de reputação e ciclo de disputa em escopo tenant/workspace. |
| Serviço core de reputação/disputas | `apps/api/src/services/reputationDisputes.ts` | Modelo operacional, regras de transição e idempotência de replay por `event_key`. |

### Trilha D (P3) — Foco Economy explícito (A2A/B2B)

- **Invoice**: cálculo e fechamento mensal por tenant/workspace como base financeira verificável.
- **Settlement**: execução por provider com contrato público e reconciliação operacional.
- **Webhook billing**: assinatura + proteção de replay + idempotência como requisito de produção.
- **Disputa + reputação**: ciclo de resolução com impacto verificável de confiança.
- **Vínculo canônico de prova econômica**: `run -> receipt -> ledger -> provider settlement`.
- **Objetivo A2A/B2B**: provar que cooperação entre agentes gera transação empresarial auditável, não apenas resposta de chat.

## Sprint P1 (Imobiliaria Digital) — Produto/Piloto (2026-03-09)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Ações críticas imobiliárias HIGH | `ops/evidence/latest/realestate-high-actions-e2e-2026-03-09.json` | Contrato/negociação com `tier=HIGH`, `txIdRequired=true` e receipt canon para ações imobiliárias críticas. |
| Command Center IMOB (funnel + blocked runs) | `ops/evidence/latest/realestate-command-center-smoke-2026-03-09.md` | Visualização operacional no Runs por workspace, filtros por risco/estado e export bundle/receipt por run. |
| Chat IMOB agentic E2E por planner/recipes | `ops/evidence/latest/imob-e2e-case-planner-smoke-2026-05-16.md` | Prova focada de recipeId, missão de temporada, supressão de CTAs resolvidas, `property.link_owner` e `case.review` como recuperação válida. |
| IMOB Lead Continuity P0 — fechamento operacional | `ops/evidence/latest/imob-lead-continuity-p0-2026-05-23.md` | Prova focada de `leadStatus`/`nextAction`, idempotência por `case.leadId`, continuidade sem reabrir pendência resolvida, compat layer sem copy contraditória e launcher IMOB `render-only` sem heurística local. |
| IMOB Orchestrator Mission Backbone P0 — fechamento operacional | `apps/api/src/services/imob/orchestrator/` + `apps/api/src/tests/imob-orchestrator-mission-e2e.test.ts` + `apps/api/src/tests/imob-orchestrator-recovery-e2e.test.ts` + `apps/api/src/tests/imob-orchestrator-concurrency-e2e.test.ts` + `apps/api/src/tests/imob-orchestrator-proof-e2e.test.ts` + `apps/web/src/components/agents/chatLauncherEngine.test.ts` | Backbone entregue com mission graph, case state, operation router, recovery resolver, CRM projection, `nextAction` única, `missionStatus`, proof por missão e suíte E2E scoped cobrindo concorrência, idempotência, recovery, proof e boundary do launcher. |
| IMOB Orchestrator PR7 — contract/commission/commercial slices | `apps/api/src/tests/prepare-contract-e2e.test.ts` + `apps/api/src/tests/commission-settlement-e2e.test.ts` + `apps/api/src/tests/commercial-activation-e2e.test.ts` + `apps/api/src/services/imob/orchestrator/imobMissionPolicy.ts` | Expansão P1/P2 cobre `prepare_contract`, `settle_commission` e `commercial_activation` no backbone canônico com `nextAction`, proof mínima, `missionStatus` governado e gates scoped sem depender de provider externo, outbound real ou publish real. |
| IMOB Market Scan P0 — no ID leak + policy judge | `ops/evidence/latest/imob-market-scan-p0-no-id-leak-policy-judge.md` | Evidência de que a saída visível do Market Scan não expõe IDs internos e que recomendação forte passa por `marketScanPolicyJudge` com `evidenceBundleId`. |
| IMOB Market Scan P1 — Source Data Quality Gate | `ops/evidence/latest/imob-market-scan-p1-source-data-quality-gate.md` | Evidência de fill-rate `price`/`areaM2`/`priceAreaM2`, status `pass/degraded/blocked`, bloqueio antes de scoring, penalidade de confiança e Guardian hash com qualidade de fonte. |
| Comissão integrada ao settlement | `ops/evidence/latest/realestate-commission-settlement-e2e-YYYY-MM-DD.json` | Fluxo comissão com PoU-gate, settlement e reconciliação com reprocessamento idempotente; o gate usa sempre o artefato mais recente. |
| Comissão integrada ao settlement (2026-07-13) | `ops/evidence/latest/realestate-commission-settlement-e2e-2026-07-13.json` | Artefato real referenciado pelo gate W4 de não-regressão para comissão imobiliária integrada ao settlement. |
| Piloto comercial controlado | `ops/evidence/latest/realestate-pilot-rollout-2026-03-09.md` | Plano `shadow -> pilot -> small` com 3 tenants de referência para rollout da vertical. |
| Rotas IMOB Command Center | `apps/api/src/routes/imob.ts` | Endpoints determinísticos de funil/bloqueios para operação imobiliária. |

## Sprint P1 (Imobiliaria Digital) — W4 D5/D6 (2026-03-09)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Template multi-vertical reutilizável | `ops/verticals/template/vertical-template.md` | Padrão oficial para novos verticais (`IMOB/LEGAL/HEALTH`) sem regressão no core. |
| Checklist de onboarding por vertical | `ops/verticals/vertical-onboarding-checklist.md` | Sequência operacional para ativação, gating, evidência e economia por vertical. |
| KPI/gates de não-regressão | `ops/evidence/latest/w4-non-regression-kpis.json` | Métricas mínimas de W4 (`activation`, `first-run`, `receiptCoverage`, `crossTenantAuthFailures=0`). |
| Evidência D5 (template + checklist) | `ops/evidence/latest/w4-vertical-template-onboarding-2026-03-09.md` | Conclusão operacional de D5 com status `GO`. |
| Evidência APE final W4 | `ops/evidence/latest/ape-weekly-cycle-run9-2026-03-09.md` | Fechamento semanal com `hardMetricsGo=true` e `nonRegressionGo=true`. |
| Gate CI W4 | `scripts/checkW4NonRegression.ts` + `.github/workflows/ci.yml` | Validação automatizada obrigatória de D6 no pipeline. |

## Governança de agentes e DocOps (2026-03-16)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Gerador DocOps do registry de agentes | `scripts/generateAgentRegistryDocs.ts` | Deriva catálogo e exemplos diretamente do registry canônico para reduzir drift entre perfis, docs e evidências. |
| Catálogo de agentes derivado do registry | `docs/ops/agent-registry-catalog.md` | Snapshot documental dos agentes com modelo, `llmUsageMode`, resolução de conflito, fallback e fontes determinísticas declaradas. |
| Exemplos de resposta derivados do registry | `docs/ops/agent-response-examples.md` | Exemplos canônicos de resposta/grounding por agente sem depender de LLM externa. |
| Evidência gerada do DocOps de agentes | `ops/evidence/latest/agent-registry-docs-2026-03-16.json` | Evidência materializada da geração com contagem de agentes e fontes declaradas por perfil. |
| Cobertura comportamental de knowledge policy | `apps/api/src/tests/knowledge-policy.behavior.test.ts` | Testes por agente para `source missing`, `source conflict`, `grounded response` e `blocked response` no `KnowledgeGate`. |

## Assistentes de coding / Governança operacional

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Instruções comuns para assistentes de coding | `IA_EIAH.md` | Define a fonte operacional comum para Claude Code, Codex e assistentes equivalentes, incluindo leitura obrigatória do roadmap v8.1, regra agent-driven, fail-closed, formato final com agentes envolvidos, resumo de alterações e atualização do Evidence Index para evidências reais. |
| Ponte Claude Code | `CLAUDE.md` | Redireciona Claude Code para `IA_EIAH.md`, evita duplicação de regras e preserva a precedência das fontes normativas do projeto. |
| Ponte Codex IDE | `CODEX.md` | Redireciona Codex no VS Code/IDE para `IA_EIAH.md`, evita duplicação de regras e preserva a precedência das fontes normativas do projeto. |
| Redis fail-closed no core publisher | `ops/evidence/latest/redis-fail-closed-full-coverage-2026-07-02.md` | Prova a remoção do fallback localhost no `redisPublisher` do core, a adoção de lazy-init fail-closed com precedência `RUN_EVENTS_REDIS_URL ?? REDIS_URL`, o gate `check:redis-fail-closed` bloqueante em CI e a limitação residual de barrel root ainda afetado por side effect pré-existente fora do escopo do PR-1. |
| Redis fail-closed no barrel raiz do core | `ops/evidence/latest/redis-fail-closed-core-barrel-2026-07-02.md` | Prova a remoção do side effect residual de Redis no import do barrel raiz `@eiah/core`, com façade lazy em `queue/connection`, barrel importando sem env Redis, fail-closed preservado no momento de uso real e consumidor residual migrado para subpaths `@eiah/core/queue/*`. |
| Guard de batch para market scan ambíguo no IMOB CRM | `ops/evidence/latest/imob-market-scan-batch-guard-2026-07-08.md` | Evidência real da correção mínima no extractor de lote para impedir que `composedIntents` promovam uma frase única de captação ambígua para `crm.batch.intake` sem sinal estrutural real. Registra testes focados passando (`batch`, `turn-engine`, `turn-resolver`, `semantic-intent`, `workspace_case_list`) e execução real do contract test HTTP mostrando que o subtest 5 agora passa por `action=crm.market_scan.offer` e `flow=property.market_scan`, restando apenas uma falha residual de CTA em `apps/api/src/tests/imob.chat.resolve-turn.contract.test.ts:344`. |
| Preservação do card canônico em `crm.market_scan.offer` | `ops/evidence/latest/imob-market-scan-offer-card-preservation-2026-07-08.md` | Evidência real da subcorreção no `imobCrmTurnEngine` para preservar `presentation.card` canônico quando o resultado base de `resolveImobTurn()` já possui card e o resultado registration-aware o perde sem oferecer `blocks` substitutivos. Registra payload E2E antes/depois mostrando `card=null` antes e `presentation.card.ctas[0]={label:\"Fazer varredura de mercado\",nextMessage:\"fazer varredura de mercado\"}` depois, além de testes focados passando. O contract test HTTP avança além da antiga falha da linha 344 e expõe novos bloqueios residuais mais profundos: `selectionCta` no passo pós-scan e teardown sem cleanup de `imob_market_scan_runs`. |
| Export específico do core para `redisPublisher` | `ops/evidence/latest/core-redis-publisher-export-2026-07-07.md` | Evidência real da correção mínima de packaging em `packages/core/package.json`, expondo `./events/redisPublisher` com artefatos `dist` já existentes. Registra `ChatAgentLauncher.tsx` sem diff, import `IMPORT_OK` no contexto `apps/api`, remoção do bloqueio `ERR_PACKAGE_PATH_NOT_EXPORTED` para o subpath e o bloqueio residual do contract test HTTP por `sandbox/runtime` com dependência real de Postgres `127.0.0.1:5433` e Redis local potencialmente envolvido. |
| Runtime real do contract test IMOB `resolve-turn` | `ops/evidence/latest/imob-contract-test-runtime-2026-07-07.md` | Evidência real de validação operacional do runtime local exigido por `apps/api/src/tests/imob.chat.resolve-turn.contract.test.ts`: `eiah-postgres` e `eiah-redis` saudáveis no `docker ps`, conectividade confirmada em `127.0.0.1:5433` e `127.0.0.1:6379`, `select 1` no banco `eiah_builder` com `postgres:senha`, execução real do contract test fora do sandbox sem `ERR_PACKAGE_PATH_NOT_EXPORTED`, sem `connect EPERM` e sem hang de bootstrap. O teste fecha em `pass 6 / fail 1 / skipped 1`, com falha funcional objetiva no subtest 5 (`actual=crm.batch.intake`, `expected=crm.market_scan.offer`). |
| Plano de implementação multicanal EIAH v1 | `ops/evidence/latest/eiah-multichannel-plan-investigation-2026-07-09.md` | Evidência real da investigação read-only que gerou `docs/architecture/EIAH_MULTICHANNEL_IMPLEMENTATION_PLAN_v1.md`, delimitando web desktop, web mobile e WhatsApp como evolução futura por contrato/engine, com adapter WhatsApp render-only, fail-closed, binding obrigatório, replay/idempotência, HITL, receipt/ledger quando aplicável e status conservador `parcial/evidenciado`, sem alteração de runtime ou `ChatAgentLauncher`. |
| F0.1 — erro estruturado de entitlement/access gate no chat IMOB | `ops/evidence/latest/f0-1-imob-entitlement-error-rendering-2026-07-09.md` | Evidência real da renderização de erro estruturado de entitlement/access gate no chat IMOB, preservando `message`, `reasonCode` e CTA real vindo do backend, mantendo fallback seguro sem CTA inventado, `ChatAgentLauncher` sem diff e status `parcial/evidenciado`. |
| F0.2 — estados do front door IMOB | `ops/evidence/latest/f0-2-imob-frontdoor-states-2026-07-09.md` | Evidência real da padronização local dos estados `loading`, `empty`, `error`, `entitlement` e fallback desconhecido no front door IMOB, reaproveitando F0.1 para access gate, sem CTA inventado, com teste focado verde e `ChatAgentLauncher` sem diff. |
| F0.3 — evidência visual/UX do front door IMOB | `ops/evidence/latest/f0-3-imob-frontdoor-ux-rendering-2026-07-09.md` | Evidência real por snapshot textual determinístico de que os estados F0.1/F0.2 são renderizáveis na UX do front door IMOB: entitlement com CTA real, ausência de CTA inventado, loading, empty, erro genérico seguro e fallback desconhecido fail-closed, com `ChatAgentLauncher` sem diff e status `parcial/evidenciado`. |
| F0.4 — checklist final de não-regressão do front door IMOB | `ops/evidence/latest/f0-4-imob-frontdoor-regression-checklist-2026-07-09.md` | Evidência real de não-regressão consolidada de F0.1/F0.2/F0.3: teste focado IMOB verde, `ChatAgentLauncher` sem diff, `chat.tsx` sem diff no PR F0.4, `chat.assistantDedupe.test.ts` sem diff no PR F0.4, checks documentais verdes e status `parcial/evidenciado`. |
| F0.5 — gate CI de regressão do front door IMOB | `ops/evidence/latest/f0-5-imob-frontdoor-ci-gate-2026-07-09.md` | Evidência real da criação do gate dedicado `check:imob-frontdoor-regression` e da integração do job `ImobFrontdoorRegression` no `CI Monorepo`, cobrindo o teste focado IMOB, `check:chat-launcher-render-only`, `check:evidence-index` e `check:docs-link-integrity`, com `ChatAgentLauncher` sem diff, `chat.tsx` sem diff e status `parcial/evidenciado`. |
| F0.6 — command center/report do front door IMOB | `ops/evidence/latest/f0-6-imob-frontdoor-command-center-report-2026-07-09.md` | Evidência real do report consolidado de F0.1-F0.5, mapeando estado atual, gates existentes, lacunas remanescentes P0-P4 e critérios objetivos para sair de `parcial/evidenciado`, sem declarar F0 global fechado. |
| F0.7 — orphan tests debt classification | `ops/evidence/latest/f0-7-orphan-tests-debt-classification-2026-07-09.md` | Evidência real da investigação/classificação da dívida `check:orphan-tests`, com inventário dos 50 órfãos preexistentes, baseline versionado na allowlist já suportada pelo repositório, política de evolução para bloquear apenas novos órfãos e confirmação de ausência de alteração em IMOB/front door/`ChatAgentLauncher`, com status `parcial/evidenciado`. |
| F0.8 — orphan tests CI gate | `ops/evidence/latest/f0-8-orphan-tests-ci-gate-2026-07-09.md` | Evidência real da promoção de `pnpm check:orphan-tests` para gate recorrente do `CI Monorepo`, removendo o step warn-only de `build_validate` e adicionando o job dedicado `OrphanTestsRegression`, sem alterar o mecanismo de orphan tests nem arquivos IMOB/front door, com status `parcial/evidenciado`. |
| F0.9 — CI gate aggregation report / status matrix | `ops/evidence/latest/f0-9-ci-gate-aggregation-status-matrix-2026-07-09.md` | Evidência real da consolidação dos gates/checks de integridade após F0.1–F0.8, mapeando jobs bloqueantes do `CI Monorepo`, ausência de warn-only no estado atual, checks fora do CI principal, dívida residual dos 50 orphan tests e critérios objetivos para avanço de maturidade, sem alterar CI, runtime ou front door IMOB, com status `parcial/evidenciado`. |
| F0.10 — node runtime consistency audit | `ops/evidence/latest/f0-10-node-runtime-consistency-audit-2026-07-09.md` | Evidência real da auditoria documental do runtime Node no CI após F0.9, classificando a coexistência de Node 20 e Node 22 como migração parcial com baseline declarativa ausente, mapeando impacto nos gates P0/P1 e propondo PR dedicado de padronização sem alterar workflows, runtime ou front door IMOB, com status `parcial/evidenciado`. |
| F0.11 — node runtime baseline declaration | `ops/evidence/latest/f0-11-node-runtime-baseline-declaration-2026-07-09.md` | Evidência real da declaração explícita de baseline Node 22 em `package.json`, `.nvmrc` e `.node-version`, fechando a lacuna documental apontada em F0.10 sem alterar workflows ou padronizar ainda os jobs residuais em Node 20, com status `parcial/evidenciado`. |
| F0.12 — build_validate Node 20 vs Node 22 comparison | `ops/evidence/latest/f0-12-build-validate-node20-vs-node22-comparison-2026-07-09.md` | Evidência real da comparação dos comandos extraídos de `build_validate` em Node 20 e Node 22, registrando outputs por etapa, diferenças observadas, warnings de baseline em Node 20, falhas operacionais locais em Node 22 e classificação conservadora `inconclusivo`, com recomendação de F0.13 em ambiente simétrico e sem alteração de workflows, package, lockfile, runtime ou front door IMOB. |
| F0.13 — symmetric build_validate Node comparison | `ops/evidence/latest/f0-13-symmetric-build-validate-node-comparison-2026-07-09.md` | Evidência real da repetição simétrica de `build_validate` em containers limpos Node 20 e Node 22, com mesmo commit, mesmo diretório, mesmas permissões e mesma instalação, separando a falha estrutural de `core_tests` por `DATABASE_URL` ausente da análise de runtime, classificando a rodada como `sem regressão detectada` e recomendando F0.14 para migrar o job a Node 22 sem alterar workflows, package, runtime ou front door IMOB nesta etapa. |
| F0.14 — build_validate Node 22 migration | `ops/evidence/latest/f0-14-build-validate-node22-migration-2026-07-09.md` | Evidência real da migração controlada do job `build_validate` para Node 22 em `.github/workflows/ci.yml`, implementada com override local de `NODE_VERSION` no próprio job, preservando o restante do workflow e mantendo separada a falha estrutural conhecida de `core_tests` por `DATABASE_URL` ausente, com status `parcial/evidenciado`. |
| F0.15 — core_tests DATABASE_URL requirement | `ops/evidence/latest/f0-15-core-tests-database-url-requirement-2026-07-09.md` | Evidência real da investigação da falha `DATABASE_URL` em `core_tests`, mostrando que o problema era a ausência de `TSX_TSCONFIG_PATH=packages/core/tsconfig.test.json` no `build_validate`, não uma exigência legítima de Postgres real. Classifica `highGlobalCoverage.e2e.test.ts` e a suíte correlata como dependentes do mock oficial `@repo/db` já previsto em `packages/core/tsconfig.test.json`, sem `DATABASE_URL` fake, sem skip silencioso, sem alteração de runtime Node e sem alteração de workflows residuais. |
| F0.16 — residual Node 20 workflows audit | `ops/evidence/latest/f0-16-residual-node20-workflows-audit-2026-07-10.md` | Evidência real da auditoria dos workflows residuais ainda em Node 20 (`lint.yml`, `release.yml`, `critical-dod.yml`), com matriz de risco, criticidade, dependências sensíveis e ordem recomendada de migração posterior, sem alterar workflows, package, lockfile, scripts, runtime ou front door IMOB, com status `parcial/evidenciado`. |
| F0.17 — lint.yml Node 22 migration | `ops/evidence/latest/f0-17-lint-node22-migration-2026-07-10.md` | Evidência real da migração controlada de `.github/workflows/lint.yml` para Node 22, limitada à troca de `env.NODE_VERSION` de `20` para `22`, preservando triggers, jobs, `PNPM_VERSION`, `RUN_DB_SMOKE` e todo o restante do workflow, sem alterar CI principal, release path, runtime amplo ou front door IMOB, com status `parcial/evidenciado`. |
| F0.18 — critical-dod.yml Node 22 migration | `ops/evidence/latest/f0-18-critical-dod-node22-migration-2026-07-10.md` | Evidência real da migração controlada de `.github/workflows/critical-dod.yml` para Node 22, limitada à troca de `node-version` de `20` para `22`, preservando todos os checks normativos do workflow e sem alterar CI principal, `lint.yml`, `release.yml`, package, runtime amplo ou front door IMOB, com status `parcial/evidenciado`. |
| F0.19 — release.yml Node 22 migration readiness audit | `ops/evidence/latest/f0-19-release-node22-readiness-audit-2026-07-10.md` | Evidência real da auditoria de readiness de `.github/workflows/release.yml`, mapeando triggers, jobs, comandos, secrets por nome, caminhos de publish NPM/GHCR/Docker, tags, permissões, estratégia segura para F0.20 e rollback, sem alterar workflows, package, lockfile, scripts, runtime ou front door IMOB, com status `parcial/evidenciado`. |
| F0.20 — release.yml dry-run/readiness workflow | `ops/evidence/latest/f0-20-release-node22-dry-run-readiness-2026-07-10.md` | Evidência real da criação de `.github/workflows/release-node22-readiness.yml` como ensaio seguro em Node 22 para os blocos equivalentes a `prepare`, `detect` e `validate_release`, sem alterar `release.yml`, sem publish real, sem Docker/GHCR push, sem `NPM_TOKEN`/`REGISTRY_PAT` e com estratégia explícita para F0.21, em status `parcial/evidenciado`. |
| F0.21 — release Node 22 readiness failure evidence | `ops/evidence/latest/f0-21-release-node22-readiness-evidence-2026-07-10.md` | Evidência real da execução do workflow `ReleaseNode22Readiness` em `main` com status `failure`, `prepare_readiness` e `detect_readiness` passando, `validate_release_readiness` falhando por mismatch entre `PNPM_VERSION='9'` no workflow e `packageManager: pnpm@10.12.4` no `package.json`, sem publish real, sem Docker/GHCR push, sem uso de `NPM_TOKEN`/`REGISTRY_PAT` e sem autorização para migrar o `release.yml`, com status `parcial/evidenciado`. |
| F0.22 — fix release Node 22 readiness pnpm setup | `ops/evidence/latest/f0-22-release-node22-readiness-pnpm-fix-2026-07-10.md` | Evidência real da correção mínima do workflow `.github/workflows/release-node22-readiness.yml`, alinhando `PNPM_VERSION` de `9` para `10.12.4` conforme `packageManager` versionado e demais workflows do repositório, sem alterar `release.yml`, sem publish real e sem autorizar migração do release path produtivo antes de nova observação verde do readiness, com status `parcial/evidenciado`. |
| F0.23 — release Node 22 readiness rerun failure evidence | `ops/evidence/latest/f0-23-release-node22-readiness-rerun-evidence-2026-07-10.md` | Evidência real do rerun do workflow `ReleaseNode22Readiness` após F0.22, mostrando que o mismatch de pnpm foi superado, mas o readiness continuou em `failure` ao avançar até `pnpm install --frozen-lockfile` e falhar no `postinstall`/build de `@eiah/core` por `TS2307: Cannot find module '@repo/db'`, sem publish real, sem Docker/GHCR push e sem autorização para migrar o `release.yml`, com status `parcial/evidenciado`. |
| F0.24 — fix release readiness install/build order | `ops/evidence/latest/f0-24-release-node22-readiness-install-build-order-fix-2026-07-10.md` | Evidência real da correção mínima do workflow `.github/workflows/release-node22-readiness.yml`, trocando `pnpm install --frozen-lockfile` por `pnpm install --frozen-lockfile --ignore-scripts` e explicitando o build ordenado de `@repo/db` antes de `@eiah/core`, para evitar que o `postinstall` construa `@eiah/core` antes de `@repo/db`, sem alterar `release.yml`, sem publish real e ainda sem observação verde do readiness, com status `parcial/evidenciado`. |
| F0.25 — record ReleaseNode22Readiness web masking failure evidence | `ops/evidence/latest/f0-25-release-node22-readiness-rerun-failure-after-f0-24-2026-07-10.md` | Evidência real do rerun do workflow `ReleaseNode22Readiness` após F0.24, registrando que a falha de ordem de install/build entre `@repo/db` e `@eiah/core` foi superada, que `packages/db`, `packages/core`, `packages/providers` e `packages/mcp-runner` avançaram no build, mas o novo run falhou no build de `apps/web` porque `packages/utils/src/index.ts` não conseguiu resolver `./masking`, sem alterar workflows, sem migrar `release.yml`, sem alterar `packages/utils` e com decisão explícita de que F0.26 deve corrigir a lacuna de masking em `packages/utils` antes de qualquer migração do release path produtivo, em status `parcial/evidenciado`. |
| F0.26 — fix packages/utils masking export for web build | `ops/evidence/latest/f0-26-packages-utils-masking-web-build-fix-2026-07-10.md` | Evidência real da correção mínima em `packages/utils/src`, substituindo o barrel que apontava para `src/masking.ts` ignorado pelo Git por um caminho versionável `src/masking/index.ts`, com build local de `@eiah/web` passando novamente e sem qualquer alteração em workflows, `release.yml`, `packages/utils/.gitignore`, IMOB/front door ou `ChatAgentLauncher`, ainda sem run real verde do readiness e com status `parcial/evidenciado`. |
| F0.27 — record ReleaseNode22Readiness apps/cli build failure evidence | `ops/evidence/latest/f0-27-release-node22-readiness-cli-build-failure-2026-07-10.md` | Evidência real do rerun do workflow `ReleaseNode22Readiness` após F0.26, registrando que a falha de masking em `packages/utils`/`apps/web` foi superada, mas o novo run falhou no build de `apps/cli` com `TS6059` por arquivos de `packages/db`, `packages/core` e `packages/contracts` entrarem no programa fora de `apps/cli/src`, além de erros de tipos `GuardrailLedger`/Prisma em que `tenantId_actionType_idempotencyKey`, `idempotencyKey` e `usageCount` não existem nos tipos gerados usados pelo CLI, sem alterar workflows, `release.yml`, `apps/cli` ou `packages/core`/`packages/db`, e com decisão explícita de que F0.28 deve investigar e corrigir o build de `apps/cli` antes de qualquer migração do release path produtivo, em status `parcial/evidenciado`. |
| F0.28 — fix apps/cli build rootDir and GuardrailLedger Prisma drift | `ops/evidence/latest/f0-28-apps-cli-build-rootdir-guardrailledger-fix-2026-07-10.md` | Evidência real da correção mínima em `apps/cli`, redirecionando o TypeScript do CLI para consumir declarações já construídas de `@repo/db` e `@eiah/core` em vez de `src` dos workspaces, trocando imports amplos de `@eiah/core` por subpaths mais estreitos e substituindo o acesso direto obsoleto a `prisma.guardrailLedger.upsert` pelo helper canônico `@eiah/core/services/guardrailLedgerStore`, com `pnpm --filter @eiah/cli build` passando localmente, sem alteração em workflows, `release.yml`, schema Prisma, IMOB/front door ou `ChatAgentLauncher`, ainda sem run real verde do readiness e com status `parcial/evidenciado`. |
| F0.29 — CLI contracts dist alias readiness fix | `ops/evidence/latest/f0-29-cli-contracts-dist-alias-readiness-fix-2026-07-10.md` | Evidência real da correção mínima residual no `apps/cli/tsconfig.json`, trocando apenas o alias de `@eiah/contracts` das declarações em `src` para as declarações compiladas do pacote contracts, mantendo os aliases já corrigidos de `@repo/db` e `@eiah/core`, com `pnpm --filter @eiah/cli build` passando localmente e sem qualquer alteração em workflows, `release.yml`, `apps/cli/src/index.ts`, `packages/contracts/src`, `packages/contracts/package.json`, `packages/contracts/tsconfig.json`, IMOB/front door ou `ChatAgentLauncher`, ainda sem run real verde do readiness e com status `parcial/evidenciado`. |
| F0.30 — ReleaseNode22Readiness apps/api IMOB build fix | `ops/evidence/latest/f0-30-release-node22-readiness-api-imob-build-fix-2026-07-10.md` | Evidência real de que o run pós-F0.29 avançou além de `apps/cli` e `apps/web`, mas parou em `apps/api build`; registra como causa a cadeia `.filter` aplicada sobre `Promise` em `apps/api/src/routes/imob.ts` e o drift `null` vs `undefined` em `apps/api/src/services/imob/imobArtifactCapabilities.ts`; prova a correção mínima nesses dois arquivos, `pnpm --filter @eiah/api build` passando localmente e ausência de alterações em release/workflows/package/lockfile/apps/cli/ChatAgentLauncher, com status `parcial/evidenciado`. |
| F0.31 — ReleaseNode22Readiness @repo/db Prisma schema path fix | `ops/evidence/latest/f0-31-release-node22-readiness-db-prisma-schema-path-fix-2026-07-11.md` | Evidência real de que o log `/home/runner/work/EIAH/EIAH/packages/db` é compatível com o `cwd` do pacote `@repo/db`, que não há hardcode relevante de runner path no repositório, e que o bloqueio real vinha do uso de `packages/db/prisma/schema.prisma` dentro de um comando `pnpm --filter @repo/db prisma ...`; registra a correção mínima em `.github/workflows/release-node22-readiness.yml` para `./prisma/schema.prisma`, sem alterar `release.yml`, demais workflows, `package.json`, `packages/db`, `apps/api`, `apps/cli`, `apps/web`, `packages/core`, `packages/contracts` ou `ChatAgentLauncher`, com status `parcial/evidenciado`. |
| F0.32 — ReleaseNode22Readiness Prisma schema formatting drift fix | `ops/evidence/latest/f0-32-release-node22-readiness-prisma-format-drift-fix-2026-07-11.md` | Evidência real de que, após F0.31 corrigir o path do schema, o bloqueio remanescente do workflow passou a ser drift de formatação em `packages/db/prisma/schema.prisma`; prova o drift via `prisma_schema_build_bg.wasm`, reaplica o output canônico do formatter ao schema, confirma idempotência local com `NO_DIFF` e preserva o escopo sem alterar workflows, runtime, `package.json`, `pnpm-lock.yaml`, `apps/api`, `apps/cli`, `apps/web`, `packages/core`, `packages/contracts` ou `ChatAgentLauncher`, com status `parcial/evidenciado`. |
| F0.32 follow-up — P1 critical chain whitespace-tolerant schema check | `ops/evidence/latest/f0-32-p1-critical-chain-prisma-format-whitespace-fix-2026-07-11.md` | Evidência real do follow-up documental da correção do gate `check:p1-critical-chain` após o `prisma format` de F0.32: registra que o check deixou de depender do alinhamento exato de espaços para `approvedBy`/`approvedAt`, preservou a validação semântica do schema, voltou a passar com `ok:true` e não exigiu nova alteração em `packages/db/prisma/schema.prisma`, workflows, `package.json`, `pnpm-lock.yaml`, apps ou `ChatAgentLauncher`. |
| F0.33 — ReleaseNode22Readiness Prisma migrate diff flags fix | `ops/evidence/latest/f0-33-release-node22-readiness-prisma-migrate-diff-flags-fix-2026-07-11.md` | Evidência real de que, após F0.32, o bloqueio remanescente do workflow passou a ser o uso das flags removidas `--from-schema-datamodel` / `--to-schema-datamodel` no `prisma migrate diff`; registra a correção mínima em `.github/workflows/release-node22-readiness.yml` para `--from-schema` / `--to-schema`, confirma no Prisma 7.2.0 instalado a mensagem de remoção das flags antigas e preserva o escopo sem alterar `release.yml`, demais workflows, `package.json`, `pnpm-lock.yaml`, `packages/db`, `apps/api`, `apps/cli`, `apps/web`, `packages/core`, `packages/contracts` ou `ChatAgentLauncher`, com status `parcial/evidenciado`. |
| F0.34 — ReleaseNode22Readiness green after F0.33 | `ops/evidence/latest/f0-34-release-node22-readiness-green-after-f0-33-2026-07-11.md` | Evidência real do primeiro run verde do `ReleaseNode22Readiness` em `main` após F0.29–F0.33, com `workflow_dispatch`, commit `164df7e`, status `Success`, duração `1m 7s` e jobs verdes `prepare_readiness`, `detect_readiness` e `validate_release_readiness`; registra warnings residuais de Node.js 20 em actions externas, mantém `release.yml` intocado e conserva o status `parcial/evidenciado`. |
| F0.35 — release.yml Node 22 migration readiness audit | `ops/evidence/latest/f0-35-release-yml-node22-migration-readiness-audit-2026-07-11.md` | Evidência real da auditoria read-only do workflow produtivo `.github/workflows/release.yml` após o readiness verde de F0.34: compara `release.yml` com `release-node22-readiness.yml`, confirma divergência ainda aberta em `NODE_VERSION`, `PNPM_VERSION`, strategy de install, ordem explícita de build e comandos Prisma, e registra que publish CLI, GHCR, Docker push, tags e secrets permanecem fora do escopo do readiness e exigem PR/decisão separados antes de qualquer migração do release path produtivo, com status `parcial/evidenciado`. |
| F0.36 — release.yml controlled migration decision | `ops/evidence/latest/f0-36-release-yml-controlled-migration-decision-2026-07-11.md` | Evidência real da decisão controlada pós-auditoria F0.35: readiness verde continua insuficiente para migrar diretamente `.github/workflows/release.yml`; a aproximação futura deve ser fatiada em Camada A (validação/build: `NODE_VERSION`, `PNPM_VERSION`, install strategy, ordem de build e comandos Prisma) e Camada B (publish/secrets/GHCR/tags), com proibição explícita de publish real, Docker push, uso de secrets ou alteração de triggers antes de PR separado e evidência indexável adicional, mantendo o status `parcial/evidenciado`. |
| F0.37 — release Node 22 validation/build dry-run bridge | `ops/evidence/latest/f0-37-release-node22-validation-build-dry-run-2026-07-11.md` | Evidência real da implementação da Camada A definida em F0.36 como workflow manual separado `.github/workflows/release-node22-validation-build-dry-run.yml`, alinhando baseline Node/pnpm, install strategy, ordem explícita de build e comandos Prisma ao caminho validado do readiness, sem tocar em `release.yml`, sem publish, sem GHCR/Docker push, sem secrets e sem triggers por tag, mantendo o status `parcial/evidenciado` até que haja run real próprio desse bridge. |
| F0.38 — ReleaseNode22ValidationBuildDryRun green | `ops/evidence/latest/f0-38-release-node22-validation-build-dry-run-green-2026-07-11.md` | Evidência real do primeiro run verde do workflow manual `ReleaseNode22ValidationBuildDryRun`, em `main`, commit `8d6b2cc`, status `Success`, duração `1m 16s` e jobs verdes `prepare_dry_run`, `detect_dry_run` e `validate_release_validation_build_dry_run`; registra warnings residuais de Node.js 20 em actions externas, confirma zero publish/secrets/tags e mantém `release.yml` intocado, com status `parcial/evidenciado`. |
| F0.39 — release publish layer risk model | `ops/evidence/latest/f0-39-release-publish-layer-risk-model-2026-07-11.md` | Evidência real do modelo de risco e da decisão documental da Camada B do release path: identifica as superfícies produtivas reais de `release.yml` (`pnpm publish`, `NPM_TOKEN`, `REGISTRY_PAT`, `docker/login-action`, `docker/build-push-action`, push para GHCR, triggers por tag e rollback), classifica os riscos em irreversibilidade externa, dependência de secrets, promoção por trigger e rollback operacional, e mantém a Camada B explicitamente não autorizada até PR separado com gates e evidência próprios, em status `parcial/evidenciado`. |
| F0.40 — release publish preflight without side effects | `ops/evidence/latest/f0-40-release-publish-preflight-no-side-effects-2026-07-11.md` | Evidência real da implementação do workflow manual `.github/workflows/release-publish-preflight.yml` como preflight sem side effects da Camada B: valida metadados/versionamento, superfícies de artefato (`apps/cli/package.json`, `apps/api/Dockerfile.prod`, `apps/workers/action-runner/Dockerfile.prod`) e permissões mínimas candidatas ainda presentes em `release.yml`, renderiza apenas comandos candidatos de publish e refs de imagem, se autoaudita contra `pnpm publish`, `npm publish`, `docker/login-action`, `docker/build-push-action`, `gh release`, `git tag` e triggers por tag, mantém `release.yml` intocado e preserva a Camada B em status `parcial/evidenciado`. |
| F0.41 — ReleasePublishPreflight green | `ops/evidence/latest/f0-41-release-publish-preflight-green-2026-07-11.md` | Evidência real do primeiro run verde do workflow manual `ReleasePublishPreflight`, em `main`, commit `6174fc9`, status `Success`, duração `28s` e jobs verdes `prepare_preflight`, `detect_preflight` e `preflight_release_publish_layer`; registra warnings residuais de Node.js 20 em actions externas, confirma zero publish/secrets/tags/GHCR e mantém `release.yml` intocado, com status `parcial/evidenciado`. |
| F0.42 — release publish gate decision after preflight green | `ops/evidence/latest/f0-42-release-publish-gate-decision-after-preflight-green-2026-07-11.md` | Evidência real da decisão documental pós-F0.41: o run verde do `ReleasePublishPreflight` reduz risco estrutural da Camada B, mas não autoriza publish real; o próximo gate mínimo decidido passa a ser um pacote auditável de ativação/rollback sem side effects, com segregação por superfície (CLI/API/worker), owners, janela operacional, boundary explícita de `NPM_TOKEN`/`REGISTRY_PAT` e critérios de autorização antes de qualquer discussão sobre publish, GHCR, tags/releases ou aproximação direta de `release.yml`, mantendo status `parcial/evidenciado`. |
| F0.43 — release activation/rollback gate without side effects | `ops/evidence/latest/f0-43-release-activation-rollback-gate-no-side-effects-2026-07-11.md` | Evidência real da implementação do workflow manual `.github/workflows/release-activation-rollback-gate.yml` como gate reforçado de ativação/rollback da Camada B sem side effects: valida decisão humana, razão, `target_version`, superfície (`cli`/`api`/`workers`/`multi_surface`), owners, janela operacional, `rollback_reference`, políticas declaradas de retry/idempotência e confirmação explícita de no-side-effects; confirma que `release.yml` ainda exige boundary controlado para `NODE_AUTH_TOKEN`, `REGISTRY_PAT`, `docker/login-action`, `docker/build-push-action` e triggers por tag; renderiza apenas plano candidato por superfície e mantém `release.yml` intocado, com status `parcial/evidenciado`. |
| F0.44 — ReleaseActivationRollbackGate green | `ops/evidence/latest/f0-44-release-activation-rollback-gate-green-2026-07-11.md` | Evidência real do primeiro run verde do workflow manual `ReleaseActivationRollbackGate`, em `main`, commit `d880d83`, status `Success`, duração `27s` e jobs verdes `validate_activation_gate_inputs`, `detect_release_surfaces` e `release_activation_rollback_gate`; registra warnings residuais de Node.js 20 em actions externas, confirma zero publish/secrets/tags/GHCR e mantém `release.yml` intocado, com status `parcial/evidenciado`. |
| F0.45 — plan unification conversation layer consolidation | `ops/evidence/latest/f0-45-plan-unification-conversation-layer-consolidation-2026-07-11.md` | Evidência real da materialização documental do artefato `plano_unificacao_EIAH_interativo_atualizado_pos_F0_44_conversacao.html` na raiz do repositório, validando parse HTML, a preservação explícita da seção `id="conversacao"` como `especificada/parcial`, a incorporação de F0.35–F0.44, a permanência de `release.yml` produtivo intocado e a ausência de alterações em runtime, `ChatAgentLauncher`, engine, apps, packages e scripts, com status `parcial/evidenciado`. |
| F0.46 — roadmap release gate chain consolidation | `ops/evidence/latest/f0-46-roadmap-release-gate-chain-consolidation-2026-07-13.md` | Evidência real da consolidação documental da cadeia F0.34–F0.45 no roadmap canônico `ROADMAP_UNIFICADO_v8_ATUALIZADO_2026-06-15.md`, registrando a sequência readiness verde -> auditoria de `release.yml` -> separação Camada A/Camada B -> validation-build dry-run -> publish preflight -> activation/rollback gate -> plano consolidado com Conversação, mantendo `release.yml` produtivo intocado, a camada Conversação como `especificada/parcial` e F0/P0 sem declaração de fechamento, com status `parcial/evidenciado`. |
| F0.47 — layer B readiness decision audit | `ops/evidence/latest/f0-47-layer-b-readiness-decision-audit-2026-07-13.md` | Evidência real da auditoria documental de readiness da Camada B após a cadeia F0.34–F0.46: confirma que readiness, dry-run, preflight, gate reforçado, roadmap e plano consolidado já reduziram risco estrutural, mas mantém `release.yml` produtivo intocado e conclui conservadoramente que a Camada B ainda não está pronta para decisão executiva de publish real, por ainda faltar evidência de uso controlado de `secrets`, login em registry/GHCR, publish real, push de imagem, tags/releases e rollback executado, com status `parcial/evidenciado`. |
| F0.48 — layer B negative-path audit | `ops/evidence/latest/f0-48-layer-b-negative-path-audit-2026-07-13.md` | Evidência real da auditoria documental de negative paths da Camada B após F0.47: mapeia falhas esperadas antes de qualquer publish real (`version invalid`, surface ausente, owners/janela/rollback ausentes, boundary de `secrets` não aprovado, login em registry/GHCR não autorizado, publish/push sem idempotência, `latest` sem política, retry sem reaprovação e `tags/releases` sem gate próprio), propõe reasonCodes fail-closed e define a evidência mínima futura ainda ausente, mantendo `release.yml` produtivo intocado e a Camada B explicitamente não autorizada, com status `parcial/evidenciado`. |
| F0.49 — layer B future gate readiness checklist | `ops/evidence/latest/f0-49-layer-b-future-gate-readiness-checklist-2026-07-13.md` | Evidência real da consolidação documental dos critérios mínimos para um gate futuro da Camada B após F0.47/F0.48: organiza checklist auditável para superfície, versão, owners, janela, rollback, boundary de `secrets`, registry/GHCR, `latest`, `tags/releases`, retry/idempotência e evidência mínima por superfície; mantém `release.yml` produtivo intocado e a Camada B explicitamente não autorizada para publish real, com status `parcial/evidenciado`. |
| F0.50 — layer B controlled negative dry-run plan | `ops/evidence/latest/f0-50-layer-b-controlled-negative-dry-run-plan-2026-07-13.md` | Evidência real da consolidação documental de F0.47/F0.48/F0.49 em um plano verificável de future controlled negative dry-run da Camada B: define matriz de cenários negativos, reasonCodes esperados, critérios fail-closed, exigências mínimas de HITL/owners/rollback e evidência futura por ciclo, mantendo `release.yml` produtivo intocado e a Camada B explicitamente não autorizada para execução produtiva, com status `parcial/evidenciado`. |
| F0.51 — layer B no-side-effect acceptance criteria | `ops/evidence/latest/f0-51-layer-b-no-side-effect-acceptance-criteria-2026-07-13.md` | Evidência real da consolidação documental de F0.47–F0.50 em critérios objetivos de aceitação para classificar uma futura validação controlada da Camada B como `no-side-effect`: exige zero publish, zero registry login, zero GHCR/Docker push, zero `tags/releases`, zero consumo operacional de `secrets`, abort precoce com `reasonCode` e prova auditável do boundary não cruzado, mantendo `release.yml` intocado e a Camada B explicitamente não autorizada para execução produtiva, com status `parcial/evidenciado`. |
| F0.52 — layer B receipt/bundle evidence contract | `ops/evidence/latest/f0-52-layer-b-receipt-bundle-evidence-contract-2026-07-13.md` | Evidência real da consolidação documental de F0.47–F0.51 em um contrato mínimo de receipt/bundle para futura validação controlada da Camada B: define campos obrigatórios de identificação, superfície, owners, rollback, `reasonCode`, abort stage, invariantes de `no-side-effect` e prova de zero boundary externo cruzado, mantendo `release.yml` intocado e a Camada B explicitamente não autorizada para execução produtiva, com status `parcial/evidenciado`. |
| F0.53 — layer B rollback reference acceptance criteria | `ops/evidence/latest/f0-53-layer-b-rollback-reference-acceptance-criteria-2026-07-13.md` | Evidência real da consolidação documental de F0.47–F0.52 em critérios objetivos para aceitar ou rejeitar um futuro `rollbackReference` da Camada B: exige referência existente, surface-scoped, auditável, verificável antes de qualquer boundary externo e consistente com `reasonCode`, `receipt` e `bundle`, mantendo `release.yml` intocado e a Camada B explicitamente não autorizada para execução produtiva, com status `parcial/evidenciado`. |
| F0.54 — layer B HITL approval evidence criteria | `ops/evidence/latest/f0-54-layer-b-hitl-approval-evidence-criteria-2026-07-13.md` | Evidência real da consolidação documental de F0.47–F0.53 em critérios objetivos para aceitar ou rejeitar evidência futura de aprovação humana HITL da Camada B: exige aprovador identificável, surface-scope, owners, `operationalWindow`, vínculo com `rollbackReference`, `reasonCode`, `receipt` e `bundle`, mantendo `release.yml` intocado e a Camada B explicitamente não autorizada para execução produtiva, com status `parcial/evidenciado`. |
| F0.55 — layer B promotion preconditions decision matrix | `ops/evidence/latest/f0-55-layer-b-promotion-preconditions-decision-matrix-2026-07-13.md` | Evidência real da consolidação documental de F0.47–F0.54 em uma matriz única de pré-condições mínimas para qualquer futura promoção controlada da Camada B: reúne readiness, negative paths, checklist de gate futuro, dry-run negativo planejado, critérios de `no-side-effect`, contrato de `receipt`/`bundle`, `rollbackReference`, HITL, owners, `operationalWindow`, `reasonCode`, fail-closed e boundary de `secrets`, mantendo `release.yml` intocado e a Camada B explicitamente não autorizada para execução produtiva, com status `parcial/evidenciado`. |
| F0.56 — layer B controlled validation readiness closure | `ops/evidence/latest/f0-56-layer-b-controlled-validation-readiness-closure-2026-07-13.md` | Evidência real da decisão explícita de closure documental da Camada B após F0.47–F0.55: classifica a camada simultaneamente como `CANDIDATE_CONTROLLED_VALIDATION=true`, `PARTIAL=true`, `PRODUCTION_NOT_AUTHORIZED=true` e `BLOCKED=true` para publish real, rollback real, HITL real, uso de secrets produtivos e side effects externos, mantendo `release.yml` intocado e fixando que a camada está apenas preparada documentalmente para futura validação controlada dedicada, com status `parcial/evidenciado`. |
| F0.57 — layer B controlled validation PR entry criteria | `ops/evidence/latest/f0-57-layer-b-controlled-validation-pr-entry-criteria-2026-07-13.md` | Evidência real da consolidação documental dos critérios objetivos de entrada para abrir um futuro PR dedicado de validação controlada da Camada B: exige escopo estrito sem side effects, cadeia F0.47–F0.56 íntegra, objetivo único, surface scoping, invariantes de `no-side-effect`, owners e `operationalWindow` documentais, fail-closed com `reasonCode`, compatibilidade com `receipt`/`bundle`, `rollbackReference` e HITL sem execução real, mantendo `release.yml` intocado e a Camada B explicitamente não autorizada para execução produtiva, com status `parcial/evidenciado`. |
| F0.58 — layer B controlled validation proposal template | `ops/evidence/latest/f0-58-layer-b-controlled-validation-proposal-template-2026-07-13.md` | Evidência real da consolidação documental de F0.47–F0.57 em um template mínimo para um futuro PR dedicado de validação controlada da Camada B: define abertura obrigatória, escopo permitido/proibido, surface, objetivo técnico único, invariantes `no-side-effect`, owners e `operationalWindow` documentais, negative paths, `reasonCode`, compatibilidade com `receipt`/`bundle`, `rollbackReference` e HITL sem execução real, mantendo `release.yml` intocado e a Camada B explicitamente não autorizada para execução produtiva, com status `parcial/evidenciado`. |
| F0.59 — F0 to F1 transition readiness decision | `ops/evidence/latest/f0-59-f0-to-f1-transition-readiness-decision-2026-07-13.md` | Evidência real da decisão formal de readiness para transição de F0 para F1: consolida F0.47–F0.58 como cadeia documental de preparação da Camada B, confirma o roadmap canônico vigente e libera apenas o início de F1 como fase controlada de front door mobile/responsividade/render-only, sem autorizar runtime, publish real, rollback real, HITL real, secrets produtivos, registry login, Docker/GHCR push, tags/releases ou side effects externos, mantendo `ChatAgentLauncher` sem lógica nova e `release.yml` intocado, com status `parcial/evidenciado`. |
| F1.0 — front door mobile responsiveness baseline audit | `ops/evidence/latest/f1-00-front-door-mobile-responsiveness-baseline-audit-2026-07-13.md` | Evidência real da auditoria inicial de baseline da responsividade mobile do front door no recorte autorizado por F0.59: confirma F1 apenas como fase controlada de front door mobile/responsividade/render-only, identifica baseline responsivo já existente em `VerticalWorkbenchShell` com grid desktop e toggle mobile do painel contextual, registra o rótulo neutro `Contexto IMOB`, confirma `chat.tsx` como composição render-only sem lógica nova no `ChatAgentLauncher` e mantém runtime, apps/packages/scripts e `release.yml` intocados, com status `parcial/evidenciado`. |
| F1.1 — front door mobile snapshots / render-only validation | `ops/evidence/latest/f1-01-front-door-mobile-snapshots-render-only-2026-07-14.md` | Evidência real da validação documental dos snapshots/render-only do front door IMOB após F1.0: reaproveita a convenção existente de smoke Playwright/evidências `phase-9-*`, revalida no código atual o shell responsivo, o toggle mobile do painel contextual e o badge neutro `Contexto IMOB`, confirma leitura conservadora de `pass` para `390px`, `768px` e `1024px`, mantém `360px` como `blocked` por falta de prova direta atualizada e preserva `ChatAgentLauncher`, runtime, apps/packages/scripts e `release.yml` intocados, com status `parcial/evidenciado`. |
| F1.2 — front door 360px direct snapshot / render-only gap closure | `ops/evidence/latest/f1-02-front-door-360px-direct-snapshot-render-only-2026-07-14.md` | Evidência real da captura direta e atualizada do front door IMOB em `360×740`, reaproveitando o ambiente docker local e o padrão Playwright já existente sem criar script novo: prova ausência de overflow horizontal (`360/360`), composer visível (`textarea.top=650`, `bottom=690`), `VerticalSelectorBar` sem quebra crítica, toggle mobile `Resumo do intake` funcional também no estado aberto, badge neutro `Contexto IMOB`, ausência de copy `PILOTO CONTROLADO` e zero alteração em `ChatAgentLauncher`, runtime, apps, packages, scripts, workflows, `package.json` ou `pnpm-lock.yaml`, substituindo o `blocked` de F1.1 por status `evidenciado` específico para `360px`. |
| F1.3 — front door mobile recurring visual gate proposal | `ops/evidence/latest/f1-03-front-door-mobile-recurring-visual-gate-proposal-2026-07-14.md` | Evidência documental da avaliação de valor para transformar F1.0-F1.2 em um gate recorrente de anti-regressão mobile no front door IMOB: inventaria `smoke-9-*`, Playwright local, evidências `phase-9-*`, teste render-only atual e ausência de gate mobile dedicado no CI, conclui que há valor real em uma F1.4 pequena com JSON determinístico por viewport (`360x740`, `390x844`, `768x1024`, `1024x768`) e asserts de overflow/composer/toggle/badge neutro `Contexto IMOB`/ausência de `PILOTO CONTROLADO`, mas mantém esta etapa estritamente documental, sem alterar apps, scripts, workflows, runtime ou `ChatAgentLauncher`, com status `proposta`. |
| F1.4 — front door mobile recurring smoke minimal implementation | `ops/evidence/latest/f1-04-front-door-mobile-recurring-smoke-minimal-implementation-2026-07-14.md` | Evidência real da implementação mínima de um smoke recorrente local para o front door mobile IMOB: cria `scripts/smoke-f1-4-front-door-mobile.mjs` seguindo o padrão Playwright/browser em `.mjs`, sem alterar apps, packages, workflows, `package.json` ou `pnpm-lock.yaml`, valida `360x740`, `390x844`, `768x1024` e `1024x768` com JSON determinístico de overflow/composer/selector/toggle/badge neutro `Contexto IMOB`/ausência de `PILOTO CONTROLADO`, usa `F1_FRONT_DOOR_BASE_URL` com default `http://127.0.0.1:5173`, não gera screenshots e mantém a promoção ao CI explicitamente fora deste PR, com status `implementado localmente/evidenciado`. |
| F1.5 — front door mobile smoke reproducibility / CI-readiness decision | `ops/evidence/latest/f1-05-front-door-mobile-smoke-reproducibility-ci-readiness-2026-07-14.md` | Evidência real da auditoria de reprodutibilidade do smoke F1.4: registra 3 execuções locais bem-sucedidas e sem flakiness para `360x740`, `390x844`, `768x1024` e `1024x768`, confirma que o script é reproduzível localmente, mas ainda depende de fallback controlado para `~/.npm/_npx` porque `playwright` não está formalizado em `package.json` nem em `pnpm-lock.yaml`, mantendo CI, workflows, package scripts e runtime intocados e concluindo `manual/local por enquanto` com recomendação de F1.6 separada para normalização antes de qualquer promoção ao CI, com status `parcial/evidenciado`. |
| F1.6a — pnpm store alignment preflight | `ops/evidence/latest/f1-06a-pnpm-store-alignment-preflight-2026-07-14.md` | Evidência real do preflight de `pnpm store` antes de retomar a F1.6: confirma que o `node_modules` local foi materializado com `storeDir=/app/.pnpm-store/v10`, enquanto o `pnpm` atual e o CI convergem para `~/.pnpm-store`; prova que `/app` não é gravável no ambiente atual, que não há `.npmrc`/`.pnpmrc` local impondo esse store e que houve risco adicional de `EAI_AGAIN` no registry, concluindo que a retomada segura da F1.6 exige relink controlado do workspace para `~/.pnpm-store` antes de formalizar `playwright`, sem alterar workflows, apps, packages, scripts, runtime ou `ChatAgentLauncher`, com status `parcial/evidenciado`. |
| F1.6b — controlled pnpm store relink / Playwright runner normalization | `ops/evidence/latest/f1-06b-controlled-pnpm-store-relink-playwright-runner-normalization-2026-07-14.md` | Evidência real da tentativa controlada de relink do workspace após F1.6a: confirma a branch dedicada, fixa `pnpm` em `~/.pnpm-store`, mas registra falha de `pnpm install --ignore-scripts` por `EACCES` ao remover `packages/core/node_modules/@eiah`, causada por ownership legada `nobody:nogroup` em diretórios `node_modules` internos de `packages/`; por isso `playwright` não foi formalizado, `package.json`/`pnpm-lock.yaml`/smoke F1.4 permaneceram intocados e a etapa termina `parcial/bloqueado`, sem alterar workflows, apps, packages, runtime ou `ChatAgentLauncher`. |
| F1.6c — workspace node_modules ownership recovery | `ops/evidence/latest/f1-06c-workspace-node-modules-ownership-recovery-2026-07-14.md` | Evidência real da tentativa conservadora de recuperar o workspace após a F1.6b: confirma que os `node_modules` internos legados de core, contracts, providers e mcp-runner seguem em `nobody:nogroup`, que `packages/db/node_modules` permaneceu correto e intocado, que o `chown` mínimo exigido para destravar o relink não foi aplicado, mas que o root voltou a resolver `tsx` e `pnpm check:docs-link-integrity` passou; registra ainda que o relink completo segue bloqueado por ownership legada mais instabilidade de registry (`EAI_AGAIN` em `pnpm install --ignore-scripts`), sem Playwright, sem alteração em `package.json`/`pnpm-lock.yaml`/scripts/workflows/apps/packages versionados/runtime ou `ChatAgentLauncher`, com status `parcial/bloqueado`. |
| F1.6d — authorized ownership repair and relink stabilization | `ops/evidence/latest/f1-06d-authorized-ownership-repair-relink-stabilization-2026-07-14.md` | Evidência real da tentativa autorizada de aplicar a correção operacional pós-F1.6c: reconfirma o escopo restrito de ownership legado em `packages/core`, `packages/contracts`, `packages/providers` e `packages/mcp-runner`, preserva `packages/db/node_modules` e o restante do repositório intocados, mas registra que o `sudo chown -R jusall:jusall ...` ficou bloqueado por senha interativa do host e que a tentativa equivalente sem `sudo` não recebeu aprovação operacional; por isso o relink não pôde ser executado, `node_modules/.modules.yaml` permaneceu ausente e a etapa termina `parcial/bloqueado`, sem Playwright, sem alteração em `package.json`/`pnpm-lock.yaml`/scripts/workflows/apps/packages versionados/runtime ou `ChatAgentLauncher`. |
| F1.6e — relink stabilization after host ownership repair | `ops/evidence/latest/f1-06e-relink-stabilization-after-host-ownership-repair-2026-07-14.md` | Evidência da estabilização operacional pós-F1.6d: após correção manual de ownership herdado em diretórios gerados de dependência, `pnpm install --ignore-scripts` concluiu com sucesso, `node_modules/.modules.yaml` foi restaurado, `docs-link-integrity` permaneceu OK, não houve diff em `package.json`/`pnpm-lock.yaml`/scripts/workflows/apps/packages e a etapa não formaliza Playwright, não altera smoke, não promove CI e não declara F1.6 concluída. |
| F1.6f — Playwright runner normalization after relink stabilization | `ops/evidence/latest/f1-06f-playwright-runner-normalization-after-relink-stabilization-2026-07-14.md` | Evidência da normalização do runner Playwright após estabilização F1.6e: formaliza Playwright como devDependency root, valida resolução formal via `import('playwright')`, ajusta o smoke F1.4 para remover ou não usar fallback frágil em `~/.npm/_npx`, registra resultado do smoke local e preserva isolamento sem workflows, sem apps/packages versionados, sem runtime/engine/API/contracts, sem `ChatAgentLauncher` e sem promoção CI. |
| F1.6g — front door mobile Chromium sandbox validation | `ops/evidence/latest/f1-06g-front-door-mobile-chromium-sandbox-validation-2026-07-14.md` | Evidência da validação manual/local do bloqueio remanescente do smoke mobile após F1.6f: reproduz o crash de launch do Chromium, testa pequenas variações seguras de flags sem resolver o problema, classifica o resultado como `ENV_SANDBOX_BLOCKED` e torna a saída JSON do smoke mais explícita com `classification`, `nodeVersion` e `playwrightVersion`, preservando `fallbackUsed=false`, `import('playwright')` formal e isolamento sem workflows, apps/packages versionados, runtime/engine/API/contracts, `ChatAgentLauncher` ou promoção CI. |
| F1.6h — controlled browser-smoke environment | `ops/evidence/latest/f1-06h-controlled-browser-smoke-environment-2026-07-14.md` | Evidência da tentativa controlada de reexecutar o mesmo smoke mobile F1.4/F1.6g em ambiente Docker local já existente (`eiah-web`): confirma runner formal via `playwright`, `fallbackUsed=false`, registra `nodeVersion`, `pnpmVersion`, `playwrightVersion`, comando e exit code, e mostra que o bloqueio muda de `ENV_SANDBOX_BLOCKED` no host para `CHROMIUM_BINARY_MISSING` no container por ausência do binário Chromium no cache do Playwright, mantendo fail-closed, sem instalar browser, sem alterar apps/packages/runtime/engine/workflows, sem `ChatAgentLauncher` e sem promoção CI. |
| F1.6i — Chromium provisioning strategy | `ops/evidence/latest/f1-06i-chromium-provisioning-strategy-2026-07-14.md` | Evidência documental da estratégia canônica proposta para provisionar Chromium no smoke mobile controlado: consolida F1.6g como `ENV_SANDBOX_BLOCKED`, F1.6h como `CHROMIUM_BINARY_MISSING`, identifica que `eiah-web` usa `node:20-bookworm` sem browsers Playwright provisionados e recomenda runner dedicado em microfase futura F1.6j, sem executar install, sem alterar Dockerfile/compose, sem CI, sem apps/packages/runtime/engine, sem `ChatAgentLauncher` e sem side effects externos. |
| F1.6j — controlled Chromium provisioning execution | `ops/evidence/latest/f1-06j-controlled-chromium-provisioning-execution-2026-07-14.md` | Evidência da execução controlada do provisioning de Chromium em runner Docker dedicado e efêmero, separado do `eiah-web`: usa imagem local `node:22-bookworm`, repositório montado read-only, cache isolado para browsers, rede reaproveitada via `container:eiah-web`, valida a rota `/app/imob/chat` com `200`, provisiona Chromium/FFmpeg/Headless Shell com Playwright e executa o mesmo smoke F1.4 com `runnerImport=formal_dependency:playwright` e `fallbackUsed=false`; o resultado permanece fail-closed por `UNKNOWN_CHROMIUM_LAUNCH_FAILURE`, com causa real registrada em stderr (`libnspr4.so` ausente na imagem base), sem alterar apps/packages/runtime/engine/workflows, sem contaminar `eiah-web` e sem promoção CI. |
| F1.6k — dedicated Playwright runtime base image selection | `ops/evidence/latest/f1-06k-dedicated-playwright-runtime-base-image-selection-2026-07-14.md` | Evidência documental da decisão de base runtime para o runner dedicado do smoke mobile F1 após F1.6j: compara imagem oficial Playwright, `node:22-bookworm` com libs nativas declaradas, Dockerfile dedicado futuro e manutenção do bloqueio; usa o inventário Docker local para confirmar ausência de imagem Playwright pronta e consolida a leitura técnica de que `node:22-bookworm` puro é insuficiente por depender de libs do SO (`libnspr4.so`). A recomendação canônica passa a ser uma imagem oficial Playwright compatível, ainda não puxada nem validada, mantendo `eiah-web` fora do papel de runner, sem `docker pull/build`, sem apps/packages/runtime/engine/workflows e com status `proposta/parcial`. |
| F1.6l — controlled official Playwright runner execution | `ops/evidence/latest/f1-06l-controlled-official-playwright-runner-execution-2026-07-14.md` | Evidência da execução real do smoke mobile F1 em runner efêmero com a imagem oficial `mcr.microsoft.com/playwright:v1.61.1-noble`: registra `docker pull` explicitamente aprovado e usado uma única vez, repo montado read-only, rede `container:eiah-web`, rota `/app/imob/chat` respondendo `200` e o smoke F1.4 passando com `classification=PASS`, `runnerImport=formal_dependency:playwright`, `fallbackUsed=false` e quatro viewports verdes (`360x740`, `390x844`, `768x1024`, `1024x768`), sem alterar apps/packages/runtime/engine/workflows, sem contaminar `eiah-web` e sem promoção CI. |
| F1.6m — manual smoke closure and CI promotion boundary | `ops/evidence/latest/f1-06m-manual-smoke-closure-ci-promotion-boundary-2026-07-14.md` | Evidência documental de fechamento manual/controlado da cadeia F1.6g–F1.6l: consolida o PASS da F1.6l com imagem oficial Playwright, declara explicitamente que CI ainda não foi promovido, preserva workflows/release.yml intactos e define critérios mínimos para uma futura F1.7 de promoção CI, sem rodar Docker, sem alterar apps/packages/runtime/engine, sem `ChatAgentLauncher` e sem side effects externos. |
| F1.7 — CI promotion decision and gate design for mobile smoke | `ops/evidence/latest/f1-07-ci-promotion-decision-gate-design-2026-07-14.md` | Evidência documental da decisão/design de promoção futura do smoke mobile F1 para CI: parte do PASS manual/controlado da F1.6l e do boundary F1.6m, define gate inicialmente informativo vs obrigatório, tag Playwright validada, política de pull/cache, artefatos/logs, fail-closed, impacto no CI e critérios mínimos para implementação futura, sem alterar workflows/release.yml, sem rodar Docker, sem alterar apps/packages/runtime/engine, sem `ChatAgentLauncher` e sem promover CI nesta etapa. |
| F1.7a — CI informative mobile smoke gate proposal | `ops/evidence/latest/f1-07a-ci-informative-mobile-smoke-gate-proposal-2026-07-14.md` | Evidência documental da proposta de implementação futura do gate CI informativo para o smoke mobile F1: parte do PASS manual/controlado da F1.6l, do boundary F1.6m e do design F1.7, inspeciona workflows em modo read-only, propõe job isolado em `ci.yml` com imagem Playwright validada, comando de `vite preview`, healthcheck da rota `/app/imob/chat`, artefatos/logs, política de pull/cache, fail-closed futuro e critérios de promoção para obrigatório, sem alterar workflows/release.yml, sem rodar Docker, sem alterar apps/packages/runtime/engine, sem `ChatAgentLauncher` e sem promover CI nesta etapa. |
| F1.7b — CI informative mobile smoke gate implementation | `ops/evidence/latest/f1-07b-ci-informative-mobile-smoke-gate-implementation-2026-07-14.md` | Evidência da implementação inicial do gate CI informativo para o smoke mobile F1: adiciona job isolado em `.github/workflows/ci.yml` baseado na proposta F1.7a, usando imagem Playwright validada, front door servido em CI por `build + vite preview`, healthcheck explícito da rota `/app/imob/chat`, smoke F1 executado com logs/artefatos e política não obrigatória inicialmente, sem alterar `release.yml`, apps/packages/runtime/engine, sem `ChatAgentLauncher`, sem Docker build/push, sem secrets e sem promover o gate a obrigatório nesta etapa. |
| F1.7c — first CI informative mobile smoke run and artifact capture | `ops/evidence/latest/f1-07c-first-ci-informative-mobile-smoke-run-2026-07-14.md` | Evidência da primeira tentativa controlada de run informativo em GitHub Actions do gate mobile smoke F1 implementado na F1.7b: registra branch/commit, push remoto bem-sucedido, ausência de run URL/ID e de artifacts para o SHA `c71be53` por incompatibilidade entre o trigger atual do `CI Monorepo` (`push` apenas em `main`/`release/**` e `pull_request`) e o push em branch `test/*`, concluindo `run ausente`, mantendo o gate informativo e propondo F1.7d para corrigir a estratégia de disparo, sem alterar workflows/release.yml, sem alterar apps/packages/runtime/engine, sem `ChatAgentLauncher`, sem release/tag/publish e sem promover para required check. |
| F1.7d — first real CI informative mobile smoke run trigger strategy | `ops/evidence/latest/f1-07d-first-real-ci-informative-mobile-smoke-run-2026-07-15.md` | Evidência do primeiro run real do gate CI informativo mobile smoke após o merge do PR `#274`: confirma que a estratégia mínima e auditável foi usar o fluxo já existente de PR para `main`, registra o run `CI Monorepo` `29367988231` em `main` com job `ImobFrontdoorMobileSmokeInformative` concluído com `success`, artifact `imob-frontdoor-mobile-smoke-informative` presente e `smoke-report.json` com `classification=PASS`, `routeStatus=200`, `runnerImport=formal_dependency:playwright`, `fallbackUsed=false`, imagem `mcr.microsoft.com/playwright:v1.61.1-noble` e quatro viewports verdes, sem alterar workflows/release.yml, apps/packages/runtime/engine ou `ChatAgentLauncher`, mantendo o gate como informativo. |
| F1.7f — mobile smoke informative recurrence and promotion policy | `ops/evidence/latest/f1-07f-mobile-smoke-informative-recurrence-promotion-policy-2026-07-15.md` | Evidência documental da política governada de recorrência do gate informativo mobile smoke após a F1.7d: separa explicitamente o primeiro run real já provado do que ainda permanece como proposta de maturidade futura, reserva `F1.7e` apenas para falha real futura, define critérios para manter o gate informativo, critérios mínimos para eventual promoção futura a bloqueante, métricas obrigatórias, pacote mínimo de evidência por run, critérios de rollback e riscos residuais, sem alterar workflows/release.yml, apps/packages/scripts/runtime/engine ou `ChatAgentLauncher`, e sem promover o gate nesta etapa. |
| F2.0 — WhatsApp adapter read-only / binding / fail-closed design | `ops/evidence/latest/f2-00-whatsapp-adapter-read-only-binding-fail-closed-design-2026-07-15.md` | Evidência documental do design inicial do adapter WhatsApp em modo estritamente read-only: fixa o adapter como `channel-adapter/render-only`, nunca como runtime paralelo, define binding obrigatório `numero -> identidade -> tenant -> workspace -> scope -> entitlement`, TTL de sessão, reasonCodes fail-closed, validação de assinatura, masking de PII, pacote mínimo de evidência por evento e bloqueio explícito de mutações/side effects (`lead.create`, `lead.discard`, ações críticas), sem alterar workflows, apps, packages, scripts, runtime, engine ou `ChatAgentLauncher`, e sem declarar WhatsApp operacional nesta etapa. |
| F2.1 — WhatsApp adapter technical contract / envelope / signature validation plan | `ops/evidence/latest/f2-01-whatsapp-adapter-technical-contract-envelope-signature-plan-2026-07-15.md` | Evidência documental do contrato técnico inicial do adapter WhatsApp: define envelope versionado `whatsapp.adapter.event.v1`, headers obrigatórios, canonical string conceitual para assinatura, replay protection por timestamp e `eventId`, idempotência por `eventId`, validação fail-closed do payload, reasonCodes técnicos adicionais, masking de PII, pacote mínimo de evidência por evento e lista de testes negativos futuros, sem implementar webhook real, sem alterar workflows, apps, packages, scripts, runtime, engine ou `ChatAgentLauncher`, e sem declarar WhatsApp operacional nesta etapa. |
| Implementação contracts-first da Public Product Taxonomy | `ops/evidence/latest/public-product-taxonomy-implementation-2026-06-25.md` | Prova execução real da frente de nomenclatura pública com policy narrativa, contrato tipado, seed oficial, consumo pelo vocabulário do EIAH, check dedicado de regressão, teste web focado, build do `@eiah/core` e build da API, sem alteração do `ChatAgentLauncher`. |
| Follow-up de cobertura anti-drift da Public Product Taxonomy | `ops/evidence/latest/public-product-taxonomy-follow-up-2026-06-25.md` | Prova a promoção real de `pnpm check:public-product-taxonomy` ao CI monorepo ao lado de `pnpm check:evidence-index`, registra que `CODEX.md` foi lido antes da alteração e delimita as próximas superfícies de copy/documentação a cobrir sem alterar `ChatAgentLauncher`, runtime de agentes, entitlements ou behavior de billing/economy. |
| Governança de capabilities de artefatos IMOB | `ops/evidence/latest/imob-artifact-capabilities-governance-2026-07-02.md` | Prova a criação das capabilities canônicas `openChat/viewCaseDossier/viewCaseReceipt/viewRunBundle`, a separação explícita entre bundle de execução e dossiê de caso, o fail-closed da UI IMOB e a limitação residual dos testes HTTP da API por bloqueio pré-existente de build/sandbox fora do escopo do PR-D1. |
| Matriz de Saídas do EIAH v1 | `ops/evidence/latest/eiah-outputs-matrix-v1-validation-2026-07-08.md` | Prova a consolidação documental da matriz `docs/architecture/EIAH_OUTPUTS_MATRIX_v1.md`, separa outputs conversacionais/operacionais/auditáveis por Core, IMOB, J_360, MKT e Billing/Economy, confirma `Closing Output` IMOB como ausente/parcial, mantém `Command Center` como dashboard e registra os gaps N-07/N-08 sem alterar runtime ou `ChatAgentLauncher`. |

## IMOB Data Hardening (2026-06-12)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Hardening de open handles nos contract tests HTTP IMOB/Interop | `ops/evidence/latest/imob-data-route-contract-hardening-2026-06-12.md` | Execução real com `EXIT:0` dos contract tests `shadow-executions`, `runs.imob-action` e `agents.interop`, além do hardening aplicado em Redis/BullMQ/Prisma e teardown de testes HTTP. |
| Mutation manual de ownerResponsible no CRM IMOB | `ops/evidence/latest/imob-owner-assignment-mutation-2026-06-13.md` | Execução real do teste focado da Trilha A, cobrindo `CASE_RESPONSIBLE_REQUIRED`, `assignOwnerToCase()` / `assignResponsibleActor()`, evento `owner_assigned` atômico e idempotência por `evidenceRef`. |

## IMOB Surface Data Reliability — 10/10 evidenciados (2026-06-15)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Evidencia de fechamento do alerta A1 | `docs/ops/evidence/latest/imob-surface-data-reliability/a1-ci-evidence.md` | Rótulos de contexto de janela adicionados ao CC (chip `Bloqueios recentes: X · 7d`, card `Bloqueios (7d)`), custo total (`${valor} (${kpiWindowDays}d)`) e Hero (`bloqueados atuais`); A1-follow-up concluido: `apiGetImobFunnelHealth` agora recebe `window: "7d"` explicitamente no frontend, eliminando risco de drift com default do backend; 9/9 testes pass. |
| Evidencia de fechamento do alerta A8 | `docs/ops/evidence/latest/imob-surface-data-reliability/a8-ci-evidence.md` | KPI strip de `properties.tsx` migrado de `syntheticProperties` para `apiListImobProperties()` com badge de fonte; 5/5 testes A8/A9 pass e 17/17 testes de nao-regressao da suite imob pass. Criterios: sem `syntheticProperties`, sem ID sintetico hardcoded, badge presente no KPI strip. |
| Evidencia de fechamento do alerta A4 | `docs/ops/evidence/latest/imob-surface-data-reliability/a4-ci-evidence.md` | Defense in depth para isolamento de workspace em `GET /imob/cases` e `GET /imob/cases/costs`; 6/6 testes pass contra banco de dados real (ausencia / match / mismatch para ambas as rotas). `workspaceId` da query e apenas checagem de consistencia; `authContext.workspaceId` e a unica fonte de verdade. Mismatch retorna 403 `WORKSPACE_SCOPE_MISMATCH` fail-closed. |
| Evidencia de fechamento do alerta A2 | `docs/ops/evidence/latest/imob-surface-data-reliability/a2-ci-evidence.md` | `syntheticThreads` removido de `dashboard.tsx`; `selectedThreadId` inicia `null` (nao URL param); `requestedThreadId` honrado somente quando confirmado na resposta real de `apiListImobChatThreads`; API vazia ou erro resulta em `threads = []` sem fallback sintetico; 9/9 testes pass. Criterios: sem `syntheticThreads`, sem `"synthetic"` no `threadSource`, `requestedThreadId` nao ativa selecao sem confirmacao real. |
| Evidencia de fechamento do alerta A3 | `docs/ops/evidence/latest/imob-surface-data-reliability/a3-ci-evidence.md` | Janela de custo por caso agora explícita na tabela do CC: prop `caseCostWindowDays?: number` (default 30) adicionado a `ImobCommandCenter`; `costLabel` passa a exibir `R$ X.XX (30d)` em cada linha; `dashboard.tsx` passa `caseCostWindowDays={30}` explicitamente. `apiListImobCaseCosts` mantido com `windowDays: 30` fixo — nenhum endpoint, query ou janela alterados; 8/8 testes pass. |
| Evidencia de fechamento do alerta A5 | `docs/ops/evidence/latest/imob-surface-data-reliability/a5-ci-evidence.md` | `syntheticPartners` removido de `partners.tsx`; estado inicial `partners = []`; API vazia → `source = "empty"`, erro → `source = "error"` (sem fallback sintetico em ambos); `delegateeId` removido da cadeia de fallback de `partnerName` — fallback passa a ser `"Parceiro sem nome cadastrado"`; badge de fonte distingue `"delegações marketplace"` / `"sem delegações"` / `"indisponível"`; `apiListDelegations` e `mapDelegationsToPartners` inalterados; 14/14 testes pass. |
| Evidencia de fechamento do alerta A6 | `docs/ops/evidence/latest/imob-surface-data-reliability/a6-ci-evidence.md` | `buildCasePriority` e `buildCaseFallbackActions` removidas de `dashboard.tsx` (page component); `buildImobCasePriority` exportada de `imobCommandCenterHelper.ts`; `buildImobCaseFallbackActions` adicionada e exportada do mesmo helper; `dashboard.tsx` importa ambas — sem lógica de negócio em page component; 10/10 testes pass. |
| Evidencia de fechamento do alerta A7 | `docs/ops/evidence/latest/imob-surface-data-reliability/a7-ci-evidence.md` | `contextCase` useMemo em `dashboard.tsx` refatorado para retornar `{ contextCase, contextCaseSource }` com tipo `"requested" \| "thread" \| "run" \| "heuristic" \| null`; card "Caso" na aba Soluções exibe `"estimado por contexto da thread"` quando `contextCaseSource === "heuristic"`; lógica de resolução e deeplinks inalterados; 11/11 testes pass. |
| Evidencia de fechamento do alerta A9 | `docs/ops/evidence/latest/imob-surface-data-reliability/a9-ci-evidence.md` | `syntheticProperties` removido de `properties.tsx`; página chama `apiListImobProperties()` real na montagem; badge de fonte no KPI strip (`"backend"` / `"sem dados"`); empty state semântico; IDs sintéticos `prop-001..prop-005` ausentes; 5/5 testes pass. |
| Evidencia de fechamento do alerta A10 | `docs/ops/evidence/latest/imob-surface-data-reliability/a10-ci-evidence.md` | KPI `"Casos em parceria"` → `"Políticas delegadas"` em `partners.tsx`; campo `activeCases` → `delegationPoliciesCount`; card `"processos ativos"` → `"políticas ativas"`; semântica alinhada a delegações marketplace, não a casos CRM; 14/14 testes pass (4 de A10 + 10 de A5). |

## IMOB PRs #144–#147 — Consolidação Operacional (2026-06-17)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Histórico operacional consolidado IMOB — PRs #144 a #147 | `ops/evidence/latest/imob-pr-144-147-operational-history.md` | Consolidação documental das quatro frentes IMOB mergeadas entre 2026-06-15 e 2026-06-17: (1) PR #144 — Command Center com superfícies reais + 10 alertas Surface Data Reliability fechados (81/81 testes); (2) PR #145 — Funil/Team consolidado com KPIs de broker sem duplicação; (3) PR #146 — gate de runtime `ownerResponsible` em `commission.settle`; (4) PR #147 — cadeia CC→Chat→run→worker→mutação→canonical→CC refresh provada em banco real (Fases 1–4.2, 9/9 E2E) + observability H1–H5 (69/69 testes, CI gate ativo). Invariante central: `ImobCase.status` decidido exclusivamente no backend; React sem regra de status. |

## IMOB Chat Workbench — Fase 7.0.1 Validation (2026-06-18)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Validação local-docker do IMOB Conversation Workbench UI Shell + follow-up de typecheck | `docs/ops/evidence/latest/imob-chat-workbench-phase7-0-1-validation-2026-06-18.md` | Prova que o frontend real local-docker responde `200` em `/app/imob/chat`, que o módulo `src/pages/app/imob/chat.tsx` é servido/transpilado pelo Vite no runtime real, que os testes focados do shell/context panel/intake cards/API client passaram (`5/5`), que PII não aparece na renderização, e que o bloqueio específico de `vite/client` foi removido via `vite-env.d.ts`, restando apenas erros estruturais mais amplos e pré-existentes no typecheck do pacote web. |

## IMOB Chat Workbench — Fase 7.0.2 Typecheck Baseline (2026-06-18)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Baseline/debt register do typecheck global do pacote web | `docs/ops/evidence/latest/imob-chat-workbench-typecheck-baseline-2026-06-18.md` | Prova a execução real de `pnpm --filter @eiah/web exec tsc -p tsconfig.json --noEmit`, classifica o baseline em dependências/tipos ausentes (`37`), imports de assets (`20`) e drift legado (`33`), confirma que os arquivos novos `ImobWorkbench*` e `ImobIntakeSummaryPanel` não aparecem no log, e registra que a única regressão plausível do patch foi removida com correção mínima em `src/pages/app/imob/chat.tsx`. |

## IMOB Chat Workbench — Fase 7.1 CTA Links Validation (2026-06-18)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Evidência indexável dos CTAs render-only do Workbench IMOB | `docs/ops/evidence/latest/imob-chat-workbench-phase7-1-cta-links-2026-06-18.md` | Prova runtime real local-docker com `200` em `/app/imob/chat` e no módulo transformado `src/pages/app/imob/chat.tsx`, documenta as condições seguras de renderização de `Ver no Command Center`, `Ver no Funil` e `Abrir RunArchive`, confirma os fallbacks `Dossiê indisponível neste piloto` e `Nenhum destino de navegação disponível no payload atual`, preservação dos botões de export e ausência de PII/destino inventado via testes focados `5/5`. |

## IMOB Chat Workbench — Fase 7.2 Safe Deeplinks Validation (2026-06-18)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Evidência indexável dos deeplinks seguros e do enriquecimento render-only do painel | `docs/ops/evidence/latest/imob-chat-workbench-phase7-2-safe-deeplinks-2026-06-18.md` | Prova runtime real local-docker com `200` em `/app/imob/chat` e no módulo transformado `src/pages/app/imob/chat.tsx`, mapeia as rotas frontend existentes de Command Center (`/app/imob/dashboard`), Funil (`/app/imob/dashboard?tab=funil`) e RunArchive (`/app/runs?domain=imob&runId=...`) com seus parâmetros suportados, confirma labels distintos para superfície geral vs contexto específico, referências mascaradas no painel, fallback seguro para Dossiê indisponível e testes focados `5/5` sem PII nem destino inventado. |

## IMOB Chat Workbench — Fase 7.2.1 Visual/Runtime Validation (2026-06-18)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Evidência indexável da validação visual/runtime dos fluxos de navegação do Workbench | `docs/ops/evidence/latest/imob-chat-workbench-phase7-2-1-visual-validation-2026-06-18.md` | Prova runtime real local-docker com `200` em `/app/imob/chat` e no módulo transformado `src/pages/app/imob/chat.tsx`, registra explicitamente a limitação de ausência de browser/headless browser no host, valida visualmente por renderização sanitizada os cenários de contexto específico, visão geral e fallback, confirma labels distintos de `Command Center`, presença condicional de `Funil` e `RunArchive`, preservação de `Dossiê indisponível neste piloto` e testes focados `5/5` sem PII nem destino inventado. |

## IMOB Chat Workbench — Fase 7.4.1 Visual Parity Validation (2026-06-18)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Evidência indexável da paridade visual render-only do Workbench IMOB | `docs/ops/evidence/latest/imob-chat-workbench-phase7-4-visual-parity-2026-06-18.md` | Prova runtime real local-docker com `200` em `/app/imob/chat` e no módulo transformado `src/pages/app/imob/chat.tsx`, registra a limitação de ausência de browser/headless browser no host, valida por renderização sanitizada a preservação de sidebar IMOB, header central, chat central, quick actions próximas ao input, painel direito, CTAs seguros e fallback de Dossiê, além de confirmar que a aproximação visual ao mock permaneceu payload-driven, sem PII nem hardcode de dados reais fictícios. |

## IMOB Chat Workbench — Fase 7.5 Visual Acceptance (2026-06-18)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Nota de aceite visual final do Workbench IMOB em piloto controlado | `docs/ops/evidence/latest/imob-chat-workbench-phase7-5-visual-acceptance-2026-06-18.md` | Prova por renderização sanitizada nova que o shell visual do Workbench IMOB ficou suficientemente próximo do mock de referência após ajustes cosméticos render-only em shell, sidebar, header central, quick actions e painel direito, preservando CTAs e condições existentes, sem inventar rotas/parâmetros, sem hardcode de dados do mock e sem expor PII. |

## IMOB Chat Workbench — Fase 9.0 Multi-Vertical Visual Foundation (2026-06-18)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Consolidação da fundação visual multi-vertical do Workbench aplicada ao IMOB | `docs/ops/evidence/latest/imob-chat-workbench-phase9-0-multivertical-visual-foundation-2026-06-18.md` | Prova por renderização sanitizada e testes focados que o shell do Chat IMOB passou a usar uma base visual reutilizável para futuras verticais, com sidebar escura, área central clara, painel direito claro, cards brancos, quick actions próximas ao input e contexto lateral render-only, preservando CTAs seguros, fallbacks existentes, ausência de PII e sem hardcode dos dados do mock. |

## IMOB Chat Workbench — Fase 9.1 Light SaaS Visual Parity (2026-06-19) — EVIDENCIADO

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Refinamento visual Light SaaS do Workbench IMOB sobre a fundação da Fase 9.0 | `docs/ops/evidence/latest/imob-chat-workbench-phase9-1-light-saas-visual-parity-2026-06-18.md` | Prova por testes focados (50/50), grep anti-hardcode limpo e `check:evidence-index ok` que os refinamentos visuais da Fase 9.1 foram aplicados sem alterar arquitetura, contratos, backend, worker, storage ou lógica de negócio. Mudanças: composer limpo com quick actions compactas (chip row sem card container), cards com inset highlight, sombras refinadas no shell externo e no container principal, header do painel contextual mais denso, botões de export/navegação com hover/shadow polidos, sidebar com stat cards e search mais refinados, header central mais compacto. Colunas `xl:grid-cols-[280px,minmax(0,1fr),360px]` e todos os textos testados preservados integralmente. Status: PILOTO CONTROLADO. |

## IMOB Chat Workbench — Fase 9.1.1 Browser Visual Smoke (2026-06-19)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Validação em browser headless (Playwright Chromium 149) das alterações visuais da Fase 9.1 no ambiente local-docker real | `docs/ops/evidence/latest/imob-chat-workbench-phase9-1-1-browser-smoke-2026-06-19.md` | Prova por Playwright Chromium headless com mock de sessão via `page.route()` que o runtime Vite em local-docker entrega todos os tokens CSS da Fase 9.1: sidebar `linear-gradient(#09111d, #101c2b)`, 2 asides, grid xl 3 colunas, compositor `rounded-[20px]`, textarea `bg=rgba(0,0,0,0)` e `border-radius=14px`, botão Enviar `rounded-[14px] bg-slate-900`, WorkbenchPanelCard `inset_0_1px_0_rgba(255,255,255,0.85)`, header "Document Intake / IMOB v2.1" visível, chip row sem card "Quick actions". Screenshots capturados: desktop 1440×900, fullpage, compositor, mobile 390×844, tablet 1024×768. 50/50 testes, grep limpo, evidence-index ok. Fase 9.1 reclassificada como EVIDENCIADO. |

## IMOB Chat Workbench — Fase 9.3 SaaS Visual Parity Final (2026-06-19)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Correção da proporção visual de `/app/imob/chat` para paridade com mock SaaS claro | `docs/ops/evidence/latest/imob-chat-workbench-phase9-3-saas-visual-parity-final-2026-06-19.md` | Prova por Playwright Chromium (18/18 checks) que o Workbench IMOB usa a largura real do viewport: **1438px** em 1440px (era ~1120px com `max-w-6xl`), **1278px** em 1280px; chat central de ~480px para ~800px (+67%); header compacto de ~130px para ~40px. Três mudanças pontuais: (1) App.tsx detecta `/app/imob/chat` e remove `max-w-6xl`/`py-10` do `<main>`; (2) `VerticalWorkbenchShell` converteu header para bar compacto inline + grid `flex-1 h-full`; (3) `ImobWorkbenchShell` passa `description=""` e `helperText=""`. Grid columns `xl:grid-cols-[280px,minmax(0,1fr),360px]` intocados; 50/50 testes pass; backend, workers, storage, auth/entitlement intocados. Status: PILOTO CONTROLADO. |

## IMOB Chat Workbench — Fase 9.3.1 Responsive UX & Density Hardening (2026-06-19) — EVIDENCIADO

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Hardening responsivo do Workbench IMOB em tablet/mobile após Fase 9.3 | `docs/ops/evidence/latest/phase-9-3-1-responsive-hardening/phase-9-3-1-responsive-hardening.md` | Prova por Playwright Chromium (30/30 checks, 5 resoluções) que a sidebar escura deixou de esmagar o chat em viewports abaixo de `lg` (1024px): tablet 768×1024 com textarea saindo de Y=1520px (fora do viewport) para **Y=803px** (visível); mobile 390×844 de Y=1346px para **Y=754px** (visível). Alteração mínima: `hidden lg:flex` no aside da sidebar em `VerticalWorkbenchShell.tsx` + `min-h-[600px]` → `min-h-[520px]`. Desktop (1440, 1280, 1024) preservado: sidebar 280px lateral, centro 742–798px, context toggle acessível. 50/50 testes unitários pass. Grid `xl:grid-cols-[280px,minmax(0,1fr),360px]` intocado; backend, workers, storage, auth/entitlement intocados. Status: PILOTO CONTROLADO. |

## IMOB Chat Workbench — Fase 9.3.2 IMOB Product Shell Alignment (2026-06-19) — EVIDENCIADO

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Alinhamento visual de `/app/imob/chat` ao mock SaaS final | `docs/ops/evidence/latest/phase-9-3-2-product-shell-alignment/phase-9-3-2-product-shell-alignment.md` | Prova por Playwright Chromium com runtime real em 4 resoluções-alvo (`1440×900`, `1280×800`, `768×1024`, `390×844`) e 26/26 checks OK de que `/app/imob/chat` opera em shell imersivo: header global corporativo oculto só nessa rota, header de produto com `IMOB Product Shell`, `Document Intake / IMOB v2.1` e retorno seguro ao Command Center, área central com `Workspace atual / Chat ativo`, painel contextual render-only preservado, input/quick actions visíveis inclusive em mobile e ausência de hardcode do mock nos arquivos do shell. Backend, workers, storage, auth/entitlement e `ChatAgentLauncher` permanecem intocados. Status: PILOTO CONTROLADO. |
| Alinhamento SaaS da sidebar esquerda do IMOB Workbench | `docs/ops/evidence/latest/phase-9-3-3-sidebar-saas-alignment/phase-9-3-3-sidebar-saas-alignment.md` | Prova por Playwright Chromium com runtime real em 4 resoluções (`1440×900`, `1280×800`, `768×1024`, `390×844`) e 22/22 checks OK de que a sidebar esquerda foi modernizada sem tocar em lógica: novo bloco `IMOB Workspace` com `Conversas do intake`, ações `Nova conversa`/`Ver operações` mais leves, `Busca rápida`, lista `Conversas recentes`, estado vazio menos denso e badges de seleção mais discretas. Handlers `handleNewConversation`, `setShowThreadPanel`, `setConversationSearch`, `loadConversation` e `ThreadPanel` foram preservados; Product Shell 9.3.2, responsividade 9.3.1 e auth/entitlement 9.2 seguem intactos. Status: PILOTO CONTROLADO. |
| Convergência visual SaaS final preservando a base multi-vertical | `docs/ops/evidence/latest/phase-9-4-multivertical-saas-product-convergence/phase-9-4-multivertical-saas-product-convergence.md` | Prova por Playwright Chromium com runtime real em 4 resoluções e 21/21 checks OK de que `/app/imob/chat` atingiu acabamento SaaS mais próximo do mock final sem deixar de ser multi-vertical: `VerticalWorkbenchShell` passou a receber `heroHighlights` e `panelToggleLabel` por props, `WorkbenchPanelCard` ganhou flexibilidade via `bodyClassName`, `ImobWorkbenchShell` concentrou a copy/config da vertical, e o painel direito/summary do intake ficou mais claro e respirado. Product Shell 9.3.2, Sidebar 9.3.3, responsividade 9.3.1 e auth/entitlement 9.2 foram preservados. Status: PILOTO CONTROLADO. |
| Compactação mobile do Product Shell do IMOB | `docs/ops/evidence/latest/phase-9-4-1-mobile-product-shell-density/phase-9-4-1-mobile-product-shell-density.md` | Prova por Playwright Chromium com runtime real em 5 resoluções (`1440×900`, `1280×800`, `768×1024`, `390×844`, `375×667`) e 19/19 checks OK de que o hero do `VerticalWorkbenchShell` foi compactado abaixo de `sm`: padding, gaps e radius reduzidos, descrição longa oculta em mobile, título/status preservados e chat/composer aparecendo mais cedo (`textareaY=550` em `375×667`). Desktop da 9.4, fundação multi-vertical, auth/entitlement 9.2 e `ChatAgentLauncher` permanecem intactos. Status: PILOTO CONTROLADO. |
| Controle da chat lane desktop/ultra-wide do IMOB Workbench | `docs/ops/evidence/latest/phase-9-4-2-desktop-composition-chat-lane-width/phase-9-4-2-desktop-composition-chat-lane-width.md` | Prova por Playwright Chromium com runtime real em 6 resoluções (`1440×900`, `1600×900`, `1920×1080`, `2048×1152`, `390×844`, `375×667`) e 38/38 checks OK de que a Fase 9.4.2 corrigiu a dispersão horizontal da conversa sem comprimir o shell inteiro: `chat.tsx` passou a centralizar apenas a largura útil do header, gate, draft banner, timeline e composer com `xl:max-w-[82%]`, mantendo painel direito visível, shell imersivo e densidade mobile da 9.4.1. Status: PILOTO CONTROLADO. |
| Contenção de scroll do histórico da sidebar esquerda do IMOB Workbench | `docs/ops/evidence/latest/phase-9-4-3-sidebar-history-scroll-containment/phase-9-4-3-sidebar-history-scroll-containment.md` | Prova por Playwright Chromium com runtime real em 6 resoluções e 32/32 checks OK de que a Fase 9.4.3 eliminou o crescimento vertical causado pelo histórico longo: a rota `/app/imob/chat` passou a rodar em frame imersivo `h-screen`, `main` IMOB foi fechado com `min-h-0 overflow-hidden`, o `aside` esquerdo e a sidebar ganharam containment explícito, e a lista `Conversas recentes` passou a rolar internamente (`listScroller=181/1766` em `1440×900`, `433/1766` em `2048×1152`) sem aumentar a altura do shell. Chat lane 9.4.2, Product Shell 9.4, mobile 9.4.1 e sidebar visual 9.3.3 preservados. Status: PILOTO CONTROLADO. |

## IMOB Chat Workbench — Fase 9.5 Vertical Selector with LEGAL Specialist Context (2026-06-20) — PILOTO CONTROLADO

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Seletor de verticais IMOB/LEGAL com painel direito reativo | `docs/ops/evidence/latest/phase-9-5-vertical-selector-legal-specialist/phase-9-5-vertical-selector-legal-specialist.md` | Prova por 65/65 testes unitários (48 existentes + 17 novos SSR) e smoke Playwright 44/44 checks OK em 3 viewports (1440×900, 1280×800, 390×844) que a Fase 9.5 adicionou `VerticalSelectorBar` (pills IMOB/LEGAL), `LegalContextPanel` (safe empty state), `ReactiveContextPanel` (wrapper reativo) e `VerticalChatTypes` em `features/workbench/vertical-chat/` sem alterar `ImobWorkbenchContextPanel`, `ImobWorkbenchShell`, `VerticalWorkbenchShell`, `ChatAgentLauncher`, `chatLauncherEngine`, backend, auth ou infra. Pill IMOB: active (#5DCAA5), `aria-selected=true`, borda sólida. Pill LEGAL: preview (border-dashed, "Em breve", `aria-disabled=true`) — entitlement LEGAL ausente no `LauncherAccessContext`. J-360 ausente no tablist (assert negativo confirmado em 3 viewports); aparece apenas como texto no `LegalContextPanel` quando `specialistAvailable=true`. Grep anti-hardcode limpo. `check:evidence-index ok`. Status: PILOTO CONTROLADO — EVIDENCIADO. |

## IMOB Chat Workbench — Fase 9.2 Auth/Entitlement Real Smoke (2026-06-19)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Validação de `/app/imob/chat` com sessão, token, tenant, workspace e entitlement IMOB reais no local-docker — sem mock de `/session/context` via `page.route()` | `docs/ops/evidence/latest/imob-chat-workbench-phase9-2-auth-entitlement-smoke-2026-06-19.md` | Prova que o fluxo real de auth/entitlement funciona: seed SQL idempotente criou `tenant-smoke-9-2` com token `seed_53670...` (= `VITE_API_TOKEN`) e instalação IMOB ativa; `/api/session/context` retornou `IMOB_INSTALLED: true` e `landingPath=/app/imob/chat`; browser Playwright acessou `/app/imob/chat` com sessão real (sem page.route()): sidebar escura, header "Document Intake / IMOB v2.1", chip row, textarea transparente confirmados. Caso negativo: token sem IMOB retorna `IMOB_INSTALLED: false`; endpoint `/api/imob/knowledge/sync-status` retorna **403 + `IMOB_ENTITLEMENT_MISSING`** (fail-closed); browser com token negativo aterra em `/app/runs` (não em `/app/imob/chat`). 50/50 testes, grep limpo. Status: PILOTO CONTROLADO. |

## Roadmap v8 — Verificacao contra codigo atual (2026-06-15)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Fonte canônica do roadmap v8 | `ROADMAP_UNIFICADO_v8_ATUALIZADO_2026-06-15.md` | Define o escopo normativo vigente com data de referência `2026-07-01`; este documento não substitui evidência de execução. |
| Checklist de execucao IMOB Data | `docs/ops/imob-data-pr-execution-checklist.md` | Consolida a trilha real de fechamento de `request.action`, duracao operacional, `ownerResponsible` e diagnosticos tenant-scoped apos o marco v8. |
| Trilha B runtime minimo multi-vertical | `docs/ops/imob-data-trilha-b-runtime-minimo-execution-checklist.md` | Registra o delta minimo executado para validacao de `responsible actor contract`, `reasonCode` de falha contratual e evidencias operacionais sem abrir migracao nova. |
| Arquivamento de runs IMOB | `docs/ops/imob-run-archive-pr-execution-checklist.md` + `apps/api/src/services/runArchiveService.ts` + `apps/api/src/workers/runArchiveWorker.ts` + `packages/db/prisma/schema.prisma` | Prova que a plataforma avancou alem do backlog normativo do v8 com persistencia/worker de `RunArchive` e modelo Prisma dedicado. |
| Funil/Team consolidado no dashboard IMOB | `docs/ops/imob-funnel-team-pr-execution-checklist.md` + `apps/web/src/features/imob/funnel/ImobFunnelTeamSection.tsx` + `apps/web/src/pages/app/imob/dashboard.tsx` | Prova consolidacao operacional da experiencia IMOB no frontend, removendo duplicacao de aba Equipe e aproximando a superficie real do comando/funil. |

## Fechamento arquitetural — Deduplicação Frontend (2026-04-15)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Módulos canônicos de helper/front | `apps/web/src/lib/formatters.ts` + `apps/web/src/lib/imobContext.ts` + `apps/web/src/lib/economyDerived.ts` + `apps/web/src/lib/reconciliation.ts` | Fonte canônica única para formatação, contexto IMOB, derivados econômicos e reconciliação nas páginas-alvo. |
| Guard rails de duplicação e runtime | `scripts/checkFrontendDuplication.ts` + `scripts/checkSelfServiceRuntimeGraph.ts` + `package.json` (`check:frontend-duplication`, `check:self-service-runtime-graph`) | Proteção ativa contra reintrodução de helpers locais e contra consumo runtime de `.js` no self-service. |
| Baseline final de convergência self-service | `artifacts/self-service-runtime-baseline.json` | Estado final convergido com `runtimeSelfServiceJsFiles=[]` e `duplicatePairCount=0`. |
| Resultado final da frente | `apps/web/src/pages/self-service/` | Self-service estruturalmente convergido: `runtime .js = 0` e `duplicate pairs = 0`. |
| Lotes de remoção A/B/C concluídos | `ops/evidence/latest/self-service-dedup-final-validation-2026-04-15.md` | Rastro consolidado de execução por lote (`components`, `leaf pages`, `config/index/router`) com validação final e atualização de baseline. |
| Ciclo final de validação executado (2026-04-15) | `ops/evidence/latest/self-service-dedup-final-validation-2026-04-15.md` | Execução final registrada dos comandos: `pnpm check:self-service-runtime-graph`, `pnpm check:frontend-duplication`, `pnpm --filter @eiah/web build`, `pnpm baseline:self-service-runtime-graph` (todos concluídos com sucesso; warning ES2024 permanece como passivo separado). |
| Institucionalização no fluxo de PR | `.github/workflows/ci.yml` + `.github/pull_request_template.md` + `apps/web/src/pages/self-service/index.tsx` | Checks `check:self-service-runtime-graph` e `check:frontend-duplication` exigidos no CI e checklist de merge para mudanças no self-service. |
| Nova frente separada para passivos remanescentes | `ops/evidence/latest/es2024-tsc-passivos-front-2026-04-15.md` | Escopo e DoD dedicados para warnings de target ES2024 e erros históricos de `tsc --noEmit`, fora da frente já encerrada. |

## Implementado extra (além do escopo normativo v8)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Compact UI global (85%) | `apps/web/src/App.tsx` + `apps/web/src/styles.css` | Ativação global da classe `compact-ui` e redução de escala visual sem alterar fluxo funcional. |
| Access unificado (Entrar/Cadastrar/Wallet) | `apps/web/src/pages/access.tsx` + `apps/api/src/routes/auth.ts` | Experiência unificada de acesso com login legado, provisionamento e autenticação por wallet. |
| Chat Launcher com histórico + nova conversa | `apps/web/src/components/agents/ChatAgentLauncher.tsx` | Histórico preservado por agente com ação explícita de nova conversa e estados de sessão. |
| Estado de processamento no chat | `apps/web/src/components/agents/ChatAgentLauncher.tsx` | Exibição de estado de processamento (`Pensando...`) durante geração de resposta. |
| Fallback contextual determinístico (unknown) | `apps/web/src/components/agents/ChatAgentLauncher.tsx` + `apps/web/src/hooks/useConversation.ts` | Resposta determinística para solicitações fora de escopo EIAH com proteção contra roteamento indevido. |
| Proposal assistant com cálculo guiado | `apps/web/src/components/agents/ChatAgentLauncher.tsx` + `apps/api/src/routes/billing.ts` | Coleta de contexto comercial e cálculo de proposta com regra de billing compatível ao backend. |
| Central de Ajuda (query/reindex/sessões) | `apps/api/src/routes/help.ts` + `apps/api/src/services/eiahHelpKnowledge.ts` + `packages/db/prisma/schema.prisma` | Base interna consultável, reindexação e persistência analítica de atendimentos (`helpdesk_sessions`). |
| IMOB chat ampliado (entrevista + telemetria + export) | `apps/api/src/routes/imob.ts` + `apps/web/src/pages/app/imob/chat.tsx` | Jornada assistida por thread com estado de entrevista, telemetria operacional e export auditável. |
| IMOB rules.configure + jornada de temporada | `apps/api/src/routes/imob.ts` + `apps/api/src/services/imob/imobConversationContract.ts` + `apps/api/src/services/imob/imobConversationState.ts` + `apps/api/src/services/imob/imobTurnResolver.ts` + `apps/api/src/tests/imob-turn-resolver.test.ts` | Fluxo stateful governado de regras de hospedagem com gate por `aluguel_por_temporada`, ação `realestate.configure_property_rules`, jornada canonical `temporada_rules` e retomada de draft. |
| IMOB leitura comercial de pipeline, bloqueios e próxima ação | `apps/api/src/routes/imob.ts` + `apps/api/src/services/imob/imobIntentCatalog.ts` + `apps/api/src/tests/imob-intent-catalog.test.ts` | Intents e builders `pipeline_status`, `blocked_run_resolution` e `next_best_action` para leitura de jornada, pendências, bloqueios e próximo passo usando canonical case sem criar regra no launcher. |
| Track P — IMOB Knowledge Search (implementado parcial / fase 1) | `ops/verticals/vertical-onboarding-checklist.md` + `apps/api/src/routes/imob.ts` + `apps/api/src/services/imob/imobTurnResolver.ts` + `apps/api/src/services/imob/imobConversationContract.ts` | Código e contrato indicam handshake agent-driven e modo `search_knowledge`, com gating por tenant/assinatura; sem evidência dedicada de execução, permanece parcial e não usa roadmap como prova. |
| Hardening estrutural do chat agent-driven | `apps/web/src/components/agents/chatLauncherEngine.ts` + `apps/web/src/components/agents/proposalDomainResolver.ts` + `apps/web/src/components/agents/imobContextResolver.ts` + `apps/web/src/components/agents/legalContextResolver.ts` + `apps/web/src/components/agents/specialistExplainCatalog.ts` + `apps/web/src/components/agents/specialistGuidanceResolver.ts` + `apps/web/src/components/agents/specialistDecisionResolver.ts` + `apps/web/src/components/agents/platformHelpResolver.ts` + `apps/web/src/components/agents/agentPresentationResolver.ts` | Modularização do runtime do chat por domínio/papel, mantendo o `ChatAgentLauncher` em modo `render-first`. |
| Hardening do help EIAH no Chat Launcher (2026-06-01) | `ops/evidence/latest/chat-launcher-help-priority-2026-06-01.md` | Evidência da frente de fechamento de quick replies, precedência explícita de intents, fallback tipado, filtro de chips inválidos e uso mínimo de `conversationState` no runtime do EIAH. |
| Dívida F-08 do runtime de chat — entrypoint único documentado com guardrail leve | `ops/evidence/latest/chat-runtime-entrypoint-debt-2026-07-02.md` | Registra inspeção real do `ChatAgentLauncher` versus `chatLauncherEngine`, lista as chamadas diretas `Launcher -> engine`, confirma ausência de imports verticais profundos no launcher, formaliza a dívida residual de entrypoint único e adiciona o gate `check:chat-runtime-entrypoint-debt` no CI sem declarar F-08 como fechado. |
| Auditoria de git hygiene para tracked ignored files | `ops/evidence/latest/git-hygiene-tracked-files-2026-07-02.md` | Registra auditoria real de `git ls-files -ci --exclude-standard`, resume a política atual do `.gitignore`, classifica o legado rastreado entre `source_keep`, `generated_safe_to_untrack` e `unknown_review`, e adiciona o gate `check:tracked-ignored-files` para bloquear novos arquivos rastreados que já deveriam estar ignorados sem apagar o legado neste PR. |
| Registry unificado de help do EIAH (2026-06-02) | `apps/web/src/components/agents/helpDictionary.ts` + `apps/web/src/components/agents/helpDictionary.global.ts` + `apps/web/src/components/agents/helpDictionary.pages.ts` + `apps/web/src/components/agents/helpDictionary.verticals.ts` + `apps/web/src/components/agents/helpDictionaryResolver.ts` + `apps/web/src/components/agents/helpDictionaryResolver.test.ts` + `ops/evidence/latest/help-dictionary-unified-registry-2026-06-02.md` | Fundação tipada do registry declarativo, resolvedor único com precedência `page > vertical > global`, integração conservadora no `engine` e cobertura inicial sem mudança de layout/UI. |
| Self-service workspace recipe visibility (2026-06-02) | `apps/api/src/routes/tenant-recipes.ts` + `apps/api/src/routes/tenantRecipeWorkspaceSelection.ts` + `apps/api/src/tests/tenant-recipe-workspace-selection.test.ts` + `ops/evidence/latest/self-service-workspace-recipe-visibility-2026-06-02.md` | Correção localizada da listagem `view=workspace` para usar o workspace explicitamente pedido pela sessão do frontend, com validação por tenant e fallback seguro para o workspace do token. |
| Self-service recipe instructions collapse (2026-06-02) | `apps/web/src/pages/self-service/index.tsx` + `ops/evidence/latest/self-service-recipe-instructions-collapse-2026-06-02.md` | Colapso visual de instruções longas nos cards de recipes do workspace, com toggle explícito de expansão/retração e sem regressão de runtime/duplicação. |
| Self-service recipe prefill (2026-06-02) | `apps/web/src/pages/self-service/router.tsx` + `apps/web/src/pages/self-service/generic.tsx` + `apps/web/src/pages/self-service/recipePrefill.ts` + `apps/web/src/pages/self-service/recipePrefill.test.ts` + `ops/evidence/latest/self-service-recipe-prefill-2026-06-02.md` | Recipes homologadas passam a abrir o formulário do agente com `recipeId`, bloco de recipe vinculada e prefill heurístico dos campos para continuidade operacional real. |
| Self-service guardian knowledge gate (2026-06-02) | `apps/web/src/pages/self-service/components/AgentFormShell.tsx` + `apps/web/src/pages/self-service/components/RunStatusCard.tsx` + `apps/web/src/pages/self-service/components/runErrorSummary.ts` + `ops/evidence/latest/self-service-guardian-knowledge-gate-2026-06-02.md` | O self-service passa a enviar `executionInput` canônico para a knowledge gate do backend e exibe mensagem operacional explícita quando o Guardian bloqueia a execução por falta de fontes obrigatórias. |
| Trust score engine Prisma key fix (2026-06-02) | `apps/api/src/services/trustScoreEngine.ts` + `apps/api/src/tests/trust-score-engine.test.ts` + `ops/evidence/latest/trust-score-engine-prisma-key-fix-2026-06-02.md` | Alinha o `trustScoreEngine` à chave única composta `unique_trustscore_agent` do schema Prisma, removendo o erro de `Unknown argument tenantId_workspaceId_agentId` observado após runs do Guardian. |
| Run events Redis outbox readiness (2026-06-02) | `apps/api/src/services/runEvents.ts` + `apps/api/src/services/runEventOutbox.ts` + `apps/api/src/services/runEventsRedisTransport.ts` + `apps/api/src/tests/run-events-redis-transport.test.ts` + `ops/evidence/latest/run-events-redis-outbox-readiness-2026-06-02.md` | Garante readiness do client Redis antes de `xadd/publish` no transporte de eventos de run, reduzindo o erro `Stream isn't writeable` após boot/reconnect sem quebrar o fluxo local de eventos. |
| Guardian tools + checklist por etapa (2026-06-02) | `packages/core/src/actions/guardian.ts` + `packages/core/src/actions/guardianChecklistTools.ts` + `apps/api/src/workers/guardianPlanManager.ts` + `apps/api/src/workers/runWorker.ts` + `apps/web/src/components/runs/RunViewer.tsx` + `apps/web/src/pages/self-service/config.ts` + `ops/evidence/latest/guardian-tools-and-checklist-steps-2026-06-02.md` | O Guardian passa a executar checks reais de runtime/artefatos/policy para a rota de go-live controlado, usa plano específico por etapa, expõe os resultados no viewer e aproxima a execução das instruções da recipe. |
| Cobertura e gate de regressão do chat | `apps/web/src/components/agents/chatLauncherEngine.test.ts` + `.github/workflows/ci.yml` | Cobertura determinística de proposal/help/IMOB/atalhos com gate obrigatório `ChatEngineRegression` no `CI Monorepo`. |
| presentationSnapshot v1 — contrato renderizável frontend evidenciado com escopo | `contracts/presentation-snapshot.v1.schema.json` + `contracts/presentation-snapshot.v1.baseline.json` + `scripts/checkPresentationSnapshotContract.ts` + `package.json` + `.github/workflows/ci.yml` + `apps/web/src/components/agents/chatPresentationSnapshot.ts` + `apps/web/src/components/agents/chatLauncherEngine.test.ts` | Contrato renderizável v1 entre `chatLauncherEngine`/helpers e `ChatAgentLauncher`, formalizado por schema versionado, baseline de compatibilidade, check dedicado `check:presentation-snapshot-contract`, wiring local/CI e testes focados para `snapshotVersion`, `governedRuntime.launcherPolicy=render_only`, limite/shape de `quickReplies` e ausência de campos backend/probatórios. Escopo: contrato frontend/renderizável; não é receipt/proof/backend ledger. |

### Implemented Extra (pronto para PR/changelog)

- **UX compacta global** (`apps/web/src/App.tsx`, `apps/web/src/styles.css`): redução de escala visual sem quebrar responsividade.
- **EIAH Access unificado** (`apps/web/src/pages/access.tsx`, `apps/api/src/routes/auth.ts`): modos `Entrar/Cadastrar/Wallet` no mesmo fluxo.
- **Chat Launcher operacional** (`apps/web/src/components/agents/ChatAgentLauncher.tsx`): histórico por agente, `Nova conversa`, texto dinâmico e estado `Pensando...`.
- **Privacidade no UI** (`apps/web/src/pages/app/agents/index.tsx`, `apps/web/src/components/agents/ChatAgentLauncher.tsx`): remoção de metadados internos visíveis na interface.
- **Fallback determinístico para unknown** (`apps/web/src/hooks/useConversation.ts`, `apps/web/src/components/agents/ChatAgentLauncher.tsx`): proteção contra roteamento indevido fora de escopo.
- **Proposal assistant** (`apps/web/src/components/agents/ChatAgentLauncher.tsx`, `apps/api/src/routes/billing.ts`): coleta guiada comercial e cálculo coerente com billing real.
- **Playbook expandido por página** (`apps/web/src/pages/app/agents/index.tsx`, `apps/web/src/assets/playbook/`): guia operacional em linguagem humana.
- **Central de Ajuda EIAH** (`apps/api/src/routes/help.ts`, `apps/api/src/services/eiahHelpKnowledge.ts`, `packages/db/prisma/schema.prisma`): query/reindex + sessões analíticas.
- **IMOB ampliado** (`apps/api/src/routes/imob.ts`, `apps/web/src/pages/app/imob/chat.tsx`, `apps/web/src/features/imob/`): entrevista de contrato, telemetria e export auditável.
- **Track P — IMOB Knowledge Search (implementado parcial / fase 1)** (`ops/verticals/vertical-onboarding-checklist.md`, `apps/api/src/routes/imob.ts`, `apps/api/src/services/imob/imobTurnResolver.ts`, `apps/api/src/services/imob/imobConversationContract.ts`): código e contrato indicam handshake agent-driven e modo `search_knowledge`, com gating por `tenant/workspace` + assinatura ativa; sem evidência dedicada de execução, permanece parcial.
- **P2 global HIGH coverage** (`scripts/checkP2HighGlobalCoverage.ts`, `ops/evidence/latest/p2-high-global-coverage.json`, `packages/core/src/actions/__tests__/highGlobalCoverage.e2e.test.ts`): transição de “inventariado” para “covered” com gate bloqueante.
- **Chat agent-driven hardening** (`apps/web/src/components/agents/chatLauncherEngine.ts`, `apps/web/src/components/agents/proposalDomainResolver.ts`, `apps/web/src/components/agents/imobContextResolver.ts`, `apps/web/src/components/agents/legalContextResolver.ts`, `apps/web/src/components/agents/specialistGuidanceResolver.ts`, `apps/web/src/components/agents/specialistDecisionResolver.ts`, `apps/web/src/components/agents/platformHelpResolver.ts`, `apps/web/src/components/agents/agentPresentationResolver.ts`, `apps/web/src/components/agents/chatLauncherEngine.test.ts`, `.github/workflows/ci.yml`): engine modularizado por domínio/papel com cobertura de regressão e gate de CI.

## Entry points

| Assunto | Arquivo | Trecho/linhas | O que prova |
| --- | --- | --- | --- |
| API entrypoint | `apps/api/src/index.ts` | 1-125 | Bootstrap da API, rotas e start de worker/outbox. |
| Web entrypoint | `apps/web/src/main.tsx` | 1-10 | Entrada do frontend. |
| CLI entrypoint | `apps/cli/src/index.ts` | 1-58 | Entrada da CLI e comandos básicos. |
| Action-runner entrypoint | `apps/workers/action-runner/src/index.ts` | 1-121 | Boot do worker MCP + gates. |
| Maintenance entrypoint | `apps/workers/maintenance-worker/src/index.ts` | 1-30 | Boot do maintenance-worker. |
| Run-worker standalone | `apps/workers/run-worker/src/index.ts` | 1-212 | Worker de runs fora da API. |

## Rotas da API (runs/events/memory/actions/billing)

| Assunto | Arquivo | Trecho/linhas | O que prova |
| --- | --- | --- | --- |
| Runs list/detail | `apps/api/src/routes/runs.ts` | 39-149 | GET /runs e /runs/:id. |
| Run events + SSE | `apps/api/src/routes/runs.ts` | 151-259 | GET /runs/:id/events e /runs/:id/stream. |
| Run replay | `apps/api/src/routes/runs.ts` | 1253-1323 | POST /runs/:id/replay. |
| Memory ingest/search | `apps/api/src/routes/memory.ts` | 30-102 | POST /memory e /memory/search. |
| Ops (queues drain/redrive) | `apps/api/src/routes/ops.ts` | 155-260 | Drenagem e redrive da fila de runs. |
| DeFi placeholders | `apps/api/src/routes/defi.ts` | 37-79 | TODOs de simulação e envio de tx. |

## Orquestração (core)

| Assunto | Arquivo | Trecho/linhas | O que prova |
| --- | --- | --- | --- |
| AgentOrchestrator | `packages/core/src/orchestrator/agentOrchestrator.ts` | 143-210 | Criação de plano, registro de eventos e persistência de steps. |
| PlanStepRecord schema | `packages/db/prisma/schema.prisma` | 203-215 | Persistência de steps no banco. |
| Action Registry | `packages/core/src/actions/actionRegistry.ts` | 66-111 | Registro e listagem de actions. |
| VersionedActionRegistry | `packages/core/src/actions/registry/VersionedActionRegistry.ts` | 10-69 | Actions versionadas. |
| Agents (lista) | `packages/core/src/actions/agents/index.ts` | 1-78 | Definição de agentes e registro. |

## Filas/workers (BullMQ)

| Assunto | Arquivo | Trecho/linhas | O que prova |
| --- | --- | --- | --- |
| Run queue + DLQ + redrive | `packages/core/src/queue/runQueue.ts` | 145-397 | Attempts, backoff, DLQ e redrive. |
| Action queue + DLQ | `packages/core/src/queue/actionQueue.ts` | 125-255 | Attempts, backoff e DLQ. |
| Ops redrive | `apps/api/src/routes/ops.ts` | 240-259 | Redrive via API. |
| Run-worker (API) | `apps/api/src/workers/runWorker.ts` | 88-103 | Worker dentro da API. |
| Run-worker (standalone) | `apps/workers/run-worker/src/index.ts` | 1-212 | Worker standalone. |

## SSE/replay/cursor/outbox

| Assunto | Arquivo | Trecho/linhas | O que prova |
| --- | --- | --- | --- |
| SSE stream | `apps/api/src/routes/runs.ts` | 190-259 | SSE com heartbeat e cursor. |
| Cursor em listRunEvents | `apps/api/src/services/runEvents.ts` | 81-120 | Paginação por cursor/createdAt. |
| Outbox (XADD) | `apps/api/src/services/runEvents.ts` | 47-76 | Publicação em Redis Stream. |
| Outbox processor | `apps/api/src/services/runEventOutbox.ts` | 89-145 | XREADGROUP + publish no Redis. |
| UI fallback polling | `apps/web/src/components/runs/RunViewer.tsx` | 303-337 | Fallback para polling quando SSE falha. |

## Governança (intent/trust/judge/ledger)

| Assunto | Arquivo | Trecho/linhas | O que prova |
| --- | --- | --- | --- |
| Intent Validator | `apps/api/src/services/intentValidator.ts` | 69-135 | Score/verdict + registro em ledger. |
| Trust Score engine | `apps/api/src/services/trustScore.ts` | 21-128 | Cálculo de Trust Score e gate. |
| Trust Score audit | `apps/api/src/services/trustScoreEngine.ts` | 21-95 | Atualização de score com audit. |
| Judge heurístico | `apps/api/src/services/judge.ts` | 25-70 | Heurística de PII + flags. |
| Judge LLM | `apps/api/src/services/judgeGate.ts` | 62-123 | Judge com LLM e policy judge-v1. |
| Judge gate no runner | `apps/workers/action-runner/src/index.ts` | 274-507 | Enforce/shadow e bloqueio. |
| RBAC middleware | `apps/api/src/middlewares/requireScope.ts` | 12-55 | Check de scope para API. |
| RBAC core fail-closed | `packages/core/src/policy/TenantPolicyStore.ts` + `ops/evidence/latest/tenant-policy-store-enforcement-2026-06-28.json` + `ops/evidence/latest/rbac-scope-fail-closed-2026-06-28.json` | arquivo completo | Policy store persistente com deny para policy ausente, inválida, de outro workspace ou indisponível. |
| GuardrailLedger store | `packages/core/src/services/guardrailLedgerStore.ts` | 6-90 | Persistência no DB. |
| Guardrail core persistente | `packages/core/src/audit/guardrailLedger.ts` + `ops/evidence/latest/guardrail-ledger-rbac-events-2026-06-28.json` | arquivo completo | Adapter persiste GuardrailLedger e GuardrailAuditLedger; gate anti-noop está evidenciado. |

## SCL/critical actions

| Assunto | Arquivo | Trecho/linhas | O que prova |
| --- | --- | --- | --- |
| SCL off-chain signing | `packages/core/src/services/sclLedger.ts` | 72-240 | Hash/assinatura e persistência do SCL. |
| Signer Vault/HTTP/local | `packages/core/src/security/signerManager.ts` | 123-178 | Provedores de assinatura. |
| Vault signer | `packages/core/src/security/vaultSigner.ts` | 3-33 | Integração com Vault HTTP. |
| SCL schema | `packages/db/prisma/schema.prisma` | 455-477 | Tabela `scl_ledger`. |
| ToolContract executor web3 not implemented | `packages/mcp-runner/src/executor/MCPExecutor.ts` | 44-96 | Executor web3 não implementado. |

## Prisma schema (entidades)

| Assunto | Arquivo | Trecho/linhas | O que prova |
| --- | --- | --- | --- |
| Run + RunEvent | `packages/db/prisma/schema.prisma` | 146-201 | Entidades de execução e eventos. |
| GuardrailLedger/AuditLedger | `packages/db/prisma/schema.prisma` | 437-499 | Auditoria e ledger. |
| ToolContract | `packages/db/prisma/schema.prisma` | 394-413 | Contratos MCP. |
| MemorySnapshot/EmbeddingChunk | `packages/db/prisma/schema.prisma` | 304-356 | Memória e vetores. |

## UI RunViewer

| Assunto | Arquivo | Trecho/linhas | O que prova |
| --- | --- | --- | --- |
| RunViewer SSE + polling | `apps/web/src/components/runs/RunViewer.tsx` | 250-337 | SSE com cursor e fallback. |

## Config/.env (o que existir)

| Assunto | Arquivo | Trecho/linhas | O que prova |
| --- | --- | --- | --- |
| Variáveis de governança | `.env.template` | 48-126 | Intent Validator, Trust Score, signer, SCL off-chain. |
| MCP + outbox | `.env.governance.example` | 13-23 | MCP enforcement e outbox stream. |
| Infra dev (Docker) | `docker-compose.dev.yml` | 15-201 | Serviços API/Web/Workers/DB/Redis. |

## Divergências / impactos

- Dois run-workers coexistem (API e standalone) → risco de divergência operacional.  \
EVIDÊNCIA: `apps/api/src/index.ts:97-103` + `apps/workers/run-worker/src/index.ts:1-212`.

## Buscas registradas (encontrado vs não encontrado)

| Assunto | Comando | Resultado |
| --- | --- | --- |
| Endpoint de aprovação humana (`/runs/:id/approve`) | `rg -n "runs/:id/approve|approve" apps packages` | ENCONTRADO: `apps/api/src/routes/runs.ts:722-775` |
| Campos Run.approval_status/approvedBy | `rg -n "approval_status|approvedBy" packages/db` | ENCONTRADO: `packages/db/prisma/schema.prisma:197-198` |
| Endpoint público `/ledger/:txId` | `rg -n "/ledger/:txId|ledger/:txId" apps packages` | ENCONTRADO: `apps/api/src/routes/governance.ts:222` |
| TrustScoreToken / tokenização de reputação | `rg -n "TrustScoreToken|reputação|tokenização" apps packages` | ENCONTRADO apenas em `apps/api/backup-20251031-103132.sql` (texto de backup, não implementação) |

## Status do Roadmap (consolidado por evidência)

| Item | Evidências | Status (no repo) | Divergência com Roadmap |
| --- | --- | --- | --- |
| Fase 4 — Gate pré‑execução (SCL obrigatório) | `apps/workers/action-runner/src/index.ts:841-919` | Implementado | Componente evidenciado; F4 permanece parcial avançada e sujeita a hardening recorrente. |
| Fase 4 — Resiliência do Signer | `packages/core/src/security/signerManager.ts:225-269` | Implementado | Componente evidenciado; não fecha isoladamente a F4. |
| Fase 4 — Reconciliação Guardrail ↔ SCL | `apps/workers/maintenance-worker/src/index.ts:286-383` + `packages/core/src/services/reconcileLedgerService.ts:15-233` | Implementado | Componente evidenciado; recorrência operacional continua obrigatória. |
| Fase 5.0 — Marketplace (catálogo + delegações) | `packages/db/prisma/schema.prisma:564-604` + `apps/api/src/routes/marketplace.ts:7-212` + `apps/web/src/pages/self-service/index.tsx:51-227` | Implementado (core) | Compatível (Roadmap v8: ✅ core concluído; UX avançada ainda pendente) |
| Fase 5.0 — “UX de delegação avançada” | **NÃO ENCONTRADO** (`rg -n "delegation advanced|delegacao avancada|delegação avançada|advanced delegation"`) | Não evidenciado | Compatível (Roadmap v8: fechamento de UX/auditoria avançada ainda pendente) |
| Fase 5.1 — PoU (modelo + serviço + pipeline + eventos) | `packages/db/prisma/schema.prisma:520-547` + `apps/api/src/services/pouService.ts:1-220` + `apps/workers/action-runner/src/index.ts:841-1314` + `packages/contracts/src/runEvent.schema.json:16-46` | Implementado | Compatível (Roadmap v8: ✅ operacional; manter hardening recorrente) |
| Fase 5.1 — Trust Gate (score + gate) | `apps/api/src/services/trustScore.ts:21-129` + `apps/workers/action-runner/src/index.ts:189-233` | Implementado | Compatível (Roadmap v8: ✅ operacional; manter hardening recorrente) |

## Checklist de completude

- Resumo executivo: OK
- Estado atual (implementado/parcial/planejado/não encontrado): OK
- Arquitetura por camadas: OK
- Fluxos principais: OK
- Governança cognitiva e execução crítica: OK
- IAs/modelos e responsabilidades: OK
- Benefícios B2B ligados a componentes: OK
- Riscos + mitigação: OK
- Próximos passos priorizados: OK
- Lista completa usados/não usados: OK

## Atualizações 2026-06-09

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Self-service UX — 3 ondas (catálogo filtrável, spinner incremental, persistência NeedMoreInfo) | `ops/evidence/latest/self-service-ux-improvements-2026-06-09.md` | Onda 1: `domain`/`exampleOutput` nos 14 agentes + filtro por domínio/texto no catálogo. Onda 2: spinner com label de evento real, sentinel de conclusão 3 s, botão Reexecutar com valores do run anterior. Onda 3: sessionStorage TTL-30 min no `NeedMoreInfoDialog`, banner de restauração, aviso de contexto stale no reexecutar, diff visual shadow vs real. |

## Atualizações 2026-06-02

- `ops/evidence/latest/guardian-run-hardening-and-export-alignment-2026-06-02.md`
  - hardening do worker para bloquear `success` em saída truncada (`finish_reason === "length"`)
  - deduplicação do prefill de recipes do `guardian` no self-service
  - alinhamento do export/preview para ignorar artefatos genéricos de `runAtivoUniversal` incompatíveis com runs do `guardian`
- `ops/evidence/latest/guardian-run-viewer-and-payload-cleanup-2026-06-02.md`
  - remoção do resumo genérico de campanha no viewer do `guardian`
  - cleanup de redundância entre `metadata.form`, `executionInput` e `rawPayload`
  - correção da exibição de `exploracao_pct`
  - apresentação do contexto/evidências do `guardian` como checklist probatório
- `ops/evidence/latest/guardian-export-and-score-alignment-2026-06-02.md`
  - export HTML/PDF do `guardian` alinhado a contexto probatório, sem fallback de pitch/campaign
  - unificação da origem de `diagnóstico`
  - remoção de badge redundante de delta/score nulo
- `ops/evidence/latest/guardian-prompt-compaction-2026-06-02.md`
  - compactação de `notes` no prefill do `guardian`
  - prompt do `guardian` reduzido e com pedido de output mais curto
  - orientação operacional sobre bytes estimados versus tokens reais
- `ops/evidence/latest/guardian-core-action-proxy-fallback-2026-06-02.md`
  - fallback local para actions core do `guardian` quando `MCP_PROXY_ALL_ACTIONS` está ativo
  - preservação do proxy MCP para tools externas com `ToolContract`
  - correção do erro `ToolContract missing: guardian.checkRuntimeHealth@1.0.0`

## CC → Chat IMOB — Despacho por actionId (2026-06-16)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Fase 1 — Transporte de contexto CC→Chat | `apps/web/src/features/imob/imobCommandCenterHelper.ts` + `apps/web/src/features/imob/ImobCommandCenter.tsx` + `apps/web/src/features/imob/imobApiClient.ts` + `apps/web/src/lib/api.ts` + `apps/web/src/pages/app/imob/chat.tsx` + `apps/web/src/features/imob/imobCommandCenterPhase1.test.ts` | CTA "consultar no chat" com `actionId`/`reasonCode`/`status` na URL; `ImobCommandCenterCaseRow` expandido com `recommendedActions[]`; badge "consulta — não altera estado" no `mode=consult`; 5/5 testes passando. |
| Fase 2 — Dispatcher backend valida actionId | `apps/api/src/services/imob/crm/imobCrmActionDispatcher.ts` + `apps/api/src/routes/imob.ts` + `apps/api/src/tests/imob-crm-action-dispatcher.test.ts` | Artefato: `docs/ops/evidence/latest/imob-cc-chat-resolution/phase2-action-dispatch.md`. Função pura `resolveImobCrmActionDispatch`: valida `actionId` vs `canonical.recommendedActions`, mapeia 11 actionIds operacionais para `executionRequest`, retorna null para consultivos (fall-through ao engine), retorna `mode=blocked` para inválidos; proteção cross-workspace; 16/16 testes unitários passando. |
| Fase 3 — Confirmação explícita antes de apiAgentsExecute | `apps/web/src/features/imob/imobChatDirectedAction.ts` + `apps/web/src/pages/app/imob/chat.tsx` + `apps/web/src/features/imob/imobChatPhase3DirectedAction.test.ts` | Artefato: `docs/ops/evidence/latest/imob-cc-chat-resolution/phase3-chat-confirmation.md`. `shouldUseDirectedActionFlow` gate; `prepareDirectedActionExecution` aguarda confirmação sem chamar `apiAgentsExecute`; badge "ação direcionada — aguardando confirmação"; guard `directedConfirmingRef` contra double-click; `source: "command-center"` no metadata via `buildAgentsExecuteMetadata`; 12/12 testes passando; nenhum erro TS novo. DoD Fase 4 documentado (não implementado). |
| Phase 4.0 — Action Contract & Handler Alignment | `apps/api/src/routes/agents.ts` + `apps/api/src/services/imob/crm/imobCrmActionDispatcher.ts` + `apps/api/src/services/imob/control/imobRunActionCatalog.ts` + `apps/api/src/actions/realestateActions.ts` + `apps/api/src/actions/tenantActionRegistry.ts` + `apps/api/src/tests/imob-realestate-action-contracts-11.test.ts` | Artefatos: `phase4-0-contract-handler-alignment.md` + `phase4-worker-option-c-decision.md`. B1 resolvido: 5 contratos novos (activate_listing/qualify_lead/schedule_visit/collect_documents/review_deal) → 9/9 ACTION_CONTRACTS, 11/11 actionIds cobertos. B2 resolvido: stubs fail-closed explícitos `registerRealestateActions()` com reasonCode HANDLER_PENDING_PHASE_4_3. B5 resolvido: `listing.activate → realestate.activate_listing` (não apply_adjustment); apply_adjustment permanece financeiro. Decisão: Opção C (ImobPostRunMutationWorker) escolhida; Opção B descartada. 5/5 suítes de testes passando; zero erros TS novos; sem mutation de ImobCase.status; sem PATCH. Bloqueadores restantes: B3 (mutation pós-run) e B4 (canonical recalculation) — pendentes Fase 4.1/4.2. |
| Phase 4.1 Pré-flight (worker) | `docs/ops/evidence/latest/imob-cc-chat-resolution/phase4-1-worker-preflight.md` | Investigação técnica pura (sem alteração de código). Confirma: `Run.caseId` propagado via `executionInput.caseId`; `Run.request.metadata.executionInput.actionId` preservado; `ImobCrmMutationService.updateCase` pronto; BullMQ pattern estabelecido. Identifica 4 bloqueadores: P1 (decisão de produto ausente), P2 (buildImobCanonicalCase não exportada), P3 (execução simulada emite success), P4 (fila durável ausente). Veredicto: NO-GO. |
| Phase 4.1a — Product Outcome Matrix | `docs/ops/evidence/latest/imob-cc-chat-resolution/phase4-1a-product-outcome-matrix.md` | Resolve P1 (decisão de produto). 11/11 actionIds com outcome completo: status resultante, stage, nextStep, pendingItems removidos/adicionados, blockers, reasonCodes, failure_behavior, simulated_behavior, economy impact, receipt/bundle. Decisões registradas: commission.settle é único terminal; visit.schedule e documents.* produzem pending_data; flow não é alterado pelo worker; simulated nunca muta. DoD Phase 4.1b documentado (P2/P3/P4). GO para implementar 4.1b. |
| Phase 4.1b — Worker Foundation | `docs/ops/evidence/latest/imob-cc-chat-resolution/phase4-1b-worker-foundation.md` | Resolve P2/P3/P4. P2: `buildImobCanonicalCase` + tipos canonicais extraídos de `routes/imob.ts` para `services/imob/imobCanonical.ts`; `routes/imob.ts` atualizado com import. P3: `shouldSkipImobPostRunMutationForSimulatedOutput(run)` — guard puro; retorna `true` se qualquer `outputs[].data.simulated === true`. P4: `apps/api/src/queues/imobRunCompletedQueue.ts` — BullMQ, payload={runId,tenantId,workspaceId,caseId,actionId,eventRunId,receiptPath?,bundlePath?}, jobId idempotente por runId. Testes: 17/17 pass (5 suítes). Regressão Phase 4.0: 34/34 inalterado. Sem mutation real. GO para Fase 4.1c após novo pré-flight. |
| Phase 4.1c — ImobPostRunMutationWorker real | `docs/ops/evidence/latest/imob-cc-chat-resolution/phase4-1c-worker-mutation.md` | Resolve P5 + implementação completa do worker. P5: `IMOB_DISPATCHER_ACTION_IDS` exportado de `imobCrmActionDispatcher.ts`; `runWorker.ts` enfileira condicionalmente após `enqueueRunAtivoUniversal` usando `baseMetadata.executionInput.{caseId,actionId}`. Worker: `apps/api/src/workers/imobPostRunMutationWorker.ts` — `IMOB_RUN_OUTCOME_MAP` (11 actionIds, Phase 4.1a matrix), 10 guards ordenados (campos, canonicidade, outcome, run.status, simulated, txId, idempotência DB, case exists, ownerResponsible, commission.settle), `ImobCrmMutationService.updateCase`, canonical recalculation via `buildImobCanonicalCase`, receipt/bundle derivados de `run.txId`/`run.id`. Registrado em `index.ts` junto a `startRunArchiveWorker`. Invariantes: `ImobCase.status` decidido exclusivamente no backend; React sem regra de status. Testes: 10/10 suítes pass (T1-T10, 21 assertions). Sem erros TS novos. |
| Phase 4.2 — E2E integrado CC→Run→Worker→ImobCase | `docs/ops/evidence/latest/imob-cc-chat-resolution/phase4-2-e2e-worker-resolution.md` | Prova integrada da cadeia completa contra banco de dados real. 8 cenários E2E, 9/9 testes passando. Arquivo: `apps/api/src/tests/imob-post-run-mutation-e2e.test.ts`. Confirmados com IDs reais: E2E-01 happy path (owner.register → stage=property_collecting, status=ready_for_review, receiptPath=/api/ledger/tx-*, dossier API 200 OK com canonical=property_capture); E2E-02 idempotência (already_processed_skip, 1 evento no DB); E2E-03 simulated=true → skipped_simulated_run, case inalterado; E2E-04 run.status=error → run_not_success_skip, case inalterado; E2E-05 cross-workspace → run_not_found, case inalterado; E2E-06 commission.settle → stage=done, status=done, pendingItems=[], blockers=[], case.completed terminal event, dossier API 200 OK; E2E-07 lead.qualify sem txId → mutado (requiresTxId=false); E2E-08 owner.register sem txId → receipt_required_no_tx_id, case inalterado. Invariantes I1–I7 todos provados por banco real. Marco: Fase 4 completa — CC→Chat→confirmação→run→worker→mutation governada→canonical→CC refresh. |
| Phase 4 Final Summary — Marco consolidado Fases 1–4.2 | `docs/ops/evidence/latest/imob-cc-chat-resolution/phase4-final-summary.md` | Índice completo de todas as evidências Fases 1–4.2, cadeia validada, 10 invariantes operacionais com provas por cenário, 5 recomendações de hardening (regressão, métricas, alertas, docs de produto, demo), texto de marco para roadmap/changelog. Cadeia: CC→Chat→confirmação→run→worker→mutation governada→canonical→CC refresh — evidenciada em E2E contra banco real. |
| IMOB Worker Observability — Kickoff (proposta) | `docs/ops/evidence/latest/imob-worker-observability/frente-kickoff.md` | Nova frente aberta após encerramento da CC→Chat (Fases 1–4.2). Sem alteração de fluxo funcional. Escopo H1–H5: H1 métricas do worker (counters por reason code), H2 alertas de fila (3+ regras staging), H3 E2E IMOB em CI como gate de merge, H4 monitor de skip/blocked reasons para oncall, H5 demo final gravada. Sequência recomendada: H3→H1→H2→H4→H5. DoD por item definido. Classificação: PROPOSTA (sem implementação iniciada). |
| H3 — CI Regression Gate (IMOB Worker Mutation E2E) | `docs/ops/evidence/latest/imob-worker-observability/h3-ci-regression-gate.md` | Gate CI ativo para E2E-01..E2E-08. Arquivos: `imob-post-run-mutation-e2e.test.ts` (close BullMQ queue no after hook — resolve exit hang), `package.json` script `test:imob-worker:e2e`, `.github/workflows/imob-worker-e2e.yml` (postgres:16 + redis:7 services, migrate:deploy, timeout 5min). 9/9 testes passando localmente com exit limpo. PR bloqueado se qualquer E2E falhar. Nenhum código funcional alterado. |
| H1 — ImobWorkerMetrics (counters in-memory) | `docs/ops/evidence/latest/imob-worker-observability/h1-worker-metrics.md` | Counters in-memory puros para ImobPostRunMutationWorker. Novo módulo `imobWorkerMetrics.ts`: `incrementCounter`, `getCounterSnapshot`, `renderCountersAsPrometheusText`, `resetCountersForTesting`. 4 counters: `imob_run_completed_jobs_total{actionId}`, `imob_post_run_mutations_applied_total{actionId,terminal,requiresTxId}`, `imob_post_run_skips_total{actionId,reason}` (12 reason codes), `imob_post_run_failures_total{reason}`. 12 pontos de incremento no worker. Exposto em `/metrics-prom`. Zero PII em labels (T-M6 confirmado). 13/13 testes passando (7 suítes T-M1..T-M7). E2E H3 reconfirmado 9/9. |
| H2 — ImobWorkerAlerts (regras de alerta puras) | `docs/ops/evidence/latest/imob-worker-observability/h2-worker-alerts.md` | Módulo `imobWorkerAlerts.ts` com 7 regras: IMOB-W-001 (ERROR: job_permanently_failed > 0), IMOB-W-002 (ERROR: receipt_required_no_tx_id > 0 = audit gap HIGH), IMOB-W-003 (WARNING: simulated em produção), IMOB-W-004 (WARNING: duplicate rate > 30%), IMOB-W-005 (WARNING: run_not_success rate > 20%), IMOB-W-006 (ERROR: job_error rate > 5%), IMOB-W-007 (WARNING: queue stall por janela de 5 min). `evaluateImobWorkerAlerts` snapshot-based + `evaluateImobWorkerStall` delta-based. Thresholds configuráveis via `AlertConfig`. Zero PII em AlertEvent (T-A5 confirmado). Sem false positives em snapshot vazio (T-A6). 21/21 testes passando (8 suítes T-A1..T-A8). |
| H4 — Runbook e Skip Reason Monitor | `docs/ops/evidence/latest/imob-worker-observability/h4-skip-reason-monitor.md` | Runbook operacional `docs/ops/runbooks/imob-worker-observability.md`: cadeia CC→Chat→run→worker→case; tabela 7 ruleIds (IMOB-W-001..IMOB-W-007) com severidade, causa, investigação, mitigação, escalonamento, evidência pós-incidente; queries seguras sem PII; política de PII. Monitor `h4-skip-reason-monitor.md`: todos os reasonCodes cobertos (alertáveis + silenciosos), thresholds de DEFAULT_ALERT_CONFIG, owner operacional por regra, links para runbook. Teste `imob-worker-h4-runbook.test.ts`: T-R1..T-R5 (25 assertions em 5 suítes): todos os 7 ruleIds presentes em ambos documentos; sem TODO/FIXME; sem tokens PII; severidades conferem com DEFAULT_ALERT_CONFIG. 25/25 testes passando. Nenhum código funcional alterado. |
| H1 — Fix teardown do metrics test | `apps/api/src/tests/imob-worker-metrics.test.ts` | `pnpm test:imob-worker:metrics` pendurava indefinidamente. Causa raiz: `@repo/db` cria `pg.Pool` no module-level que mantém timers internos mesmo sem queries abertas. Fix 1: adicionado `after()` hook com `imobRunCompletedQueue.close()` + `closePrismaResources()`. Fix 2: script `test:imob-worker:metrics` em `package.json` adicionado `--test-force-exit` (Node.js 22). Agora 14/14 pass com exit limpo. Padrão documentado: qualquer teste que importe `imobPostRunMutationWorker.ts` precisa fechar `imobRunCompletedQueue` e `prismaResources` no teardown. |
| H5 — Demo Final & Validação Recorrente | `docs/ops/evidence/latest/imob-worker-observability/h5-final-demo-validation.md` | Encerramento da frente IMOB Worker Observability & Regression Gates. Roteiro reproduzível em 4 passos: E2E worker (9/9), observability suite H1+H2+H4 (14+21+25=60/60), verificação de /metrics-prom, avaliação manual de alertas. Script agregador `pnpm test:imob-worker:observability` (H1+H2+H4). Total: 69/69 testes passando. Checklist de encerramento: todos os 5 itens H1–H5 evidenciados. Zero alterações no fluxo funcional (outcome map, guards, React/Chat, MutationService inalterados). Frente ENCERRADA/EVIDENCIADA. |

## IMOB Chat Document Intake — feat/imob-chat-document-intake (2026-06-17)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Phase 2 — Worker Intake Handler + E2E (PARCIAL AVANÇADO) | `docs/ops/evidence/latest/imob-chat-document-intake/phase-2-worker-e2e.md` | Branch `feat/imob-chat-document-intake`, commit `600a943`. Cadeia completa: upload .docx → maskContractPii → extractLeaseContractFromText → classifyImobContract → createDraft (TTL 30min) → confirm → createRunRecord (agent=EIAH) → processIntakeRun → ImobCase (stage=documents_collecting, status=ready_for_review, flow=documents.collect) → 2x ImobCaseEvent (case.action.completed + case.document.intake). Idempotência dupla: #1 por documentHash via evidenceRef (reasonCode EXISTING_CASE_FOUND); #2 por runId (entrega duplicada BullMQ). Sentinel "INTAKE" nunca persiste como caseId real (E2E-IN-07). piiMasked=true em todo payload/metadata persistido (E2E-IN-06). Invariantes I-IN-1..8 provados por banco real. Resultados: 1A 37/37, 1B 41/41, Phase 2 E2E 13/13, check:evidence-index ok. ChatAgentLauncher intocado. Fases 3+ (UI, export, lifecycle completo) não implementadas — classificação PARCIAL AVANÇADO, não DONE. |
| Phase 3 — Export Renderer + Endpoint (PARCIAL AVANÇADO) | `docs/ops/evidence/latest/imob-chat-document-intake/phase-3-export.md` | Branch `feat/imob-chat-document-intake`, commit `d4b13e5`. Export server-side de runs de intake: `GET /api/imob/runs/:runId/intake/export?format=html\|docx\|pdf`. Renderer via `imobContractIntakeRenderer.ts`: HTML puro (XSS escaping) + DOCX via JSZip/OOXML (zero deps novas). PDF delegado ao frontend (jspdf em apps/web) — retorna guidance JSON com `htmlExportUrl`. Guards: scope tenant/workspace, `piiMasked=true` em event payload, anti-PII scan (`hasPiiResidue()`) em nextStep/pendingItems/riskFlags, cross-tenant isolation por duplo findFirst. Export lê exclusivamente de run/case/event persistidos — nunca de draft em memória. Falha de geração retorna `EXPORT_GENERATION_FAILED` sem rollback. Headers: `X-Export-Hash` (SHA-256), `X-Generated-At` (ISO 8601). Invariantes I-EXP-1..9. Resultados: 1A 37/37 + 1B 41/41 + Fase 2 E2E 13/13 + renderer 15/15 + export 13/13 = 119/119, check:evidence-index ok (281). ChatAgentLauncher intocado. Stage/status/journeyType inalterados. UI/worker não alterados — classificação PARCIAL AVANÇADO, não DONE. |
| Phase 4 — Chat UI render-only, local-docker (PARCIAL AVANÇADO) | `docs/ops/evidence/latest/imob-chat-document-intake/phase-4-chat-ui-staging.md` | Branch `feat/imob-chat-document-intake`, commit `9b5c2e9`. Widgets Chat UI render-only: `ImobContractIntakeDraftCard` (pending→confirming→confirmed/error), `ImobContractIntakeResultCard` (export HTML/DOCX/PDF guidance). API real local-docker validada: upload DOCX → draft card payload (`piiMasked=True`, `hasCPF=False`), export HTML 200 com `X-Export-Hash`, export DOCX 200, PDF guidance `PDF_DELEGATED_TO_FRONTEND`. Testes: 36/36 (`renderToStaticMarkup`; DC-1..14, RC-1..16, AC-1..6). Total acumulado 155/155. Invariantes I-UI-1..8: ChatAgentLauncher intocado, UI render-only, PII ausente dos cards, PDF é guidance JSON (não arquivo), stage/status/journeyType inalterados. Nota: confirm endpoint tem bug pré-existente (RunStatus enum mismatch `"queued"`), não introduzido pela Fase 4. check:evidence-index ok (283). Classificação PARCIAL AVANÇADO, não DONE. |
| Phase 4.6 — RunStatus Confirm Fix, local-docker (PARCIAL AVANÇADO) | `docs/ops/evidence/latest/imob-chat-document-intake/phase-4.6-confirm-fix.md` | Branch `feat/imob-chat-document-intake`. Bug corrigido: `status: "queued" as any` → `status: "pending"` em `apps/api/src/routes/imob.ts:3779`. `"queued"` não existe no enum `RunStatus` (`{pending,running,success,error,blocked}`); Prisma lançava runtime error. Fix usa `"pending"` (status canônico inicial). Testes CONF-01..06 contra banco real (supertest + worker direto): confirm retorna `runStatus=pending`, DB confirma `status=pending`, regression guard (DB rejeita `'queued'` como enum inválido), draftId inválido→409, sem auth→401, worker processa run success→ImobCase criado. Validação local-docker: upload→confirm (`runStatus=pending`)→DB pending→export HTML 200 (`X-Export-Hash=c27506a9...`)→DOCX 200→PDF `PDF_DELEGATED_TO_FRONTEND`. Invariantes I-46-1..7. Total acumulado 161/161. ChatAgentLauncher intocado. stage/status/journeyType inalterados. Classificação PARCIAL AVANÇADO, não DONE. |
| Phase 5 — Lifecycle Hardening, local-docker (PARCIAL AVANÇADO) | `docs/ops/evidence/latest/imob-chat-document-intake/phase-5-lifecycle.md` | Branch `feat/imob-chat-document-intake`. Lifecycle hardening: draft consume/scope guards, UploadedDocument persistence, worker RunStatus guards. Testes LC-01..06 contra banco real: LC-01 draft consumido em confirm (re-confirm→409), LC-02 cross-workspace confirm→403 DRAFT_SCOPE_MISMATCH, LC-03 upload cria uploadedDocument (tenantId/workspaceId/agentSlug=imob-intake/MIME), LC-04 storageRef no draft = uploadedDocument.id no DB, LC-05 run.status=pending→worker skip (nenhum ImobCase), LC-06 run.status=running→worker skip. Local-docker smoke: upload→confirm (pending)→DB pending→export HTML/DOCX/PDF todos OK. Política MVP documentada em `docs/ops/imob-chat-intake-lifecycle-policy.md`: draft TTL=30min in-memory (não-durável por design), uploadedDocument sem auto-delete MVP, RunStatus transitions documentadas, BullMQ jobId determinístico, idempotência dupla (documentHash + runId). Invariantes I-LC-1..7. Total acumulado 167/167. ChatAgentLauncher intocado. stage/status/journeyType inalterados. Classificação PARCIAL AVANÇADO, não DONE. |
| Phase 5.5 — Final E2E / Release Candidate (PARCIAL AVANÇADO) | `docs/ops/evidence/latest/imob-chat-document-intake/phase-5.5-e2e-rc.md` | Branch `feat/imob-chat-document-intake`. Mecanismo canônico de self-completion: confirm endpoint cria run com status=success diretamente (intake síncrono — processamento feito no upload) + enfileira job via enqueueImobRunCompleted. Elimina dependência do action runner para o fluxo de intake. Bugfix BullMQ jobId: separadores `:` substituídos por `-` (BullMQ 5.x rejeita `:` em custom jobIds — bug nunca ativado antes por ausência de chamada real). Testes RC-01..03 contra banco real: RC-01 upload→confirm (runStatus=success, mutationQueued=true)→processJob direto→ImobCase (documents_collecting/ready_for_review)+event case.document.intake criados sem run.update manual; RC-02 exports HTML/DOCX/PDF 200 OK; RC-03 piiMasked=true em evidenceDraft. Todos os testes anteriores re-executados: confirm 6/6, lifecycle 6/6, E2E fase 2 13/13, RC 3/3 = 28/28 total da rodada. Total acumulado feature 170/170. BullMQ delivery automática validada pelo script bash (validate-imob-chat-intake-phase55.sh) em Docker (worker IS running). Em tests NODE_ENV=test o worker não é iniciado (guard em index.ts:110) — padrão de teste: processImobRunCompletedJob direto. Invariantes I-RC-1..7. ChatAgentLauncher intocado. stage/status/journeyType inalterados. worker não alterado. Classificação PARCIAL AVANÇADO, não DONE. |
| Phase 5.6 — RC Evidence Final (PARCIAL AVANÇADO) | `docs/ops/evidence/latest/imob-chat-document-intake/phase-5.5-final-e2e-rc.md` | Commit `fd6cfc2`. Evidência canônica de aceite da Fase 5.5: documenta self-completion do confirm endpoint (run criado com status=success porque intake é síncrono, processamento já ocorreu no upload), enqueueImobRunCompleted chamado (mutationQueued=true), bugfix BullMQ jobId (`:` → `-`). Resultados: confirm 6/6, lifecycle 6/6, E2E fase 2 13/13, RC 3/3 — total acumulado 170/170. check:evidence-index ok, 287 refs. Pendências P1 registradas: object storage (obrigatório antes de múltiplas instâncias), draft durável Redis (P2), auto-delete uploads (P2), monitoramento drafts expirados (P3). ChatAgentLauncher intocado. stage/status/journeyType inalterados. worker não alterado. Classificação PARCIAL AVANÇADO, não DONE. |
| Phase 6 — Production Readiness / Go-No-Go (PILOTO CONTROLADO) | `docs/ops/evidence/latest/imob-chat-document-intake/production-readiness-go-no-go.md` | Smoke local-docker 10/10 (API health, upload, confirm runStatus=success+mutationQueued=true, DB run success, ImobCase criado pelo worker BullMQ automático, event case.document.intake, export HTML X-Export-Hash, DOCX 200, PDF reasonCode=PDF_DELEGATED_TO_FRONTEND). Decisão Go/No-Go: GO para piloto single-node, NO-GO para multi-instância (requer object storage P1). Storage atual: filesystem local do processo API — compartilhamento impossível em N instâncias. Draft in-memory: TTL 30min, não-durável, impacto aceitável em single-node (comunicar ao usuário em caso de restart). Rollback plan documentado: reversão do commit fd6cfc2 é segura (dados existentes não corrompidos). Pendências formalizadas: object storage P1 bloqueante, draft Redis P2, auto-delete uploads P2, retenção legal P2, monitoramento P3. Total testes integração 170/170. check:evidence-index ok, 288 refs. Classificação: PILOTO CONTROLADO — não declarar DONE global. |
| Phase 6.1 — Pilot Rollout Guardrails (PILOTO CONTROLADO) | `docs/ops/imob-chat-intake-pilot-runbook.md` | Runbook operacional de piloto controlado single-node: escopo do piloto (upload/confirm/export, actionId=imob.contract.intake apenas), pré-requisitos por workspace (agentAssignment.enabled=true), limites de volume (10 MB max DOCX, alerta > 50 uploads/dia antes de object storage), limitações documentadas (filesystem local, draft in-memory TTL 30min, sem auto-delete, sem multi-instância), procedimento de re-upload após restart, métricas mínimas (logs imob-intake.case_created/run_not_success_skip/idempotent_skip, tamanho de uploads/, jobs failed na fila Redis), critérios de promoção (object storage P1, draft Redis P2, SLO 7 dias, volume > 50 uploads/semana), critérios de bloqueio/rollback (PII não mascarado, duplicação de ImobCase, taxa de erro > 10%), rollback procedure (revert fd6cfc2, dados pós-fd6cfc2 não corrompidos). Classificação: PILOTO CONTROLADO — não declarar DONE global. |
| Phase 8 — Storage Provider Abstraction (PILOTO CONTROLADO) | `docs/ops/evidence/latest/imob-chat-document-intake/phase-8-storage-provider.md` | Abstração de storage implementada para `UploadedDocument`/DOCX com `StorageProvider` (`putObject/getObject/exists/deleteObject`), `LocalStorageProvider` preservando o modo atual e `ObjectStorageProvider` preparado por `env` (`STORAGE_PROVIDER`, `OBJECT_STORAGE_BUCKET`, `OBJECT_STORAGE_PREFIX`). `storageKey` scoped por `tenantId/workspaceId`, path traversal bloqueado, upload/download e leituras posteriores migradas para buffer via provider. Validação local-docker: provider unitário 5/5 com exit limpo; lifecycle `LC-01..LC-06` e upload/download por rota observados como `ok`, com hang residual de teardown documentado nas suítes integradas. Runbook e `.env.template` atualizados. Status permanece PILOTO CONTROLADO; multi-instância continua bloqueada até adapter/credenciais reais de object storage + draft durável/retenção. |
| Phase 8.1 — Redis Draft Store (PILOTO CONTROLADO) | `docs/ops/evidence/latest/imob-chat-document-intake/phase-8-1-redis-draft-store.md` | Draft store abstraído em `redis|memory` para o intake IMOB. `DRAFT_STORE=redis` passa a ser o modo preferencial do runtime; `memory` permanece como fallback explícito em dev/test. `consumeDraft` ficou fail-closed por `tenantId/workspaceId`, `confirm` preservou `DRAFT_EXPIRED`/`DRAFT_SCOPE_MISMATCH` e reinserção segura do draft quando a criação da run bloqueia. Validação real: unitário host `2/2` limpo, draft store Redis `20/20` limpo em local-docker, `imob-intake-lifecycle` (`LC-01..LC-06`) e `imob-intake-confirm` (`CONF-01..CONF-06`) observados como `ok` com `DRAFT_STORE=redis`, mantendo apenas hang residual de teardown após as asserções. Multi-instância continua `NO-GO`. |
| Phase 8.2 — Object Storage Operational Gate (PILOTO CONTROLADO) | `docs/ops/evidence/latest/imob-chat-document-intake/phase-8-2-object-storage-gate.md` | Revisão do `ObjectStorageProvider` com fail-closed explícito para `STORAGE_PROVIDER=object`. A build atual aceita apenas `OBJECT_STORAGE_ADAPTER=s3-compatible` no contrato, exige `OBJECT_STORAGE_BUCKET`, `OBJECT_STORAGE_ENDPOINT`, `OBJECT_STORAGE_REGION`, `OBJECT_STORAGE_ACCESS_KEY_ID` e `OBJECT_STORAGE_SECRET_ACCESS_KEY`, e mesmo com env completa retorna erro explícito de que ainda **não há adapter real instalado** nesta build. Suite `storage.provider.test.ts` passou com cobertura de bucket obrigatório, prefixo, escopo `tenant/workspace`, bloqueio de path traversal, client fake e falhas explícitas de configuração. Sem env/bucket real e sem SDK instalado, não houve smoke real; multi-instância permanece `NO-GO`. |
| Phase 8.3 — Upload Retention Cleanup (PILOTO CONTROLADO) | `docs/ops/evidence/latest/imob-chat-document-intake/phase-8-3-upload-retention-cleanup.md` | Política de retenção para uploads do intake IMOB implementada com sweep por `createdAt` e escopo `agentSlug=imob-intake`, usando o `StorageProvider` da Fase 8.0 para `exists/deleteObject`. O worker fica desabilitado por padrão (`UPLOAD_CLEANUP_ENABLED=false`) e, quando habilitado, opera em `dry-run` por padrão (`UPLOAD_CLEANUP_DRY_RUN=true`). A deleção efetiva só remove o registro de `uploaded_documents` após confirmar a remoção do objeto; falha de delete ou objeto ausente preservam o registro. Evidência real: suíte focada `upload-retention-service.test.ts` validada em local-docker com `8/8` asserts passando, cobrindo expiração, não-expirado, dry-run, escopo tenant/workspace, chave legada segura e fail-closed em erro/not_found. Multi-instância permanece `NO-GO` sem object storage real. |
| Phase 8.3.1 — Upload Retention Teardown Hardening (PILOTO CONTROLADO) | `docs/ops/evidence/latest/imob-chat-document-intake/phase-8-3-1-upload-retention-teardown.md` | Isola a causa do hang residual da suíte `upload-retention-service.test.ts`: import de `createLogger` via barrel `@eiah/core`, que reexportava filas/eventos/DB e deixava handles laterais abertos, além de puxar `@repo/db` no host. A correção troca o import para `@eiah/core/logging/logger`, preservando a política de cleanup. Evidência real: host local passou com exit `0`, e o runtime canônico `docker exec eiah-api ... upload-retention-service.test.ts` passou com `8/8` testes e exit `0`, eliminando a necessidade de interrupção manual. |
| Phase 8.4 — Intake Observability (PILOTO CONTROLADO) | `docs/ops/evidence/latest/imob-chat-document-intake/phase-8-4-intake-observability.md` | Observabilidade mínima adicionada ao piloto IMOB Document Intake com eventos estruturados e counters sem PII para `storage_provider_mode`, `draft_store_mode`, `upload_received`, `draft_created/consumed/expired/scope_mismatch`, `upload_retention_skipped/candidates/deleted/failed` e `object_storage_gate_failed`. Os counters passaram a ser expostos também em `/metrics/prom`, o runbook foi atualizado com as métricas mínimas do piloto, e os testes focados passaram em host e local-docker, incluindo validação explícita de `reasonCode` seguro no gate de object storage e de imports diretos sem barrel amplo `@eiah/core`. |
| Phase 8.5 — Object Storage Real Adapter Preflight (PILOTO CONTROLADO) | `docs/ops/evidence/latest/imob-chat-document-intake/phase-8-5-object-storage-real-adapter-no-go.md` | Pré-flight operacional da integração real `s3-compatible`. A fase parou na pré-condição: `STORAGE_PROVIDER`, `OBJECT_STORAGE_ADAPTER`, `OBJECT_STORAGE_ENDPOINT`, `OBJECT_STORAGE_REGION`, `OBJECT_STORAGE_BUCKET`, `OBJECT_STORAGE_ACCESS_KEY_ID`, `OBJECT_STORAGE_SECRET_ACCESS_KEY` e `OBJECT_STORAGE_FORCE_PATH_STYLE` estavam ausentes no host, e a build segue sem SDK/adapter real instalado. A decisão documental foi fixada em nível de adapter: `Cloudflare R2` como alvo preferencial do primeiro smoke e `AWS S3` como alternativa compatível, sem declarar qualquer provider como provisionado. Nenhuma integração falsa foi criada; o gate fail-closed existente permaneceu intacto e a suíte `storage.provider.test.ts` passou com exit `0`. Decisão: object storage real `NO-GO`, multi-instância `NO-GO`. |

## IMOB Chat — blocked_run_resolution Smoke (2026-06-23)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Smoke API/runtime da intent blocked_run_resolution — supressão de blocos detalhados | `docs/ops/evidence/latest/blocked-run-resolution-smoke/smoke-results.json` | Prova por smoke API real (`resolve-turn`) em 2026-06-23 que a intent `crm.case.blocked_run_resolution` retorna apresentação compacta: texto `Bloqueio ativo / Pendência crítica / Também faltam / Owner / Próximo passo` presente, `blocks=[]`, `caseBrief/preparedFollowUp/actionableChecklist/handoffPack` ausentes, `widget=null` (suprimido pela correção de `?? → !== undefined` em `applyCanonicalJourneyToResolvedData`, linha 3306 de `imobCrmBusinessRead.ts`), `card.title=Bloqueio do atendimento`. 12/13 checks OK (falha residual: textarea headless, não produto). `chat.tsx`, `ChatAgentLauncher`, `packages/core`, schema Prisma e migrations intocados. Escopo limitado à correção desta intent específica. |

## IMOB Chat — Visit Slot Collection Smoke (2026-06-24)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Patch de coleta estruturada de slots para missão visit.schedule — engine emite slotCollection schema-driven | `ops/evidence/latest/imob-visit-slot-collection-smoke-2026-06-24.md` | Prova por execução real de testes (7/7 novos + 134/134 regressão imob-lead-continuity) que: engine emite payload `slotCollection` com os quatro campos mínimos (`propertyId`, `visitorName`, `visitorPhone`, `preferredDate`) quando `visit_missing_property`; texto livre vira `propertyTextCandidate` sem gerar `propertyId` falso; continuidade faz lookup DB por texto candidato (auto-vínculo se único hit, `propertyCandidates` se múltiplos, preserva candidato se zero hits); sem `propertyId` resolvido status permanece `collecting` (não agenda); com todos os slots e `propertyId` resolvido → `ready_for_review`; `ImobSlotCollectionCard` aceita prop `prefilled` para valores pré-conhecidos; lógica reside inteiramente em engine/continuidade, sem regra cognitiva no `ChatAgentLauncher`. Status: **parcial** — validação E2E e Playwright pendentes. |

## IMOB Chat — Visit Mission Preservation Fix (2026-06-24)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Fix de orquestração: blocked_run_resolution em visit.schedule não desvia para captação genérica | `ops/evidence/latest/imob-visit-slot-collection-smoke-2026-06-24.md` | Prova por testes unitários (11/11 novos + 134/134 regressão) que: `buildBlockedRunResolutionCtas()` usa CTA "Vincular imóvel da visita" / `nextMessage: "vincular imóvel da visita"` quando `caseContext.flow === "visit.schedule"` com imóvel pendente; `buildBlockedRunResolutionNextStep()` retorna "vincular imóvel da visita" (não "cadastrar imóvel") no mesmo contexto; "vincular imóvel da visita" classifica como intent `visit` (score=4) não `capture` (score=3); CTA de captação genérica (flow ≠ visit.schedule) mantém "cadastrar imóvel" sem regressão; `hasPendingFieldSignalForActiveFlow()` em `imobTurnResolver.ts` tem branch `visit.schedule` que retém fluxo quando mensagem contém termos de imóvel/propriedade/data; `ChatAgentLauncher`, checks/gates, handlers deferred Phase 4.3 e infra S3/R2/Redis/J360 intocados. Status: **parcial** — dist requer rebuild (pnpm build falha por erro pré-existente rootDir; servidor deve rodar em modo dev `tsx watch` para refletir patches). |

## IMOB Visit — Runtime Rebuild Smoke (2026-06-25)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Confirmação de ambiente pós-merge: fix de visit.schedule em efeito no runtime local | `ops/evidence/latest/imob-visit-runtime-rebuild-smoke-2026-06-25.md` | Prova que: fix de visit slot collection + mission preservation está no source e no dist local (Jun 25 06:54); dev server roda em `pnpm dev` (tsx watch), servindo TypeScript diretamente sem depender do dist; 145/145 testes passando (11 novos + 134 regressão); texto antigo "Ainda falta vincular o imovel da visita" removido do source e dist; CTA "vincular imóvel da visita" presente nas linhas 2082/2156 do dist compilado; `slotCollection` emitido na linha 417 do engine compilado; `pnpm build` reporta TS6059 (rootDir — pré-existente, não relacionado ao fix de visita) mas `noEmitOnError` não configurado → dist é emitido mesmo com erros; `check:src-dist-route-parity` ok. Causa raiz do rootDir documentada, fix proposto como PR separado `fix(build): align api tsconfig rootDir for deployable dist`. Status: **parcial** — E2E browser e staging pendentes; noEmitOnError=false cria risco de build CI não-honesto. |
| Binding canônico de confirmação textual para directed action IMOB | `ops/evidence/latest/imob-directed-action-confirmation-binding-2026-06-25.md` | Prova que o runtime/contrato IMOB passou a persistir `pendingAction` canônica com `actionId`, `sourceActionId`, `caseId`, `threadId`, `entityType`, `journey` e status governado; que `confirmo` é interceptado antes de reclassificação para confirmar somente a ação pendente original; que mismatches/ausência/ambiguidade falham fechado com novos reason codes; e que `ChatAgentLauncher`, billing, economy, entitlements e catálogo estrutural permaneceram intocados. |

## RBAC / GuardrailLedger P0 (2026-06-28)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| TenantPolicyStore fail-closed enforcement | `ops/evidence/latest/tenant-policy-store-enforcement-2026-06-28.json` | Prova por execucao real de `pnpm check:rbac-fail-closed` e da suite core que `TenantPolicyStore.ts` e `TenantPolicyStore.js` permanecem sem fallback permissivo, sem `return true` legado e com os reason codes fail-closed vigentes. |
| RBAC scope fail-closed end-to-end | `ops/evidence/latest/rbac-scope-fail-closed-2026-06-28.json` | Prova por execucao real das suites core, HTTP e `agents-policy-fail-closed` que denies normais seguem `403` com `reasonCode` propagado e sem mascaramento por fallback 500. |
| GuardrailLedger persistente no caminho RBAC | `ops/evidence/latest/guardrail-ledger-rbac-events-2026-06-28.json` | Prova por execucao real do check anti-noop, da suite RBAC e do build do `@eiah/core` que allow/deny continuam persistindo via `recordGuardrailLedger` e `recordGuardrailAudit` sem regressao para placeholder. |
| CI anti-regressao allow-all / no-op | `ops/evidence/latest/rbac-guardrail-ci-anti-regression-2026-06-28.json` | Prova que os gates `check:rbac-fail-closed` e `check:guardrail-ledger-noop` existem, passam localmente e estao integrados ao `ci.yml` e ao `critical-dod.yml`. |

## Approval IMOB sem bypass (2026-06-28)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| ApprovalGate IMOB fail-closed | `ops/evidence/latest/imob-approval-gate-2026-06-28.json` | Prova por execucao real das suites de gate e piloto que o ApprovalGate IMOB bloqueia HIGH/CRITICAL sem approval valido, bloqueia approval expirado/invalido e mismatch de tenant/workspace, e que o runtime auditado nao usa mais `humanApprovalGranted: true` hardcoded. |
| Acao critica IMOB sem side effect sem approval | `ops/evidence/latest/imob-critical-action-no-side-effect-without-approval-2026-06-28.json` | Prova por execucao real da suite do worker que acoes criticas HIGH sem approval valido sao bloqueadas antes de `updateCase`, registram `case.action.blocked` com `reasonCode` e nao produzem side effect de mutacao. |
| Surface de receipt/bundle/approval do IMOB | `ops/evidence/latest/imob-approval-receipt-surface-2026-06-28.json` | Prova por execucao real do worker e dos checks P0 que a trilha auditavel continua expondo `approvalDecision`, `approvalStatus`, `receiptPath` e `bundlePath` no payload do caso, e que o receipt canon mantem `ApprovalReceipt` com decisao de approval. |

## P0-B2 Redis Worker Ownership Lease — Runtime Evidence (2026-07-01)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Gate `duplicateSideEffects=0` executado contra Redis real local | `ops/evidence/latest/p0-b2-redis-ownership-runtime-2026-07-01.md` | Prova por execucao real de `EIAH_RUN_REDIS_REAL_TESTS=true REDIS_URL=redis://127.0.0.1:6379 pnpm test:worker-ownership-lease` contra container `eiah-redis` local que: duplo opt-in (`EIAH_RUN_REDIS_REAL_TESTS=true` + `REDIS_URL`) ativa o teste; baseline sem flag skipa corretamente; 10/10 testes passam; `duplicateSideEffects=0` executa (nao skipa); `acquiredCount === 1`; `sideEffectCount === 1`; `duplicateSideEffects === 0`; SET NX PX Lua CAS garante exclusao mutua real; `check:worker-topology` ok; `@eiah/core typecheck` limpo. Classificacao: local-runtime evidence — nao staging/prod. P0-B permanece parcial ate decisao de politica ou evidencia de staging. |

## Docs normativos — Links internos (2026-07-02)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Gate de integridade de links internos dos docs normativos | `ops/evidence/latest/docs-link-integrity-2026-07-02.md` | Prova por execucao real que `AGENTS.md`, `CLAUDE.md`, `CODEX.md`, `IA_EIAH.md` e todos os arquivos Markdown de `docs/architecture/` passam por validacao automatica de links internos; que os links mortos auditados foram removidos; e que a CI agora bloqueia regressao documental com `pnpm check:docs-link-integrity`. |

## Billing webhook signature constant-time (2026-07-02)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Evidencia minima do gate HTTP e do helper de assinatura do webhook billing | `ops/evidence/latest/billing-webhook-signature-2026-07-02.md` | Prova por execucao real que o webhook billing aceita assinatura valida tanto em valor cru quanto em `sha256=<assinatura>`, rejeita assinatura invalida/malformada/com tamanho divergente sem `throw`, e usa verificacao constant-time com `crypto.timingSafeEqual` no caminho real da rota. |

## P2 HIGH evidence recency (2026-07-02)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Gate de recencia da evidencia P2 HIGH | `ops/evidence/latest/p2-evidence-recency-2026-07-02.md` | Prova por execucao real que o inventario P2 HIGH atual continua coerente no check funcional, mas que o artefato `ops/evidence/latest/p2-high-global-coverage.json` esta stale para o limite de 30 dias; que o novo gate `check:p2-evidence-recency` falha fechado quando a evidencia envelhece; e que a CI agora bloqueia esse drift de recencia. |

## P2 HIGH global coverage regeneration (2026-07-02)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Regeneracao real do artefato P2 HIGH | `ops/evidence/latest/p2-high-global-coverage-regeneration-2026-07-02.md` | Prova por execucao real que `ops/evidence/latest/p2-high-global-coverage.json` passou a ser gerado por script, com `generatedAt` renovado, `coveredActions` alinhadas ao inventario HIGH atual e `uncoveredActions=[]`; e que os gates `check:p2-high-global-coverage` e `check:p2-evidence-recency` passam apos a regeneracao. |

## Audit criticality by content (2026-07-02)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Gate `audit:criticality` validando conteudo minimo por arquivo critico | `ops/evidence/latest/audit-criticality-content-2026-07-02.md` | Prova por execucao real que o gate `audit:criticality` continua validando existencia e passou a validar tamanho minimo e marcadores estaveis por arquivo critico, falhando fechado para arquivo vazio, truncado ou sem sinais essenciais de approval, reconciliacao, roadmap, lease e Redis fail-closed. |

## White-label runtime gap (2026-07-02)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Formalizacao conservadora do gap de white-label runtime | `ops/evidence/latest/white-label-runtime-gap-2026-07-02.md` | Prova por leitura real de docs, rotas, middlewares, contratos e superfices web que o repositorio ja possui branding de sessao, isolamento tenant/workspace, entitlement fail-closed e vocabulario de white-label em billing, mas ainda nao possui `partnerId` em runtime, partner/domain routing verificavel, segregacao por parceiro nem gates de nao-regressao suficientes para declarar white-label runtime governado como fechado. |

## Brand Kit de Marketing — Extração de Design System (2026-07-02)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Extração documental do design system real (tokens, tipografia, componentes, superfícies-herói) | `ops/evidence/latest/brand-kit-extraction-2026-07-02.md` | Prova por leitura real de `apps/web/tailwind.config.ts`, `apps/web/src/styles.css` e componentes de UI (RunViewer, GovernancePanel, ImobCommandCenter, ImobApprovalContextCard, ThreadPanel, VerticalSelectorBar, WorkbenchPanelCard) que toda cor/fonte/token do brand kit cita `arquivo:linha` real; confirma ausencia de assets estaticos do frontend (zero logo/favicon/icone) e de biblioteca de icones; localiza as 7 superficies-heroi pedidas com rota real; nenhum codigo de produto foi alterado (somente leitura). Status `parcial` — extracao e localizacao evidenciadas; pecas finais de campanha permanecem `proposta` ate producao/aprovacao do CEO. |

## Fase 3 — dividas documentadas closure (2026-07-02)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Consolidacao conservadora dos PRs 6-9 da Fase 3 | `ops/evidence/latest/fase-3-dividas-documentadas-closure-2026-07-02.md` | Prova por releitura real das evidencias, documentos e gates das frentes F-07, F-08, F-09 e F-10 que a Fase 3 evidenciou documentacao, checks e guardrails leves, mas nao autoriza declarar compatibilidade multi-versao completa, entrypoint unico engine-side, limpeza total do legado tracked ignored ou white-label runtime governado como totalmente fechados. |

## IMOB Knowledge Base v1 (2026-07-02)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Fundacao documental governada da IMOB Knowledge Base v1 | `ops/evidence/latest/imob-knowledge-base-v1-2026-07-02.md` | Prova por execucao real que `knowledge/imob/` passou a ter manifesto versionado, schemas locais, 5 entries seed conservadoras, regras de seguranca obrigatorias, check deterministico `check:imob-knowledge-base` ligado ao CI e nenhuma alteracao de runtime, launcher, engine, billing, Redis ou Agent Protocol. |
| Loader engine-side deterministico da IMOB Knowledge Base em shadow mode | `ops/evidence/latest/imob-knowledge-loader-shadow-2026-07-02.md` | Prova por execucao real que a KB IMOB agora possui loader backend deterministico e fail-closed a partir do manifesto, com preservacao dos campos de governanca, snapshot auditavel `shadowMode: true`, teste focado verde e nenhuma alteracao de UX, `ChatAgentLauncher` ou resposta final do chat. |
| Integracao governada da IMOB Knowledge Base no engine-side IMOB | `ops/evidence/latest/imob-knowledge-engine-integration-2026-07-02.md` | Prova por execucao real que o `imobTurnResolver` passou a anexar `knowledgeContext` governado e auditavel ao fluxo `search_knowledge`, com mapa deterministico intencao -> entry, degradacao segura para temas sensiveis, provenance/source obrigatorios, falha fechada da KB e nenhuma alteracao de `ChatAgentLauncher` ou UX ampla. |
| Plano conservador de rollout da IMOB Knowledge Base | `ops/evidence/latest/imob-knowledge-rollout-plan-2026-07-02.md` | Prova por leitura real dos artefatos da KB IMOB e execucao dos checks documentais que existe um plano operacional governado para promover a capacidade de `shadow` para `pilot` e `small`, com metricas obrigatorias, gates de promocao, rollback explicito e status conservador sem ativacao real neste PR. |
| Shadow run operacional controlado da IMOB Knowledge Base | `ops/evidence/latest/imob-knowledge-shadow-run-2026-07-02.md` | Prova por execucao real de um ciclo shadow deterministico com 10 cenarios controlados, `kbMatchRate=0.8`, `kbNoMatchRate=0.1`, `sensitiveBlockRate=1`, `provenanceCoverage=1`, `knowledgeContextErrorRate=0`, `auditGap=0`, `duplicateSideEffects=0` e zero regressao user-facing, sem alterar `ChatAgentLauncher`, `apps/web` ou ativar `pilot/small`. |
| Pacote documental de pilot readiness da IMOB Knowledge Base | `ops/evidence/latest/imob-knowledge-pilot-readiness-2026-07-02.md` | Prova por releitura do plano de rollout, do shadow run verde e dos contratos engine-side que o repositorio agora possui criterios de elegibilidade por tenant/workspace, gates de entrada/permanencia/rollback, politica de revisao humana e owners minimos definidos para um `pilot` controlado futuro, sem ativar `pilot`, `small`, `ChatAgentLauncher` ou `apps/web` neste PR. |
| Pacote documental de pilot activation gate da IMOB Knowledge Base | `ops/evidence/latest/imob-knowledge-pilot-activation-gate-2026-07-02.md` | Prova por releitura da readiness, do shadow run verde e dos artefatos engine-side que o repositorio agora possui activation gate conservador para um `pilot` futuro, com pendencia explicita `PENDING_REAL_TENANT_SELECTION`, pendencia explicita `PENDING_REAL_OWNER_ASSIGNMENT`, janela operacional ainda nao declarada, gates de ativacao/rollback e confirmacao de que nenhum `pilot` ou `small` foi ativado neste PR. |

| Correcao de drift visual do badge de pilot na UI IMOB | `ops/evidence/latest/imob-pilot-badge-drift-fix-2026-07-02.md` | Prova por inspeção e teste focado que a UI IMOB deixou de renderizar `Piloto controlado` por hardcode local, passou a usar o rótulo neutro `Contexto IMOB`, manteve `ChatAgentLauncher`/backend/engine intocados e nao comunica pilot operacional sem `tenant/workspace`, owners, janela e evidência real por ciclo. |

## IMOB captacao intake flow (2026-07-02)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Correcao da duplicacao de mensagens assistente no historico/persistencia do Chat IMOB | `ops/evidence/latest/imob-assistant-message-dedupe-fix-2026-07-02.md` | Prova por execucao real de teste focado e regressao do PR-A que o placeholder assistente pre-run deixou de ser persistido no chat IMOB, que a reconstrucao de historico passou a aceitar fingerprint assistente estavel alem do fallback por `runId`, e que textos como `Preencha os campos abaixo...` e `Atualizei o cadastro...` deixam de duplicar por causa do registro inicial sem `runId`, sem alterar `ChatAgentLauncher`, backend/engine ou ativar `pilot/small`. |
| Correcao da persistencia/restauracao de `slotCollection` no historico do Chat IMOB | `ops/evidence/latest/imob-slotcollection-history-fix-2026-07-02.md` | Prova por execucao real de teste focado e regressao PR-A/PR-B que o payload assistente passou a persistir `slotCollection` no `metadata`, que `mapStoredMessageToChat(...)` voltou a reidratar o card de coleta apos reload com degradacao segura para payload malformado e que o historico legado sem `slotCollection` segue compativel, sem alterar `ChatAgentLauncher`, backend/engine ou ativar `pilot/small`. |
| Correcao do eco artificial de CTA, submit e autoprompt estruturado no Chat IMOB | `ops/evidence/latest/imob-suppress-user-echo-fix-2026-07-02.md` | Prova por execucao real de teste focado e teste IMOB existente que `suppressUserEcho` passou a bloquear `appendMessage` e `persistMessage` de mensagens user artificiais no `chat.tsx`, impedindo que `cadastrar imóvel`, `Salvar cadastro` e autoprompts estruturados conhecidos aparecam como fala natural do usuario, sem alterar `ChatAgentLauncher`, backend/engine ou ativar `pilot/small`. |
| Correcao do drift de labels tecnicos e duplicacao de CTA no cadastro de imovel | `ops/evidence/latest/imob-captacao-intake-flow-fix-2026-07-02.md` | Prova por execucao real de teste focado e regressao do painel contextual que o card de coleta IMOB passou a renderizar labels humanos para `propertyType/goal/cep/city/address`, que o chat bloqueia `slotCollection` duplicado quando o turno ja possui `message.form`, que a copy visual neutra `Contexto IMOB` foi preservada e que `ChatAgentLauncher`, backend e engine permaneceram intocados. |

## Agent Protocol compatibility matrix (2026-07-02)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Matriz conservadora de compatibilidade multi-versao do Agent Protocol | `ops/evidence/latest/agent-protocol-compatibility-matrix-2026-07-02.md` | Prova por execucao real que `agent-protocol.v1` e a unica versao publicamente evidenciada com schema, baseline, exemplo, policy, changelog, runtime e checks; que `v2` permanece apenas proposta; que compatibilidade `N-1` nao pode ser declarada como suportada sem artefatos publicos suficientes; e que a nova matriz documental possui check proprio de referencias/baselines reais em CI. |

## Gitignore architecture docs tracking fix (2026-07-06)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Correcao da 3a recorrencia de drift entre `.gitignore` e `docs/architecture/` (F-09) | `ops/evidence/latest/gitignore-architecture-docs-tracking-2026-07-06.md` | Prova por commit real `520cddd6f0cabb7bd48fca286ce92bfa46b12f54` e execucao real dos gates `check:tracked-ignored-files` (`ok:true`, `newViolations:0`) e `check:docs-link-integrity` (`ok:true`, 13 arquivos escaneados) que `docs/architecture/chat-runtime-entrypoint-debt.md`, `docs/architecture/fase-3-dividas-documentadas-closure.md` e `docs/architecture/white-label-runtime-gap.md` — antes ignorados pelo `.gitignore` e nao rastreados, apesar de referenciados por PRs #182/#185/#187 — agora estao versionados junto dos 5 arquivos legado; registra explicitamente que a decisao D8 (padrao enumerado vs `!docs/architecture/*.md`) permanece pendente do CEO e que a causa estrutural do drift nao foi eliminada, apenas o sintoma atual. |


## API global error handling — PR-API-ERRORS-01 / N-13 (2026-07-07)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Middleware de erro 4-arg + asyncHandler centralizado + process handlers em `apps/api/src/index.ts` (N-13, D17 ratificado) | `ops/evidence/latest/api-global-error-handling-2026-07-07.md` | Prova por execucao real (`api-global-error-handling.test.ts`, `pass:3/fail:0`, sem Postgres/Redis) que uma rejeicao sincrona ou assincrona (padrao N-10) num handler de rota retorna 500 governado `{ok:false, reasonCode:"INTERNAL_ERROR"}` sem vazar stack ao cliente e sem penduar a requisicao, e que uma resposta 403 ja enviada por um gate existente nao e reclassificada; prova que `createGovernedRouter()` (novo, envolve `.get/.post/.put/.patch/.delete` em `asyncHandler` na criacao do router, sem exigir alteracao por handler) cobre os 8 arquivos de rota amostrados na triagem original de N-13 com 7 alteracoes de 1 linha (imob.ts cobre tambem os 33 handlers de imobCrmRouter.ts, que registra no mesmo router); confirma ausencia de regressao rodando os testes HTTP reais que existem em `main` para os 7 routers alterados (`imob.knowledge.search.contract.test.ts` pass:3/fail:0 incluindo assercao de 403; `billing.*` pass:17/fail:0 combinado; `runs.imob-action` pass:1/fail:0; `marketplace.installations.activate` pass:3/fail:0; suite de continuidade de lead pass:147/fail:0) e confirmando via `git stash` que as 2+8 falhas de `workspace.memberships.contract.test.ts`/`ledger-bundle.contract.test.ts` sao pre-existentes em `main`, nao introduzidas por esta mudanca; registra como limitacao explicita que os testes dedicados de GATE-01/GATE-02 nao existem na base `main` desta PR e por isso nao foram executados diretamente. |

## Orphan test detection gate — N-12 (2026-07-06)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Gate estrutural (`check:orphan-tests`) para o padrao recorrente de teste real sem nenhuma referencia em package.json/CI (F-06, N-10, N-11) | `ops/evidence/latest/orphan-tests-gate-2026-07-06.md` | Prova por execucao real (`scripts/checkOrphanTests.ts`, validado contra 3 casos conhecidos-cobertos e 3 confirmacoes manuais independentes) um inventario inicial de 288 arquivos `*.test.ts`/`*.test.tsx` com **210 orfaos**, incluindo `billing.webhook-signature.test.ts` (F-06, deliberadamente nao adicionado a allowlist) e, como achado adicional transparente, os proprios testes novos de GATE-01/02 desta sessao; confirma via validacao antes/depois nas branches `pr-imob-fix-n10` e `pr-core-n11` (sem misturar commits) que `imob.command-center.smoke.test.ts` e `imob.chat.resolve-turn.contract.test.ts` saem da lista apos as correcoes; registra a decisao de gate warn-only (`continue-on-error`) em `ci.yml` com plano de promocao explicito condicionado a um PR-CI-01 de triagem do volume, nao aberto nesta sessao. |


## CI unit suite job — PR-CI-02 (2026-07-07)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Job `ci_unit_suite` em `ci.yml` (sem `services:`) para o Grupo B (unit/mock puro) da classificacao D14, ratificada como Opcao 3 hibrida | `ops/evidence/latest/ci-unit-suite-2026-07-07.md` | Prova por execucao real que a suite unit/mock do Grupo B segue viavel em `ci.yml` sem infra, mas precisou de um ajuste corretivo apos a 1a execucao do GitHub Actions: a validacao local inicial rodou `162` arquivos com `pass:711/fail:0`, enquanto o runner limpo do PR expôs `1` falha unica (`apps/workers/action-runner/src/index.test.ts`, `ERR_MODULE_NOT_FOUND` para `@repo/db/dist/index.js`), levando a exclusao desse arquivo do manifesto para preservar o escopo sem `services:` e sem build amplo; o estado corrigido fica em `161` arquivos, `pass:710/fail:0` via `pnpm test:ci-unit-suite`, unico env var `TSX_TSCONFIG_PATH`, e `blockingOrphanCount` `210 -> 50` (delta liquido `-160`, porque o arquivo removido do manifesto volta a aparecer como orfao deliberado nesta PR). Documenta, no total, `16` arquivos excluidos do manifesto: 11 com falha real de assercao, 2 reclassificados para o Grupo A (import transitivo de `@repo/db` via `capabilityExecution`/`imobArtifactCapabilities`), 1 estrutural (`tests/multitenant/tenant-isolation.test.ts`), 1 falso positivo do N-12 (`provider-boundary-enforcement.test.ts`) e 1 incompativel com runner limpo sem build amplo (`apps/workers/action-runner/src/index.test.ts`); os gaps de heuristica D14/N-12 e a dependencia de build do worker ficam registrados como frentes futuras, nao corrigidos nesta PR. |

## Auto-merge da PR de evidência APE — decisão D23 (2026-07-08)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| Auto-merge condicionado (path-scoped, evidence-index revalidado) na PR automatica de renovacao de evidencia APE, branch fixa `bot/ape-weekly-evidence` em vez de sufixo por `run_id` (D23 ratificado pelo CEO, mitigacao de N-20) | `ops/evidence/latest/ape-weekly-automerge-decision-2026-07-08.md` | Prova por YAML validado (`yaml.safe_load`, 17 steps, `id: create-evidence-pr` presente, `if: steps.create-evidence-pr.outputs.pull-request-number != ''` presente no step de guarda) e por `check:evidence-index` `ok:true` apos indexacao que o novo step de auto-merge so roda quando a action `peter-evans/create-pull-request@v7` de fato cria/atualiza uma PR, falha explicitamente (sem habilitar auto-merge) se o diff tocar qualquer path fora da pasta ops/evidence/latest ou de `docs/EVIDENCE_INDEX.md`, revalida `check:evidence-index` como segunda camada de seguranca antes do merge, e usa `gh pr merge --auto --squash` (auto-merge nativo do GitHub, que so completa apos os required status checks passarem, nao contorna branch protection); documenta com honestidade que a raiz de N-20 (branch por `run_id` acumulando PRs nunca revisadas, confirmada via a PR stale `bot/ape-weekly-evidence-28380235280` de 2026-06-29) e endereçada pela branch fixa, e que a suficiencia das permissoes `contents`/`pull-requests: write` ja declaradas e o respeito a branch protection sao comportamento documentado da plataforma, nao verificado por execucao real nesta sessao (sem acesso a API do GitHub Actions). |

## Realocação de gates recorrentes — decisão D25 (2026-07-08)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| `check:p3-stability-recurring` e `check:p4-trackp-rollout` saem de `ci.yml` (bloqueio de PR) e passam a rodar bloqueantes em `ape-weekly.yml` (D25 ratificado pelo CEO); `check:p1-reconciliation-recurring` permanece em `ci.yml` | `ops/evidence/latest/gate-relocation-decision-2026-07-08.md` | Prova por validacao real (nao fabricada) contra a evidencia da branch `ops-ape-weekly-run41` (7bc8ace) que `check:p1-reconciliation-recurring` retorna `ok:true` (evidencia dentro da janela de 14 dias) enquanto `check:p3-stability-recurring`/`check:p4-trackp-rollout` retornam `ok:false` honesto (`economy_stability_not_recurring`/`ape_cycles_not_green`, NO_GO real por `check:e2e-recency`/`check:backup-restore` vencidos, D2/D24 em andamento) — confirmando que a realocacao resolve o deadlock circular (PR de conserto bloqueada pelo proprio gate que teria que consertar) sem alterar nenhum limiar, metrica ou o codigo dos dois scripts; documenta a avaliacao explicita do mecanismo `rolloutMode=enforce` de `scripts/ci/ape_cycle_weekly.cjs` como escopo diferente (cobre os 15 checks de um unico ciclo novo, nao a recorrencia dos ultimos 3 ciclos publicados) e por isso descartado em favor de 2 steps bloqueantes dedicados logo apos `Run APE cycle`; registra a condicao de reavaliacao futura (quando D2/D24 resolverem o NO_GO subjacente) como decisao explicita pendente, nao automatica. |

## Fix de ordem — N-24: publicar evidência APE mesmo em ciclo NO_GO (2026-07-09)

| Assunto | Arquivo | O que prova |
| --- | --- | --- |
| `id: run_ape_cycle` no step de geracao + `if: !cancelled() && steps.run_ape_cycle.outcome == 'success'` em todos os 8 steps entre `Run APE cycle` e `Create PR with renewed APE evidence` em `ape-weekly.yml`, sem reordenar nenhum step (N-24, achado na 1a execucao real de 2026-07-09, commit `6bdca45`) | `ops/evidence/latest/ape-weekly-reorder-n24-2026-07-09.md` | Prova por leitura direta do run real de 2026-07-09 (falha em `check:p3-stability-recurring` com evidencia fresca `run44` gerada mas nunca publicada porque todos os steps seguintes, incluindo `Evidence index`/`Upload artifacts`/`Create PR`, foram pulados por padrao apos a falha) e por leitura de `scripts/checkApeCycleHardMetrics.ts` que o step `Hard metrics` (pre-existente, anterior a D25) tem o mesmo padrao de falha condicionada a `hardMetricsGo:false` que P3/P4 — confirmando que o bug de "NO_GO derruba publicacao" ja existia estruturalmente antes de D25, que so adicionou mais dois pontos de falha na mesma cadeia; confirma tambem, por leitura de `checkApeEvidenceLinkage.ts`/`checkProviderPricingRecency.ts`, que nenhum outro step da cadeia tem esse comportamento; documenta a escolha da Opcao 2 estendida (if condicional em 8 steps, sem mover nenhum de posicao) sobre a Opcao 1 (reordenar), por resolver de uma vez tanto a publicacao quanto o efeito colateral descoberto de P3 pular P4 em cascata (perda de observabilidade independente que existia como jobs paralelos em `ci.yml` antes de D25); valida via `yaml.safe_load` que o YAML final tem 18 steps, `id`/`if` corretos nos 8 steps visados, e confirma por inspecao que D25 (job termina vermelho em NO_GO) permanece intacta — um step que roda e falha ainda marca o job como falho ao final, `if:` so controla se o step roda, nao se ele pode falhar; registra como nao verificavel nesta sessao (sem acesso a execucao real do GitHub Actions) o comportamento exato na proxima execucao, recomendando um disparo manual pos-merge. |
